# RFC：節目の一枚絵（key art）— Swift 版（U2）向けメモ

**状態＝提案（未実装）。** 2026-09-09 ユーザー要望を受けて作成。実装は U2（Swift）フェーズ。
本メモは「何を・どこに・どう選び・どう発注するか」を決めるためのもので、コードは一切変更していない。

---

## 1. 背景と位置づけ

ユーザー要望（2026-09-09）＝**ゲーム開始時・重要なイベント・一区切りに一枚絵を出したい。これだけでゲームの質が上がる。絵は Codex に生成させる。**

### 1.1 「グラフィックの向上が主目的ではない」との整合

`design-snapshot.md` 冒頭に **「グラフィックの向上が主目的ではない」** がある。本提案はこれと衝突しない。理由は 4-9 にある。

> 【AIの使いどころ＝制作時（4-9で確定）】「AIによる動的生成」は *実行時にLLMを呼ぶ* 意味ではない。LLMは**制作時**に高品質な素材を大量生成し、実行時はそれをプロシージャルに組み替える。

本提案は **4-9 を文章から絵へ広げるだけ**である。絵は制作時に生成した固定素材で、実行時は既存の状態から選ぶだけ。実行時 LLM は使わない。盤面の描画（DOM グリッド＋文字グリフ）は一切変えない＝「グラフィックの向上」ではない。

### 1.2 位置づけ＝写本の挿画

美術の方向は既に「静謐な写本」（§10.2b）で確定している。一枚絵はその**挿画（illuminated plate）**として置く。装飾ではなく、**節目に一度だけ現れて、次の頁へ送るための間**として機能させる。

---

## 2. ユーザーが決めた仕様（2026-09-09）

| 項目 | 決定 |
|---|---|
| 大きさ | **マップ画面が埋まるくらい**（盤面領域を占める。全画面の暗転ではない） |
| 絵柄 | **白黒・渋い** |
| 記憶 | **一度見た絵は記憶する**（同じ絵を繰り返さない） |
| 人物 | **描く** |

---

## 3. 差し込む瞬間（スロット表・初期セット23枚）

**★組み合わせ爆発を最初に断つ。** 冒険者は150名以上、印5種、極3種、変質3段階あり、掛け合わせると青天井になる。**絵は個体ではなくカテゴリに紐づける。** 下表がその固定表であり、**表に無い瞬間には絵を出さない**。

行番号は `proto/src/web/main.ts`（`M`）と `proto/src/world.ts`（`W`）。TS 側の現行実装での位置であり、Swift ではミラー先の同等地点に置く。

### 3.1 開始（3枚）

| slot | 瞬間 | 発火地点 | 出す条件 |
|---|---|---|---|
| `title` | タイトル画面の地 | `titleScreen()` `M:9236` | 常設（唯一「記憶」の対象外＝毎回出る背景） |
| `world_begin` | 新しい世界の始まり | `chooseDifficultyIfNew()` `M:3530` の直後 | 世界に一度 |
| `descent_first` | 初めて迷宮へ降りる | `startDive()` `M:5739` | 世界に一度 |

### 3.2 一区切り＝世代（4枚）

| slot | 瞬間 | 発火地点 | 出す条件 |
|---|---|---|---|
| `death_loss` | 化石化の告知（喪失の極） | `deathFlow()` `M:7862` | `fossil.tonePole === "loss"` |
| `death_myth` | 同（神話の極） | 同 | `tonePole === "myth"` |
| `death_grudge` | 同（怨念の極） | 同 | `tonePole === "grudge"` |
| `retire` | 退隠（死なない世代交代） | `retireFlow()` `M:3029` | 初回のみ |

`TonePole` は `"loss" | "myth" | "grudge"` の3値（`proto/src/types.ts:5`）。`alien` は極ではなく変質段階（`VariationStage`）なので絵の軸には使わない。

### 3.3 重要イベント＝印（5枚）

`awardSeal()`（`W:878`）が**新規取得で true を返した瞬間**に1枚。印は5種で固定（`types.ts:77`）。

| slot | 印 | 意味 |
|---|---|---|
| `seal_abyss_boss` | `abyss_boss` | 成れの果ての討伐 |
| `seal_requiem` | `requiem` | 因縁の鎮魂 |
| `seal_setpiece` | `setpiece` | 山場の決着 |
| `seal_legend` | `legend` | 伝説の承認 |
| `seal_depth` | `depth` | 深淵への到達 |

### 3.4 重要イベント＝エリアボスの決着（3枚）

`handleBossResolve()`（`M:6156`）の3分岐にそれぞれ1枚。

| slot | 分岐 | 発火地点 |
|---|---|---|
| `boss_slay` | 討つ | `M:6222` |
| `boss_requiem` | 鎮める | `M:6212` |
| `boss_memorial` | 名を呼ぶ（縁ある相手限定） | `M:6194` |

### 3.5 重要イベント＝山場（3枚）

`fossilScene()` 内の setpiece 決着。型は3つ（`proto/content/setpieces.json`）。

| slot | 型 | 発火地点 |
|---|---|---|
| `setpiece_legend_return` | `legend_return` | `M:7576` |
| `setpiece_grudge_hunt` | `grudge_hunt` | `M:7599` / `M:7617` |
| `setpiece_inheritance` | `inheritance` | `M:7650` / `M:7674` |

### 3.6 到達（3枚）

| slot | 瞬間 | 発火地点 |
|---|---|---|
| `abyss_unlock` | 5印が揃い深淵帯が開く | `awardSeal()` `W:886`（5印目）／提示は `promptDescend()` `M:3398` |
| `abyss_first` | 深淵帯へ初めて降りる | `startDive()` `M:5759`（`ABYSS_DEPTH=50`） |
| `ascend` | 奉献の成立＝クリア | `ascendWithRelic()` `M:7297` |

### 3.7 相棒（2枚）

| slot | 瞬間 | 発火地点 |
|---|---|---|
| `companion_fall` | 相棒の死 | `companionDies()` `M:4791` |
| `companion_abandon` | 見捨てる | `fossilizeAbandoned()` `W:499` |

**合計 23 枚。** 一度見たら二度と出ないので、**1周を通して最大23回**しか現れない。頻度の上限を別途設ける必要がない＝「静かな雰囲気」の原則と両立する。

---

## 4. 選択ロジック＝純関数（TS 側に置く）

### 4.1 なぜ TS に置くか

先例が `proto/src/echo.ts` にある。「最期の残響」は **`computeEcho(fossil, worldTime) → Echo | null` という純関数で導出し、`echo-check.ts` が受理ゲートで固定**する形をとった。

同じ形にすると、**Swift 移植時に出し分けロジックを作り直さずに済む**。絵そのものと表示だけが Swift の仕事になる。

### 4.2 形

```
proto/src/keyart.ts          … 純粋・DOM 非依存
  export type KeyArtSlot = "title" | "world_begin" | ... （23 の安定ID・as const で列挙）
  export type KeyArtEvent = { kind: "death"; tone: TonePole } | { kind: "seal"; key: SealKey } | ...
  export function pickKeyArt(ev: KeyArtEvent, world: World): KeyArtSlot | null
        … 該当スロットを返す。既に見ていれば null。world は読むだけ（変更しない）
  export function markArtSeen(world: World, slot: KeyArtSlot): boolean
        … awardSeal と同じ冪等の流儀＝新規なら true

proto/src/keyart-check.ts    … npm run check 同梱（echo-check.ts と同じ位置づけ）
  ・23 スロットが全て pickKeyArt から到達可能（死蔵なし）
  ・同じ (event, world) で必ず同じ結果（決定論）
  ・見た後は必ず null（記憶が効いている）
  ・スロットID と assets の実ファイルが 1:1（欠け・余りを fail）
```

**★golden への追加を推奨。** 現在の `gItems` は `(slot, enchant, 価値)` しか記録せず**基の名前を見ていない**（薙鎌の minDepth 変更で指紋が動かなかった件・PR #412 で報告済み）。同じ轍を踏まないよう、key art の指紋は**スロットIDの列**を記録する。

---

## 5. セーブ互換

**`World.seenArt?: string[]`（任意フィールド）を追加する。**

**SAVE_VERSION は据え置き（9 のまま）。** 本リポジトリの先例どおり、任意追加フィールドは `world.ts migrateWorld` の非破壊バックフィルで吸収している（`Character.loadout`・`World.diveCount`・`Fossil.spells`・`World.homeUnlocked` などが version 9 のまま追加されている）。旧セーブは `seenArt` 欠落 → `[]` でバックフィル＝**これまで遊んできた世界でも、次の節目から絵が出る**。

`tools/stress-save.ts`（55,782 checks）の対象に含める＝旧セーブ×多世代×全難易度での往復を裏取りする。

**記憶の単位は「世界ごと」**（`World` に持つ＝セーブファイル単位）。新しい世界を始めれば絵はまた出る。新しい世界は新しい物語なので、これが自然だと考える（§8 の未決論点①）。

---

## 6. Codex への発注仕様

### 6.1 スタイル・バイブル（23枚で画風を揃えるための固定条件）

| 項目 | 指定 |
|---|---|
| 色 | **グレースケールのみ。** 彩度ゼロ |
| 質感 | 銅版画／メゾチント／墨のにじみ。線が硬すぎず、粒子が残る「渋い」階調 |
| 明度 | **暗部主体。** ゲームの地は `#0e0c09`（墨黒）。絵の平均輝度はそれより明るく、しかし白飛びさせない |
| 周縁 | **矩形の切り口を出さない。** 四辺を地の黒へ溶かす（ビネット／フェード）＝盤面に敷いたとき額縁に見えないように |
| 主題 | 1枚につき1つ。群像でも視線の集まる先は1点 |

**★朱（`--acc #c2452f`）と金泥（`--gold-leaf`）は絵に入れない。** この2色は UI で「不可逆操作」「印・装備中」を意味する記号として使っている。絵に混ざると意味色が薄まる。**色は UI の専有、絵は白黒**という分担にする。

### 6.2 人物の描き方（★重要な制約）

**人物は描く。ただし個人を特定しない。** 後ろ姿・逆光・遠景・顔は陰、のいずれかにする。

理由＝冒険者は名簿だけで150名以上あり、名前・異名・出自は手続き的に組み替えられる。特定の顔を割り当てると、プレイヤーが実際に出会った人物と食い違う。**「誰か」ではなく「その瞬間に立ち会っている人間がいる」ことだけを描く**のが、この作品の物語生成と両立する唯一の描き方だと考える（§8 の未決論点③）。

### 6.3 禁止事項

- **絵に文字を焼き込まない。** i18n で詰む。加えて、絵の上の文字は**コントラスト検査の母集合の外**に出る（§10.2g が閉じているのは CSS の色だけで、画像は判定面にできない）。
- 特定可能な実在の人物・既存作品の意匠を用いない。
- 中央に極端な高輝度を置かない（この上に UI を重ねる可能性を残すため）。

### 6.4 出力規格

| 項目 | 指定 |
|---|---|
| マスタ | 長辺 **2048px** / 8bit グレースケール PNG（可逆・原版として保管） |
| 配布 | 長辺 1536px 前後へ縮小し HEIC もしくは JPEG q85。**1枚 300KB 目安・23枚で 7MB 前後** |
| 縦横比 | **可変に耐える構図にする。** マップ領域は `#mapWrap { flex: 1 }`（`proto/web/index.html:113`）で端末により縦横比が変わる。**中央の 4:5 を安全枠**として主題を収め、その外側は切れてよい |
| 命名 | `art/<slot>.png`（slot は §3 の安定ID。例＝`art/seal_requiem.png`） |

### 6.5 検収

1. **23枚を1枚のコンタクトシートに並べて目視。** 画風のブレ（線の太さ・階調・粒子）が一目で分かる形にする。1枚ずつ見ても揃わない。
2. **機械検査**＝枚数23・全てグレースケール（彩度0）・規定解像度・文字なし（OCR で検出）・命名規約。`keyart-check.ts` に載せる。
3. **ゲーム内で敷いた状態のスクリーンショット**を `tools/visual-check.ts` の対象に足し、盤面に敷いたときに額縁化していないかを見る。

---

## 7. 表示側（Swift）の要件

- **出し方**＝マップ領域に敷く。`.aspectRatio(contentMode: .fill)` ＋ clip。全画面の暗転はしない（ユーザー指定「マップ画面が埋まるくらい」）。
- **消し方**＝**タップで送る。自動で消さない。** 読ませる時間を奪わない。
- **絵の上に文字を置かない。** 説明文は絵の外（シート側）に置く。どうしても重ねる場合は不透明の帯を敷き、その帯を判定面として §10.2g の表に載せる。
- **VoiceOver**＝スロットごとに日本語の `accessibilityLabel` を持つ。絵柄の説明ではなく**何の節目かを言う**（例＝「因縁の鎮魂の印を得た」）。§10.11 の VoiceOver 4点に接続。
- **Reduce Motion**＝フェードインは `prefers-reduced-motion` で無効化し、即時表示にする。§10.2d の RM 分類表に行を足す。
- **高コントラスト**＝絵そのものは差し替えない（白黒ゆえ元から高コントラスト側）。絵の外の文字は既存トークンのまま。

---

## 8. 決定済みの論点（2026-09-09）

### 8.1 記憶の単位＝**世界ごと**（確定）

`World.seenArt` に持つ＝セーブファイル単位。新しい世界を始めれば絵はまた出る。

**決め手は技術的な理由。** 永久記憶にすると保存先がセーブの外（iOS なら UserDefaults 相当）になり、**設定の「セーブ書き出し／読み込み」で持ち運べなくなる**。この作品はセーブが世界そのものなので、絵の既視感だけが世界に付いて来ないのは筋が通らない。加えて「新しい世界は新しい物語」という 4-1 の建て付けとも合う。

### 8.2 枚数＝**23枚を最終形とし、4枚のパイロットで画風を確定してから残り19枚**（確定）

23枚を一度に発注しない。**この企画の最大の失敗要因は画風のブレ**であり、23枚を並べ終わるまでそれが分からないのは危険が大きすぎる。

**パイロット4枚**＝`title` / `death_grudge` / `seal_requiem` / `ascend`。この4つは**背景・人物・象徴・クライマックス**という最も離れた4象限を踏むので、揃えばあとは揃う。

- 第1段：パイロット4枚を発注 → §6.5 の検収（コンタクトシート目視＋機械検査）→ **画風を確定してスタイル・バイブルに実例を追記**
- 第2段：残り19枚を確定したバイブルで発注 → 23枚まとめて再検収

**減らす必要が出た場合の畳み方**（優先順）＝①§3.3 の印5枚を1枚（`seal_any`）に畳む ②§3.5 の山場3枚を1枚に畳む。この2つで 23→15枚になる。**逆に増やす方向へは広げない**（カテゴリを足すと組み合わせ爆発の抑えが緩む）。

## 9. 未決の論点（ユーザー判断が要る）

1. **人物の匿名性**＝§6.2 の「個人を特定しない」規律で良いか。特定の人物像を描きたい場合は、対象を**縦糸のアウレル／ミラ**など固定登場人物に限る必要がある。
2. **TS/web にも表示を入れるか**＝**提案＝導出（`keyart.ts`）だけ TS に置き、表示は Swift のみ。** web は仕様書兼テスト用ビルドであり、絵の同梱は容量に見合わない。導出だけなら受理ゲートで固定できる。
3. **`title` を記憶の対象外にする**（毎回出る背景として扱う）で良いか。

---

## 10. スコープ外

- 盤面（DOM グリッド／文字グリフ）の描画変更。**触らない。**
- 実行時の画像生成。**しない**（4-9＝実行時 LLM の排除）。
- 立ち絵・アニメーション・カットシーン。本メモは静止画1枚のみを扱う。
- 音（節目の効果音）。別途。
- U2（Swift）本体の着手。**本メモは提案であり、着手はユーザーの個別承認事項。**
