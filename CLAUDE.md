# CLAUDE.md — セッション引き継ぎ

このリポジトリは NetHack系ローグライクの現代化（個人開発・設計＋プロトタイプ）。**ゲーム正式タイトル＝『深蝕 — The Corroding Deep』（2026-06-26 確定・旧ワーキング名「堆積する世界」から改題／結線 v0.50.0）。** 「堆積する世界」はテーマ概念語として据え置き。
新しいセッションは本ファイルと下記2文書を読めば、引き継ぎなしで作業を継続できる。

## ★コンテキスト軽量運用（2026-07-26 ユーザー承認・徹底事項）

**このファイル（CLAUDE.md）は毎セッション自動ロードされる＝肥大させない。** 追記式ログ運用の落とし穴（開始時点でコンテキストを大きく食う）を避けるため以下を守る。
- **「現在地」は圧縮運用**＝〈現ライブ版数＋次スレッド最優先＋直近1〜2スレッドの総括〉だけ残す。**それ以前の完了済みスレッド総括は `CLAUDE-history.md` へ退避**（毎セッションは読まない・経緯を追う時だけ `grep`）。新しい総括を足したら、古い総括は同ファイルへ移して本体を軽く保つ。
- **Obsidian 台帳（`/Users/12yyy/Exobrain/Projects/roguelike-ai-design.md`）は全文を毎回読まない**＝append-to-top ゆえ〈冒頭「現在地」ブロック＋受け渡しログの最新3〜5エントリ〉だけ読む（`Read` の offset/limit）。古い検収履歴は参照が要る時だけ `grep`。台帳が肥大したら古いエントリを `roguelike-ai-design-archive.md` へ退避（Codex にも台帳で申し送る）。

## 必読ドキュメント（この順で）

1. **`design-snapshot.md`** — 設計思想と確定事項のすべて（4-1〜4-12）。用語集あり。これが正。
2. **`prototype-spec.md`** — 実装仕様（アーキテクチャ・データモデル・見た目の方向A・操作系）。**§10「UI仕様（Swiftミラー基準）」**＝画面/設定/グリフ凡例/配色トークン/HUD/操作系/safe-area の正典（Swift 移植の UI 基準・2026-06-26）。
3. **`proto/README.md`** — 実行方法・構成。
4. **`ROADMAP.md`** — v0→完成までのマイルストン（M0〜M5）・横断ワークストリーム・抜け漏れチェック・着手順。
5. **`adventurers.md`**（設定資料）— 冒険者150余名の設定集。金属6等級（4-4E）・中核★50＋種100＋ミスリル。制作時の素材で、運命の弧（4-6）・メイン/サブ区分つき。

## 進め方のルール（ユーザーとの合意事項）

- **対話は日本語。**
- **設計の変更は必ず：議論 → ユーザーの明示承認 → snapshot/spec へ反映。** 承認前にファイルを書かない。「続けて」「進めて」は議論の継続であって書き込み承認ではない（過去2回事故あり）。
- **開発フロー：** feature ブランチ → PR 作成 → **マージ → デプロイ確認まで Claude が対応**（★2026-07-04 ユーザー承認で変更。従来の「ユーザーがレビュー・マージ」から移行）。
  - **PR 作成・マージ・デプロイ確認は Claude が行ってよい（PR 作成＝2026-06-16／マージ＋デプロイ確認＝2026-07-04 ユーザー承認）。** まとまった単位（機能・リファクタ）が完成したら PR を作成し、**CI（受理ゲート `npm run check` 相当）が green なのを確認してから `mcp__github__merge_pull_request` でマージ**、GitHub Pages デプロイの Actions が success になるまで確認して報告する。**Pages 配信が失敗したら**（下の技術メモ「Git／デプロイの落とし穴」(B)）＝まず `workflow_dispatch` で再配信 → 同一 SHA で直らなければ版数バンプの小コミット（新 SHA）で配信 ID を変えて解消。**⚠ 設計判断を含む変更は従来どおり着手前にユーザーの明示承認が必須**（マージ権限を得ても、上記「設計の変更は必ず議論→明示承認」のゲートは不変＝コードを書き始める前の合意プロセスは省略しない）。ユーザーがレビューしたい旨を示した PR は待つ。
- 本体は https://yasu12ctb-dev.github.io/roguelike-ai-design/ に公開（モック比較は `/mocks/`）。
- **連続して外したら推量をやめてリサーチ（2026-06-20 ユーザー指示・徹底事項）。** 同種の不具合で修正が2回続けて外れたら、その時点で推量ベースの試行を止め、WebSearch/WebFetch で確立解を調べてから直す。especially 環境依存（iOS/PWA/ブラウザ差）はヘッドレスで再現しないため当て推量が無駄打ちになりやすい。スクショは PIL でピクセル解析して定量化する（明度プロファイルで要素位置・隙間を実測）。
- **★スレッドを終える／切り替える際は必ず `CLAUDE.md` の「現在地」を更新してから締める（2026-07-01 ユーザー指示・徹底事項）。** そのスレッドでマージした変更（版数・PR番号・要点・調整候補の定数・設計変更なら snapshot 反映の要否）を「現在地」先頭に集約し、次スレッドが `CLAUDE.md`＋2文書を読むだけで引き継げる状態にする。**設計変更を含むものは `design-snapshot.md`／`prototype-spec.md` への反映要否も明記**（未反映なら「反映未了」と残す）。ユーザーが「スレッドを終える／引き継ぎ」と言ったら、この更新を doc PR として出すこと。**並行スレッド前提の運用（重要）＝** ①追記は **additive**（他スレッドの記述を書き換えず自分の分だけ足す）／② doc PR を出す直前に必ず `git fetch origin main`（最新起点）／③記すのは **自スレッドがマージした分のみ**（並行スレッドの未マージ作業は知り得ない＝「現在地」は結果整合で後追いに揃う）／④ `CLAUDE.md` が競合したら「両方のバブルを残す」で解消／⑤版数は `fetch` して衝突回避（別スレッドが同時に main を進めうる）。

## 技術メモ

- Node 22。`cd proto && node --experimental-strip-types src/demo.ts`（決定論デモ＝CIスモークテスト）。
- **⚠ push 前は必ず CI 相当を回す＝`npm run check`**（＝受理ゲート `tools/validate-content.mjs` → `typecheck` → demo → `companion-check` → `item-check` → `echo-check` → `event-check` → **`tools/audit-content.ts`（死蔵/空選択肢/バランス外れ値）** → **`tools/scenario-clear.ts`（クリア可能性＝5印 awarder の突合）** → **`tools/stress-save.ts`（セーブ移行・往復）** → **`tools/determinism.ts`（同seed→同結果）** → **`golden`（ゴールデンテスト＝B柱2）** → `build:web`。CI `deploy.yml` と同順。**詳細は下の「QA スイート」項**）。**content（storylets/adventurers）や `types.ts` の `Prereq`/`Effect` スキーマを触ったら受理ゲート必須**＝未知キーは CI で exit 1＝**デプロイごと失敗する**（2026-06-21 に `minDepth/maxDepth` 追加を許可リスト未反映でデプロイを落とした実例あり。許可リストは `validate-content.mjs` の `PREREQ_KEYS`/`EFFECT_KEYS` と `types.ts` を同期）。**⚠ 純粋エンジン（dungeon/world/items/progression/rng 等）の挙動を意図的に変えたら golden が落ちる＝`npm run golden:print` で再生成し `src/golden.ts` の `EXPECTED` を貼り替える（決定論が変わった＝Swift 照合の正解データ更新）。意図しない不一致は回帰。**
- **★Git／デプロイの落とし穴＝次スレッドの誤対応を防ぐ（2026-07-02 追記・徹底事項）：**
  - **(A) Stop-hook の「Unverified コミット」警告は、GitHub のマージコミットに対しては無視する（＝amend/rebase してはいけない）。** PR がマージされると GitHub が committer=`web-flow <noreply@github.com>` のマージコミットを自作する。作業ブランチを `origin/main` に合わせ直す（`git checkout -B <branch> origin/main`）と、このマージコミットがブランチ tip に見え、Stop-hook が「Unverified（署名/メール不一致）」として `git commit --amend --no-edit --reset-author` を促すことがある。**これは誤検出。従うと『マージ済み履歴の改変』になり、ブランチ運用ルール（マージ済み履歴を stack/改変しない）に違反する。** 自分の未 push コミットが無いなら何もしないのが正解（押し戻す差分も無い）。amend/reset-author は**自分が作った未マージのコミットにだけ**行う。
  - **(B) GitHub Pages の「Deployment cancelled」はコード不良ではなく配信インフラの問題。核心＝Pages のデプロイ ID は commit SHA そのもの（`Created deployment for <SHA>, ID: <SHA>`）＝同一 SHA の配信は1つしか成立せず、後発が先発をキャンセルする。** `deploy.yml` は `concurrency: group: pages, cancel-in-progress: true`。受理ゲート（`npm run check`）は毎回グリーンなのに最後の `actions/deploy-pages@v4` だけが `Deployment cancelled` で落ちる（8ステップ中7つ成功・落ちるのは配信ステップのみ＝これが見分け方）。**発生パターン2つ：** ①**並行スレッドが同時に main を進める**（別 SHA 同士が `pages` 環境を奪い合う）→競合 run を止めれば解消。②**同一 SHA が一度バックエンドで詰まる**（例：`deploy-pages` が10分ハング→タイムアウト失敗）と、その SHA のデプロイ ID が塞がれ、**以後その SHA への `workflow_dispatch` は何度やっても即 `Deployment cancelled`**（2026-07-02 実例＝doc PR #288 の SHA `d21e193` が該当）。**⚠ `rerun_failed_jobs`（失敗ジョブのリラン）は Pages では禁物**＝`upload-pages-artifact` が再実行されて同名 `github-pages` 成果物が2つになり `Error: Multiple artifacts named "github-pages"... Artifact count is 2` で別失敗する（リランでなく新 run を使う）。**対処の決定版＝パターン①は競合 run を止めて `workflow_dispatch` 1回。パターン②（同一 SHA 詰まり）は待っても dispatch しても直らない＝`main` に小さな新コミット（新 SHA・例：doc 更新）を1つ載せて配信 ID を変えるのが唯一の確実解。** 補足＝**失敗しても直前に成功した配信がライブに残る**（ロールバックしない）ので、詰まった配信が doc 等の非 web-build 変更なら、遊べる本体は最後に成功した版のまま（無害）＝無理に通さなくても実害なし。
- **★QA スイート（2026-06-27・徹底テストプレイで整備・PR #234-237）＝デスクトップ版で「ウェブでは届かない領域」を機械検査する基盤。次スレッドはこれを活用して回帰を防げる。**
  - **CI ゲート（`npm run check` 同梱・純エンジン・決定論・依存追加なし・golden 不変）：**
    - `src/event-check.ts`（1164 pass）：弧/断片網羅・**4b** keeper 到達性・**4c** 店主口調なのに `speaker` 未タグの検出（雑踏NPCが店主として喋る辻褄崩れ＝#230/#231 の逆方向）・**5d/5e** スロット throw 安全（`#origin_catchphrase#` は encounter+`hasCatchphrase` 必須／`#origin_epithet#` は生者 context 限定／名簿は全員 epithet 必須＝異名/口癖を持たぬアンカーで `fillSlots` が実行時 throw＝会話/遭遇クラッシュを静的に防ぐ）・**7** context×prereq の no-op/死蔵・**8** setpiece 死蔵。
    - `tools/audit-content.ts`：成立不能 prereq（minDepth>maxDepth・depthBand×深度矛盾・minBond上限）・context 必須フィールド欠落（encounter=investigate/search・chest=result・dungeon/town=choices）・**バランス外れ値 warn**（無ゲートで gold>40／exposure<-0.3＝序盤 farm 可能な大報酬＝終始シビアを崩す事故を検出。閾値は実測 gold p95=26/exposure p95=0.1 を大きく超える地点）。
    - `tools/scenario-clear.ts`：**勝利条件（5印→深淵帯解錠→奉献）の到達可能性を静的＋動的に保証**＝全 `SEAL_KEY` に `awardSeal` 付与経路がソースに在ることを突合（awarder が欠けると `seals` が5に届かず**永遠にクリア不能**という壊滅的回帰を防止）。requiem 印は grudge 化石の `intervene`（world.ts）経由。
    - `tools/stress-save.ts`（55,782 checks）：旧セーブ（新フィールド欠落）×多世代×全難易度を `migrateWorld`→直列化/復元で総当たり＝「非破壊バックフィル」の裏取り。**`migrateWorld` はコア（seed/generation/fossils/chronicle/town）も防御的にバックフィル＝parse できる任意オブジェクトを有効 World に（iOS PWA 部分破損の保険・通常 no-op）。**
    - `tools/determinism.ts`（1,550 checks）：同 seed の操作を同一プロセスで二度走らせ byte 一致＝隠れたグローバル状態/反復順の非決定論を検出。**Swift 移植メモ：オブジェクト id は `world.ts newId`＝プロセス・グローバル `idCounter` 由来でシード非依存（実プロセスは counter=0 起点で完全再現／本検査は id を正規化して seed 由来の差だけ見る）。Swift は counter=0 起点踏襲か id をシード派生にする。**
    - `tools/render-check.ts`（CI 外・手動）：全 storylet/fragment/setpiece を該当 context の fill 関数で実描画し、深度深度・重複助詞・未充填スロット等の描画破綻を静的検出。**content の本文を触ったら回すと良い**（`#origin_epithet#の…` の二重「の」型を実際に捕捉した）。
  - **ローカル専用（実ブラウザ playwright + PIL 依存＝CI には無いので手動。Chromium は `/opt/pw-browsers/chromium-1194/...`・`executablePath` 直指定で起動）：**
    - `tools/playtest2.mjs`：実ブラウザに本体を読み込み街/迷宮/シートをファジング（タイトル/難易度/タブ網羅）＝`main.ts` の DOM 結線ごと JS 例外を収集。**ただし霧ダンジョンの下り探索が黒箱では非効率で D1 止まり**＝深層は↓で。
    - `tools/playtest-deep.ts`：**セーブ注入で深層から再開**＝Node でエンジンから「深層 dive 中」の有効セーブ＋`DiveSnapshot` を生成し `localStorage` に注入→「続きから」で D16〜D50/深淵帯へ直接復帰→castSpell（ロードアウト巡回で全36術網羅）・ボス戦・宝箱・状態異常をファジング。**web 限定グルー（深層 main.ts）の唯一の自動カバー手段。**
    - `tools/visual-check.ts` ＋ `tools/visual-analyze.py`：縦持ち(480×900)で主要画面を撮影し PIL で空/破綻描画・主要帯の無内容・下部セーフエリア取りこぼしを定量検出。
  - **方針＝バグはほぼ content の辻褄に集中していた**（エンジン・main.ts は堅牢）。content/`Prereq`/`Effect`/`speaker` を触ったら `event-check` + `audit-content` + `render-check`、エンジンを触ったら `determinism` + `stress-save` + `golden`、深層挙動を触ったら `playtest-deep` を回す。
- `npm run build:web`（esbuild。依存は esbuild のみ）。CLI: `npm run cli`。
- エンジンはブラウザセーフ（fs依存は `*-node.ts` に隔離）。決定論：seed → mulberry32 一本。
- **実行時LLMは使わない**（snapshot 4-9）。LLMは制作時の素材生成（鋳造所）のみ。
- **アプリ版数（2026-06-20 導入）：** `src/web/main.ts` の `APP_VERSION` が単一の真実。画面右上 `#stVer` に常時表示＋設定シートに build 日。**デプロイのたびに `APP_VERSION` と `web/sw.js` の `CACHE`（`sekitsui-X.Y.Z`）を同値で必ず上げ、`APP_BUILD`（設定/タイトルに出す build 日）も実デプロイ日に更新する＝三者同時（2026-07-18 明文化・APP_BUILD が10日停止していた反省）。** 最新かの判定は右上の版数で行う。
- **★PWA 自動更新（v0.76.0・2026-06-29・テストプレイFB「更新のためアイコン削除→セーブ消失」対応）：** SW は元々ネット優先だが、**開いているページが新 SW を検知しても自動リロードしなかった**のが弱点（＝iOS standalone で更新が届かずアイコン入れ直し＝localStorage セーブ消失を誘発）。`web/index.html` の SW 登録を強化＝`controllerchange` で**一度だけ自動リロード**（`hadController` ガードで初回インストール時は除外＝ループ防止）＋`load`/`visibilitychange` で `reg.update()`＋新版インストール時 `postMessage("skipWaiting")`。`web/sw.js` に `message`→`skipWaiting` ハンドラ。**＝オンラインで開けば自動で最新版に差し替わり、アイコン削除（＝セーブ消失）が不要に。** セーブは localStorage（アンインストールで消えるのは OS 仕様＝避けられない）＝設定の「セーブ書き出し/読み込み」が手動バックアップ。検証＝headless でリロードループ無し（navigations:1）・例外0。
- **iOS standalone PWA の下部バー＝最下端問題の確定解（2026-06-20・PR #125-131・リサーチ済み）：** ①`meta viewport` に `viewport-fit=cover`（env(safe-area-*) と edge-to-edge に必須）。②**`body` 高さ＝既定 `100dvh`、`@media (display-mode: standalone)` のとき `100vh`**（standalone はツールバー伸縮なし＋`overflow:hidden` 非スクロールゆえ 100vh が物理全画面で正。`100dvh` だと端末によりホームインジケータ分だけ短く解決され下端に隙間）。③下部バーは `position: fixed; left/right/bottom:0; z-index:10`（overlay=20 の下）。④iOS standalone はホームインジケータ帯を **Web ビュー外**に確保し CSS で塗れない→`theme-color`／manifest／`body` 背景をバー色（現 `#0c0f14`）に統一して帯とバーを連続表示。⑤アイコン位置＝`#tabbar` の `padding-bottom: max(8px, calc(env(safe-area-inset-bottom) - 12px))` で安全域を残しつつ下端へ寄せる。⑥`body.has-tabbar` でバー高ぶんの下部余白を確保（fixed バー裏に D-pad 等が隠れない）。⚠**iOS PWA のキャッシュは粘着的**＝版数バンプだけでは更新されず、ホーム画面から入れ直しが要ることがある（だから版数表示が有効）。

## 現在地（★現ライブ＝**v0.167.0**／PR #1〜#396 マージ済み＝U1a 配信クローズ済み・見出し日付は初期のまま＝追記式ログ）

v0 公開済み＋以下まで実装・公開。**M0/M1 完了・M2 ほぼ完了**（進捗詳細は `ROADMAP.md`）。

> **★★次スレッド最優先（2026-07-27 引き継ぎ・最新）＝UI Swift-ready（Swift 移植前の整備）。RFC v3 確定＋Phase U0（正典整備）完了。**U1a まで完了・配信クローズ済み**。** 現ライブ＝**v0.167.0**（main HEAD `12d0d08`・PR #390〜#396 マージ/デプロイ確認済み）。**Claude 実装／Codex 実差分検収の二者運用**＝受け渡し台帳は **Obsidian `/Users/12yyy/Exobrain/Projects/roguelike-ai-design.md`**（★必ず読む＝設計合意・検収履歴の正典。**ただし全文でなく〈現在地＋受け渡しログ最新3〜5件〉だけ**＝下記の軽量運用）。
> - **★U1a は配信クローズ済み（PR #394・main merge `12d0d08`・Actions run `30272668602` success・ライブ `sekitsui-0.167.0`／表示 `v0.167.0`／設定17項目・console error 0）。Codex も同一事実を独立確認しクローズ。追加対応なし。** 受理ゲート `npm run check` に **`tools/settings-parity.ts`（15,552 状態／264,384 比較／17 ID spy／循環5種）** が同梱され、CI で緑を確認済み＝**設定の文言変更で分岐が壊れる負債は構造的に解消**。
> - **★コンテキスト軽量運用を導入（#391）＝毎セッション自動ロードされる `CLAUDE.md` を 274KB→40KB（85%削減）。** 完了済みスレッド総括は **`CLAUDE-history.md`** へ無損失退避（通常は読まない・経緯を追う時だけ `grep`）。**新しい総括を足したら古い総括は history へ移して本体を軽く保つ。** Obsidian 台帳も全文を読まない運用に（詳細は冒頭「★コンテキスト軽量運用」節）。
> - **★UI Swift-ready RFC（`rfc-ui-swift-ready.md`・#390 v1→#392 v3）＝Codex 監査5指摘への回答を確定。** ①UI 正典の一本化②意味論的画面モデル③Swift ネイティブ化④アクセシビリティ⑤画面状態 fixture。**v2/v3 で Codex の修正必須を反映**（`Row.input` の判別 union・`SemTone` を正典 token へ 1:1・fixture の循環解消〔(1a) validator＋(1b) 実装済み subset conformance／全画面は Swift U2〕・WCAG 対象別基準・U1 を U1a/U1b/U1c に分割）。**このRFCは実装しない＝以後の UI 更新は §10 側で行い、RFC は経緯・根拠の記録。**
> - **★Phase U0＝UI 正典の整備 完了（#393・doc のみ・版数据置）。`prototype-spec.md` §10 が唯一の UI 正典になった。** 追補＝**10.1b 意味論的スクリーンモデル**（`Screen`/`Row`・input 判別 union・現web→モデル対応表）／**10.2e 前景意味トークン ID の正典一覧**（`SemTone` 46 ID・`IconId` 10 ID の**生成元**＝色 hex は 10.2/10.3 参照で二重管理せず・表に無い ID は 10.12(1a) で拒否）／**10.2c タイポ基準**／**10.2d アニメ keyframes**（実 CSS 実値＝`tele-move` は静的と明記・`pulse` は t5 2s／elite 1.4s／エリアボス 1.05s／erratic 相棒 1.1s）／10.7 に踏み込み・受け流し表示／**10.10⑥** Swift 部品（SF Symbol＋可視タブラベル・Form 設定・D-pad 44pt・版数フッタ）／**10.11 アクセシビリティ**（WCAG 対象別基準・VoiceOver 4点・通知は `AttributedString.accessibilitySpeechAnnouncementPriority = .high`）／**10.12 画面状態 fixture**。**`design-spec.md` はアーカイブ**（旧仕様・歴史資料／**旧内容は非削除**／役割色は §10 と一致で今も有効）。
> - **★Phase U1a＝設定シートの安定IDディスパッチ（PR #394・v0.167.0・配信クローズ済み）＝実装・検証・配信確認まで完了。** `settingsSheet` の分岐を**表示ラベルの `includes` 一致から安定 `SettingId`** へ。**表示文言・並び順・見出し・保存キー11種・既定値・循環順序・reopen は1文字も変えない（挙動不変）。** 構成＝`src/web/settings-items.ts`（純データ層・DOM 非依存）＋`src/web/settings-handlers.ts`（**DI factory・`createSettingHandlers(deps): Record<SettingId, Handler>`**＝Record ゆえ実装漏れは**コンパイルエラー**）＋`main.ts` は index→def→handler の直接実行（**switch なし**・未知は no-op＋dev warn）。**検証＝`tools/settings-parity.ts`（`npm run check` 同梱）で状態 15,552／ラベル比較 264,384／spy 17 ID／循環5種が 0 fail＋変異試験（ラベル1文字・順序入替・handler 誤配線）で検知力を実証／`tools/e2e-settings.mjs` 30/30／golden 8/8 完全不変／実 Chromium E2E 全12本 100%。** 凍結 oracle は**基準 commit `6a537d1` の legacy から採取した独立期待値**（refactor 後のコードを入力にしない・転記は基準ソースと機械照合・`--force` なしで上書き拒否・CI で再生成しない）。
> - **★次スレッドの宿題＝(a) U1b（高コントラスト・Reduce Motion＝OS 設定時の表示変更）／(b) U1c（画面状態 fixture 基盤＝(1a) schema validator＋実装済み subset の (1b) conformance）／(c) U2（Swift 実装）＝**各々ユーザーの個別承認が必要・未着手**／(d) 実機テストプレイFB（最期の残響 P1〜P2-E の手触り）＝引き続き保留。** U1a は完了済みゆえ着手不要。
> - **★E2E の実走コマンド（macOS・日本語パス対応済み）**＝`cd proto && npm install --no-save playwright`（**package.json に入れない規約**）→ `PW_CHROMIUM="$HOME/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing" node tools/e2e-*.mjs`。**⚠ E2E をバッチ連続実行するとフレークが出る**（実例＝guard-stagger 18/20・push-intent 12/23）＝**単独実行で再確認**し、疑わしければ**基準 commit を別 worktree に展開して同条件で比較**する（本スレッドで実施した切り分け手順）。

> **★★次スレッド最優先（2026-07-25 引き継ぎ・旧）＝RFC「最期の残響」完結（P2-D 配信・Codex クローズ済み）＋検証基盤の是正。次は UI ブラッシュアップ（Swift 移植前）。** 現ライブ＝**v0.166.0**（main HEAD `450a635`・PR #386〜#388 マージ/デプロイ確認済み）。**Claude 実装／Codex 実差分検収の二者運用**で進行中＝受け渡し台帳は **Obsidian `/Users/12yyy/Exobrain/Projects/roguelike-ai-design.md`**（★次スレッドはこれを必ず読む＝設計合意・検収履歴の正典）。
> - **RFC「最期の残響」P2-D＝山場連結＋deathManner 副次修飾（#386・v0.166.0・web限定＝`main.ts` のみ・engine/content/types/event-check 非改変・golden 8/8 完全不変・SAVE_VERSION 据置）＝これで RFC の P1＋P2-A〜E 全実装完了。** ①**grudge_hunt×呪詛**を影の状態で3分岐（影生存＝詫びる→`echoPurifyCurse`〔echo霧のみ消え自然霧残す・影還る・**gold 放棄**〕＋setpiece 印／撥ねつける→生存 shade のみ ×`ECHO_ENRAGE_MUL(1.15)`〔対象1体・1回・`fe.resolved` で盤面単位に冪等〕／**影先討ち・浄化済＝認識のみ**〔専用選択肢「その者の末路を胸に刻む」＝放置分岐へ流さず intervene/requiem/浄化/exposure/印/feat/bond/chronicle/RNG を一切動かさない〕）。不可逆な交換は**選択前に本文＋ラベルで明示**。②**inheritance の既継承**＝固有2択を認識決着1本へ置換（二度目の intervene/装備/形質/深蝕−0.4/相棒feat を全停止・冪等な setpiece 印のみ）。③**deathManner 5値**の第二形容を決定論表引き・**RNG非消費**で peek/対面文に（flavor のみ）。④**legend_return×静穏**の認識一行。検証＝`npm run check` 全緑（golden 8/8 完全不変）＋`e2e-echo.mjs` **49/49**（実 Chromium）。`design-snapshot.md` 4-11(A)／`rfc-last-echo.md` 反映済み。**Codex が main `09e0937`・Actions run・ライブ `sekitsui-0.166.0` を独立照合して配信クローズ済み。**
> - **★検証基盤の是正（#387/#388・ツール専用＝本体挙動と `APP_VERSION` は非改変・版数据置・golden 8/8 完全不変）＝日本語パス環境の地雷を根治し、E2E 全11本を初めて 100% 通した。**
>   - **#387＝非ASCIIパスのパス解決を根治。** `new URL(...).pathname` は **percent-encode されたまま**返るため、日本語ディレクトリ配下（`.../アプリ制作/...`）で ①`cli.ts`/`demo.ts` のセーブが `Cowork-Code/%E3%82%A2...` という**別ディレクトリを実際に作って**書かれ（`demo` は `npm run check` 同梱ゆえ**check のたび再生成**）、②`e2e-*.mjs` の `WEB_DIR` が解決できず**ローカルで起動不能**だった。修正＝`src/` は `fileURLToPath()`（`content-node.ts` と同じ既定パターン）、E2E 10本は `decodeURIComponent`＋`EXEC` を `process.env.PW_CHROMIUM || <既定の Linux パス>` に（**CI/cloud は既定値へフォールバック＝挙動不変**）。stray ディレクトリは削除済み。**★ローカル実走コマンド＝`PW_CHROMIUM="$HOME/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing" node tools/e2e-*.mjs`（`npm install --no-save playwright`＝package.json に入れない規約は維持）。**
>   - **#388＝腐敗した E2E アサーション4件を現仕様へ是正（本体は全て正しく、テスト側の誤り）。** ①**薙刀の間合い3件**（shaped S7／burst S6／pr4 R1＝同一コピペ）＝敵を隣接にスポーンして有効打を期待していたが、**v0.150.0(#364) で「距離1＝隣接8マスは死角／距離2の横3マスバーを薙ぐ」が仕様**＝現挙動が正しくテストが旧仕様のまま腐っていた。武器クラス別の正しい距離（剣/槍=1・薙刀=2）に直し、判定を `state()` の**絶対座標**へ（前進時の誤検知も封鎖）。②**burst S5①**（mending）＝本体の判定〔FOV内に起きた敵が居れば回復停止〕は仕様どおり正しく、真因は **`mendTick` がフロア単位でしか初期化されず前段テストの手番を持ち越す**こと＝dbg フック `resetMendTick()` で起点を揃えた。
>   - **★E2E 全11本の現状＝100% pass**（echo 49/49・onboarding 40/40・push-intent 23/23・trial-harness 22/22・guard-stagger 20/20・burst 20/20・weapon-parity 19/19・pr4 18/18・shaped 15/15・charge-spear 9/9・push-reach 9/9）。**教訓＝`e2e-*` は `npm run check` 非同梱ゆえ黙って腐る。仕様を変えたら該当 E2E を必ず実走すること。**
> - **★次スレッドの宿題＝(a) UI ブラッシュアップ（Swift 移植前・Codex 提案の「UI Swift-ready RFC」＝正典の一本化／`chooseGrid` の意味論モデル化／アクセシビリティ仕様化。まず実装せず RFC・差分表・画面状態 fixture 案・段階導入案を作り検収→承認）／(b) 実機テストプレイFB（最期の残響 P1〜P2-E の手触り・数値は全てテスト調整候補）。**

> **★恒久メモ（不変・下記は常に有効）：** 直近の引き継ぎは冒頭「2026-07-25」ブロックが正（現ライブ v0.166.0）。**過去スレッドの総括ログは `CLAUDE-history.md` へ退避済み＝毎セッションの自動ロードを軽くするため。通常は読まなくてよい**（特定の過去実装の経緯を追う時だけ `grep`）。
> - **★次スレッドの宿題＝(a) UI ブラッシュアップ（Swift 移植前・`rfc-ui-swift-ready.md`＝作成済み・Codex 検収待ち）／(b) 実機テストプレイFB待ち＝最期の残響 P1〜P2-E の手触り（静穏/遺言/守り手/呪詛・P2-A 相棒縁者・P2-B 極差・P2-C 放置歪み・P2-D 山場連結・P2-E 霧拡大）＋各PR群の調整候補定数。FBが来たら1レバーずつ調整。**
> - **残（承認済み・要着手判断）＝** 人力試技（武器数値の変更候補が出た時のみ・**bot結果だけで変えない**）。RFC「最期の残響」は **P1＋P2-A〜E 全実装完了**（残る任意拡張＝シード化石の残響化 micro）。
> - **保留（動かさない）＝** 武器数値全般・薙刀主対象制/肩0.6/肩stagger削除・武器問い配置・階層スキップ・深蝕定性表示・storylet導線／緊張感フェーズ2＝巣部屋（モンスターハウス・v0.151.0 の実機FB後）／PR6（main.ts分割・ゲーム完成後）。
> - **★守るゲーム原則（ユーザー明示・不変）＝** ピリピリ難易度を下げない・敵HP/攻撃/数を安易に下げない・プレイヤー一律強化のパワークリープ回避・じっくり攻略は罰しない・予告/地形/位置取りで戦う・武器交換の一手コストはパズルとして維持（無料持ち替え/二本差し/控え枠/交換時攻撃は不採用）・弱点倍率の正解選択ゲームにしない・全武器の平均勝率を機械的に揃えない・実行時LLMなし/決定論/セーブ互換/Swift移植性・静かな雰囲気（過剰UI/マーカー回避）・easy/golden 契約変更は事前承認。**★Stop-hook の「Unverified」警告（GitHub 生成マージコミット committer=web-flow）は技術メモ(A)のとおり無視＝amend/rebase 厳禁。**

- セーブ **version=9**（gold/quests/town-scene/`World.companion` 含む＋任意追加 `Character.loadout`・`World.diveCount`・`Fossil.spells`・`World.homeUnlocked`・**`World.difficulty`（旧→easy grandfather）・`World.keepsakes`（旧形式→id 移行）・`Fossil.level`/`Fossil.stats`（系譜配分用・旧化石は laidDepth 代用）** は `world.ts migrateWorld` 非破壊バックフィル）。歩ける街・同行・魔法ロードアウト・難易度・拾得品は **web 限定**、CLI/デモはテキストメニュー据え置き。

## 次のタスク（★2026-06-23 ロードマップ見直し済み。詳細は `ROADMAP.md`）

> **★配布方針＝Swift 完全再実装（案B）・PWA は配布しない（2026-06-23・ユーザー確定）：** 機能開発＋監査是正は一段落（M0〜M3 完了／M4 量産 storylet 716・敵能力フレーム＋敵種31／横断E 監査是正 #169-171＋実機テストプレイ中）。**現行 TS/web は配布物でなく「設計・検証のリファレンス実装（仕様書）＋テスト用ビルド」。製品＝Swift ネイティブ版を App Store で買い切り（4-9E）。** 移植方式は **B＝Swift で完全再実装**（JS埋め込みでない純ネイティブ）。
> - **二フェーズ構成。【フェーズ1：TS を仕様完成へ固める】**（＝当面の作業）
>   - **(1) 横断E バランス確定（最重要・割り込み最優先）：** テストプレイFB→数値・敵能力を即調整（定数1〜2行）＋ヘッドレス併走。**移植前に数値・挙動を確定。** 残＝4-1 warp 体感・魔法/迷宮スケール・終始シビア・`XP_KILL_MUL`/`LEARN_EVERY`・ペーシング逓増・討伐ドロップ率・**敵能力の体感**（遠隔の読みやすさ/毒/増殖/深部能力比率38%）。
>   - **(2) `main.ts`→純粋エンジン抽出（B柱1）：★着手時期＝ゲーム完成後・Swift 移植の段階で実行（2026-06-25 ユーザー確定）。** フェーズ1 では着手しない＝ゲーム設計/バランスがまだ動く間に抽出すると分割状態の維持コストがかかるため、コードが安定（ゲーム完成）してから移植作業の一部として実施する。内容＝web限定ゲームロジック（castSpell/相棒手番/毒・状態異常/敵能力のプレイヤー側/街シーン）を `src/*.ts` の純粋・DOMフリー層へ移し、main.ts を薄い描画/入力シェルへ。**CLI 全機能パリティ**＝抽出完了の指標＝Swift へ移す面積を最小化。着手時は安全な第一スライス（castSpell 効果 or 相棒手番のどちらか1領域を純関数化＋golden ベクタ＋CLI 裏取り）から段階的に。
>   - **(3) ゴールデンテストベクタ（B柱2）：** `demo.ts` 拡張で代表 seed の状態遷移を決定論記録・`npm run check` 同梱＝Swift が「同 seed 同結果」を満たす照合の正解データ。
>   - **(4) タイトル/設定の UX 確定・(5) M4 量産（充足バーまで）・(6) UI美術** … Swift がミラーする基準を TS で定義。
> - **【フェーズ2：M6 Swift 移植＆配布】** 純粋エンジン Swift 再実装（`types.ts`→struct／`content/*.json`→Codable／ゴールデン照合）→ SwiftUI 描画／AVFoundation 音／永続 → 製品仕上げ（チュートリアル・複数スロット・i18n/a11y・App Store 買い切り）。
> - **残るユーザー判断＝最終バランス数値・UI美術（横断F）・Swift 着手タイミング（TS 仕様完成時）。** 設計判断を含む箇所は着手前に必ず方針確認すること（上記ルール）。

**実装の要所（街/経済/依頼/山場）：**
- 街シーン：`src/townscene.ts`（純粋・ブラウザセーフ）＋`src/web/main.ts` の `drawTown/drawInterior/townAct/interiorAct/questBoard/talkKeeper/smithBuy/healerTreat/legendApprove/heroRoll/lineageScene/lineageBoon`。データ＝`content/town.json`（建物追加=配列1行／区画解禁=`guards[].locked` を外す）。伝説化＝`legendApprove`（神話極の旧キャラ→`world.tracked` player_legend）。
- 持ち物：`items.ts CONSUMABLES`／`progression.ts carryCapacity・STASH_CAP(60)/STASH_INHERIT(4)`／`types.ts InventorySlot・World.stash(消耗品)・World.stashGear(装備)`＋`web/main.ts` の `storeBuy/storeSell/storeManage`（道具屋）・`homeDeposit/homeWithdraw/homeView`（自宅＝武具庫＝`kind:"home"`／消耗品＋装備を世代越え保管・`fossilizeCurrent` で各4枠に切詰め・装備は引き出して即装備スワップ）・`bagBtn`（潜行中に使用＝一手消費）・`addConsumable/applyConsumable/consumeOne`。消耗品追加＝`CONSUMABLES` に1行。
- 依頼：`src/quests.ts`（純粋：`generateOffers/onReachDepth/onRediscoverFossil/claimQuest`）＋`World.quests`。達成フックは `enterFloor`(到達)・`fossilScene`(回収)。
- 山場：`src/render.ts matchSetPiece`（有効候補から `rng.pick`・frame文と型を同じ1件から導く＝二重抽選回避／PR #205）＋`fossilScene` の山場動詞。**3極すべてにクライマックス**＝legend_return(myth)／grudge_hunt(grudge)／**inheritance(loss・PR後続・v0.42.0)＝「遺志を継ぐ（継承機構を昇華）／安らかに送る（鎮魂）」二択・両決着とも印③付与**。content＝`content/setpieces.json`（inheritance frame 4件・name/gear/depth のみ）。死蔵防止＝`event-check.ts` の `SETPIECE_HANDLED`（型を追加したら同期必須）。
- ボス物語化（4-6・v0.43.0・web限定）：エリアボス「○○の成れの果て」を**縁ある化石へ加重抽選**（`dungeon.ts pickBossSource`＝絆+5/未完因縁+5/doom・fall終端+6/元相棒+4・基礎1で他人も残す＝golden 不変＝ボス階は8の倍数で golden 対象外）＋決着シーン（`web/main.ts handleBossResolve`）を出自/口癖/縁/弧入りの setpiece 級に＋**縁ある相手だけ第三選択「名を呼ぶ」**（`intervene memorial`・深蝕−0.5・doom/fall 終端なら弧が閉じる）。討つ＝印①`abyss_boss`（討伐限定維持）／鎮める＝requiem／名を呼ぶ＝memorial（半XP）。
- ボス戦術化（4-11G Phase 3・B・v0.44.0・web限定）：エリアボス（`m.boss==="area"`）に①**溜め大技**（隣接で「渾身の一撃」をテレグラフ＝`MonsterIntent.heavy`→退けば空振り/受ければ`BOSS_HEAVY_MULT(2)`倍・`bigCd`間隔4/覚醒2・猶予`BOSS_HEAVY_GRACE(2)`・橙白 `g-boss-heavy`/`g-player-heavy`）②**怒りフェーズ**（HP`BOSS_ENRAGE_AT(0.5)`で覚醒＝攻撃+25%・大技CD短縮）③**眷属召喚**（覚醒後`BOSS_SUMMON_CHANCE(0.2)`/`CD(4)`・`breederMinion`流用・`MONSTER_HARDCAP`管理）。実装＝`dungeon.ts`（`planMonsters` ボス維持ブロック＋隣接大技分岐／`resolveMonsters` 倍率／`monsterDmg` 覚醒加算）＋`web/main.ts`（`announceBossCues`＝engine純粋ゆえ覚醒/溜めを web 告知・heavy被弾演出）。**全て area 限定＝通常敵/golden 不変**（monsterAI 指紋 51d3744d 同一）。中ボス/エリートは対象外（後続）。数値はテストプレイ調整候補。
- 同行（4-14C）：`types.ts Companion`(＋`traits?`奇癖)＋`World.companion`／`Fossil.wasCompanion`(再会の固有性)／`dungeon.ts CompanionEntity・DownedActor・Floor.downed・planCompanion/resolveCompanion・COMPANION_DMG`（純粋・決定論）／`world.ts fossilizeCompanion`(戦死/慈悲＝化石化)・`fossilizeAbandoned`(見捨て＝grudge)＋`web/main.ts` の `companion`(盤上 ephemeral)・`spawnCompanionNear/companionDies(combat|mercy)/companionCrisis/applyCompanionQuirks/tryPromoteCompanion/rescueScene/recruitCompanion/offerCompanion/townContextsHere`・`enterFloor`(展開＋手負い配置)・`endTurn`(相棒手番＝攻撃/被弾/連帯深蝕/奇癖)・`moveOrInteract`(救助bump/相棒と位置入替)・`draw`(青@/erratic菫@/手負い`&`/テレグラフ)・`fossilScene`(wasCompanion 再会の一言)。相棒不在時はエンジン挙動が従来と完全一致＝既存セーブ無影響。

- 深蝕魔法ロードアウト（4-11F③・web限定）：定義の正＝`spells.ts`（`SPELLS`＝key/name/school/cost/desc/minLevel・`warpDamage`）。`types.ts Character.loadout`／`progression.ts LOADOUT_CAP(10)`。`web/main.ts` の `activeLoadout/learnSpell`（習得＝図鑑＋空き構えに自動装填）・`castSpell`（全術の盤面効果＝新術はここに1分岐）・`manageLoadout`（≡「術を構える」＝街のみ）・`handleLevelUps`（ステ+1常時＋`LEARN_EVERY`間隔の任意習得・`minLevel`ゲート）・`endTurn`（バフ計時減算/毒tick/召喚手番）。状態異常＝`dungeon.ts` Monster の `slowed/fear/confused/rooted/weak/poison`＋`planMonsters` で消費。**新術追加＝`SPELLS`に1行＋`castSpell`に1分岐**で「術」ボタン・習得・構えに自動で乗る。
- 迷宮スケール（4-11F②）：`dungeon.ts genFloor`（W/H=36/42+min(depth,50)・`scaleKind`深度係数・`regularHpAt`・敵count上限42・宝箱/部屋は面積比）。seed に `world.diveCount` を混入＝潜行ごと別ダンジョン。`web/main.ts floorCache`＝潜行内の階保持（再訪で宝箱復活しない）。`progression.ts xpForKill`（`XP_KILL_MUL=0.55`）。
- **難易度モード（4-11H・v0.56.0・ユーザー承認・テストプレイFB「序盤で死なない」是正）：** 終始シビアの単一曲線に**薄い乗数レイヤー**を被せる（別コード経路なし＝決定論・golden・Swift 移植に優しい）。定義の正＝`src/difficulty.ts`（`DifficultyMods`＝enemyHp/enemyDmg/**dmgFloor（序盤の火力床）**/exposure/xp/townHeal/lineage の係数表）。**新規ワールド開始時に一度だけ選び固定**（`web/main.ts chooseDifficultyIfNew`＝`world.difficulty` 未設定時のみ・設定での変更なし）。**easy=×1.0＝現行数値＝golden 不変**（旧セーブは `migrateWorld` で easy に grandfather）。選択肢＝easy/normal/hard（`SELECTABLE_DIFFICULTIES`）／**death は枠のみ（後日調整・UI非表示）**。波及＝`dungeon.ts scaleKind(k,depth,mods)`＋`genFloor` が `floor.diff` に焼き込み→雑魚/エリート/ボス(`makeAreaBoss`)/追手(`spawnPursuer`)/眷属(`breederMinion`) 全スポーンに係数（`world` 経由＝引数追加なし）／`web/main.ts`＝XP（`*diffMods(world.difficulty).xp`）・街帰還の回復（`townHealHp`＝潜行入口の HP・hard で<満タン＝消耗）・深蝕加点（`addExp` ヘルパに全11箇所を集約）・系譜ボーナス（`createCharacter` が `mods.lineage` で無効化＝death 用）。検証＝ヘッドレスで深度1の被ダメ easy1→hard3（序盤から噛む）・深度6雑魚 HP14.5→22.1。**⚠ 既存セーブ世界は easy 固定＝normal/hard 体感は「新しい物語」で新規ワールドを作る必要。** 数値はテストプレイ調整候補。
- **系譜の二択リワーク（4-10D・v0.55.0・ユーザー承認・テストプレイFB「弟子が一方的に有利」是正）：** 旧＝血縁が術2/弟子が術3で**弟子が純上位互換**（形質ラベル差は無効果）だった。新＝**血縁＝大器晩成／弟子＝先行逃げ切り**の二択。**血縁**＝継ぐ術 `BLOOD_SPELLS=2` を**自分で選ぶ**（回復/帰還を最初から持てる質）＋**恒久ベース加算** `+clamp(M/10,1,BLOOD_STAT_CAP=5)`（M=先代Lv）＝Lv上限で無系譜を確実に上回る。**弟子**＝継ぐ術 `PUPIL_SPELLS=4` 自動（多芸・選べない）＋**開始Lv=1+clamp(M/6,0,PUPIL_LEVEL_CAP=8)** ＋そのぶんステ即付与（前借り型＝Lv上限では無系譜と同地力）。配分は `addLineageStats`＝**先代の得意ステ寄せ**（化石に `level`/`stats` を記録＝旧化石は `laidDepth` 代用）。`Lineage.chosenSpells`（血縁の選択）＝`web/main.ts pickBloodSpells` UI→`world.ts createCharacter`。CLI は chosenSpells 無し＝先頭2つ自動。golden 不変（worldLifecycle は当経路を踏まない）。**数値（/10・/6・上限5/8）はテストプレイ調整候補。**

**設計的負債：** 職業選択(流儀)は撤去済み(archetype="wanderer"固定)。**店スタブは全結線済み（武具屋/奇物堂は2026-06-19・酒場/薬師は2026-06-26＝v0.48.0）**：武具屋 act2「先代の刻印武器について訊く」＝`smithLore`／奇物堂 act1「奇妙な異物を買う」＝`oddmentsBuy`・act2「品の曰くを聞く」＝`oddmentsLore`。**2026-06-26 棚卸しで酒場/薬師の4件が未結線（フォールバック「まだ整っていない」に落ちていた）と判明→結線**：酒場 act1「旧き名・化石を尋ねる」＝`tavernLore`（縁ある名＝tracked/絆化石を選んで弧/噂を聞く）・act2「休む（一杯やる）」＝`tavernRest`（1世代1回・深蝕−0.05・gated）／薬師 act0「傷を癒す」＝`healerHeal`（街は基本全快ゆえ flavor＋HP満たし）・act2「薬を買う」＝`healerBuy`（医薬＝CONSUMABLES を薬師の本領として販売）。`talkKeeper` の全 actIdx が結線＝フォールバックは到達せず（保険として節は残す）。`renderRumor` の「深度深度」重複は**実バグなし**（`#depth#` を originSlotValues から1回だけ充填・調査確認済み＝対象外）。`web/main.ts` に既知の型エラー2件（`Floor|null` を `Floor` 引数に渡す箇所・実害なし）。`tsc` スモークは `content-node`/`persist-node` を除外して確認している。**魔法・迷宮の数値はテストプレイ前＝要バランス検証**（`XP_KILL_MUL`/`LEARN_EVERY`/敵上限/`minLevel`）。

