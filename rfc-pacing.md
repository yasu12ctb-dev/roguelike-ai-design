# RFC：先への期待と単調さ — ボスの山場と深層の起伏

**状態＝提案（未実装）。** 2026-09-11 作成。ユーザーの実機FB「気に入っているが、深く進んでも先が気にならない・単調」と、Codex の申し送り2件（2026-09-10「ボスの山場強調」「探索の期待と単調さ」）を受けて、実測とリサーチのうえで**段階化した計画**にまとめた。コードは一切変更していない。**各段階の着手には個別にユーザーの明示承認が要る。**

守る条件（ユーザー明示・不変）＝硬派なローグライク・ハックアンドスラッシュの形を崩さない／難易度を下げない／敵HP・攻撃・数を安易に上げ下げしない／プレイヤー一律強化のパワークリープ回避／じっくり攻略は罰しない／静かな雰囲気（過剰UI・強制ムービー・頻繁な操作停止なし）／予告マスを覆わない／無音・Reduce Motion でも識別できる／決定論・セーブ互換・Swift 移植性。

---

## 1. 実測＝何が増えて、何が増えないか

`genFloor` を同一シード8本の平均で深度別に測った（2026-09-11・main `43787ca`）。

| 要素 | D1 | D8 | D16 | D24 | D37 | D50 | 頭打ち |
|---|---|---|---|---|---|---|---|
| 床（歩けるマス） | 427 | 588 | 847 | 1,125 | 1,680 | 2,218 | なし |
| 階段→階段の最短歩数 | 42 | 64 | 87 | 83 | 125 | 172 | なし（4.1倍） |
| 敵の数 | 13 | 25 | 37 | 50 | 50 | 50 | **D24**（hardcap） |
| 床 ÷ 敵（薄いほど空） | 32.8 | 23.5 | 22.9 | 22.5 | 33.4 | **44.2** | U字＝深層は D1 より薄い |
| 敵の能力の種類 | 1 | 4 | 7 | 10 | 10 | 10 | **D24** |
| 地形ハザードの種類 | 0 | 3 | 3 | 3 | 4 | 4 | **D25** |
| storylet の発火率 | 0 | 0.40 | 0.55 | 0.55 | 0.55 | 0.55 | **D12** |

**結論＝深度が増やすのは歩く距離だけで、判断の種類は D24 で止まる。** D24〜D50 の26階、プレイヤーは新しい問いを一つも与えられないまま歩く量だけが倍になる。これが「先が気にならない」の機械的な形。

生成器の形も深度で変わらない。矩形部屋＋幅1のL字通路、大部屋率 0.16、ループ率 0.2 は全深度で同じ定数（`dungeon.ts` `genFloor`）。深度が動かすのは寸法 `36+depth × 42+depth` だけ。**D1 の画面と D40 の画面は、松明の色調と帯名以外は同じ絵。**

### 1.1 ボスの現状（Codex の三段で棚卸し）

| 段 | 既にあるもの | 不足 |
|---|---|---|
| 接近 | 術「先見」だけがボス階を判定し「主の気配。節目の山場が待つ」を出す（`main.ts foresee`） | 術を修めていなければ**予告が一切届かない**。降り階段のプロンプトは固定文（`stairsPrompt`） |
| 対峙 | グリフ Ω・専用色・peek の「（主）」・下り階段の傍に配置（必ず通せんぼ） | **初視認の告知がゼロ**（ログも音もない）。進入バナーは共通文。**BGM は通常曲のまま**（`BgmScene` に boss なし） |
| 戦闘 | 溜め大技の形つき橙予告・覚醒・眷属召喚（`announceBossCues`） | 告知が全部ログ行。HP の段階表示なし |
| 決着 | 縁があれば3択シート・`boss_down`・年代記・印・帰還の扉・秘宝 | 縁がないボスはシートなし（★ログ・音・年代記・ドロップはある） |

**深淵帯だけが「その型」を持つ**（進入時 `sfx("boss")`＋専用ログ・`main.ts:4735`）。D8/D16/… にはそれが無い。「ボスとしてフォーカスされていない」という FB はこの非対称そのもの。

---

## 2. 他作品の解法（2026-09-11 リサーチ）

| 型 | 実例 | 要点 | 適性 |
|---|---|---|---|
| 生成器のつまみを area で振る | Hauberk（Nystrom） | 生成器を作り直さず、同じコードのパラメータで cramped〜spacious を出し分ける | 高 |
| 同じ地形に**可動イベント**を重ねる | Cogmind「Cargo Convoys」 | 主要マップが samey だった問題を、**移動する任意の高リスク高報酬イベント**で解いた。地形は変えていない | 非常に高 |
| 敵を**増やさず置換**する | Cogmind「Area Denial」 | 新種を**総数への追加でなく既存スポーンの置換**として、高価値の分岐点に置く | 非常に高（hardcap 不変） |
| 報酬を「状況」にする | Brogue「Treasure Machine」 | 鍵保管室＋報酬部屋の二部構成。報酬でなく**報酬を得る状況**を作る | 高 |
| 進入前に危険を告げる | Angband「level feeling」 | 危険と財宝を一言で告げる。弱点＝階段を上下して引き直す **scumming** | 高。**本作は `floorCache` が潜行内の再生成を止め、潜行をまたぐと `diveCount` がシードに混ざる＝引き直せない。弱点が構造的に無い** |
| 枝分かれで別ルール領域 | DCSS の支道 | 縦一本の単調さを構造で割る | 中。縦一本の形を崩す＝要判断 |
| プレイヤーが難度と報酬を選ぶ | Path of Exile の map mods | 修飾を自分で選ぶ所有感 | 中。難易度モードと二重になる |

**共通の教訓＝効いている手法の大半は「生成器を作り替える」ではなく「同じ地形の上に別の層を重ねる」。** 本作はこれに向いている。`Floor.echo`／`Floor.hazards` が「web が `enterFloor` 初訪で seeded 配置＝engine 非使用＝golden 安全」の型をすでに持ち、`routeGuards`（主経路ガード・v0.152.0）が「追加スポーンなし・RNG 非消費で既存の敵を再配置する」先例を持つ。

---

## 3. 方針（3原則）

1. **生成器は最後に触る。** まず同じ地形の上に層を重ね、それでも足りない時に Phase C で帯別パラメータへ進む（golden `genFloor` が動くので承認事項）。
2. **追加ではなく置換。** 敵の総数・hardcap は動かさない。既存スポーンの一部を「役のある個体」に変える（Cogmind の流儀・`routeGuards` の延長）。
3. **「表示だけ」と「配置差」を分けて出し、分けて測る。** Phase A は表示だけ、Phase B は配置差。効いたのがどちらかを実機FBで切り分けられるよう、**同じ PR に混ぜない**。

Codex 案との差＝Codex は「次の節目の危険と報酬を予告して回収する」を第一手に置いた。本 RFC は**回収先（Phase B）を予告（Phase A の A1）と同じ優先度に置く**。予告は「先が気になる」に効くが、回収先の中身が同じなら二度目から効かないため。ただし A1 は数行で済むので先に出す。

---

## 4. 計画

### Phase A｜ボスの山場（表示だけ・web 限定・engine 非改変・golden 不変・SAVE_VERSION 据置）

| # | 中身 | 発火条件 | 文言案 | 再遭遇・再開 |
|---|---|---|---|---|
| A1 | 降り階段の一行 | `stairsPrompt("down")` で次深度が `isBossDepth`（`>=8 && %8===0`）。判定式は `foresee` と共有する純関数へ | 「下り階段がある。深度8へ降りるか？」＋「──下から、重い気配が上がってくる。節目の層だ。」 | 帰還の扉は階段プロンプトを通らない＝再放送なし。断定はせず気配だけ。報酬は予告しない |
| A2 | 初視認の一度きり告知＋進入バナーの副題 | ボスが**視界に入った最初の1手**。`Floor.bossSeen?: true`（任意・`DiveSnapshot` は `floor` を直列化するので再開後も一度きり） | ログ「${名}が、降り階段の前に立っている──この層の主。」＋既存 `sfx("boss")`。バナー `深度 8 ─ 浅層 ・ 節目の層` | `bossEnragedSeen` は `enterFloor` でクリアされ往復で再放送される＝**同じ型にしない**。floor 側に持たせる。盤面には何も描かない（予告マスを覆わない・無音でもログとバナーで識別） |
| A3 | 決着後の「越えた」一行 | `handleBossResolve`／`rewardKill` の直後・area ボスのみ | 共通「主を越えた。」＋ D8 のみ「ここから下は、浅層ではない。」（`depthBand(8)=shallow → 9=mid` の境界と一致） | 縁なしボス（シートなし）にも同じ一行が出る＝縁の有無で焦点の差が縮む |
| A4 | ボス専用 BGM | **保留**（A1〜A3 の実機FB後） | `BgmScene` に `boss` を追加。既存の迷宮曲に深淵帯の最低域を薄く重ねる（音源ファイル不要） | `setBgm` の呼び出し点が増え、帰還の扉の往復・再開で状態が壊れないか検査が要る＝切り戻す単位が大きい |

**Codex の留保への対応**＝長い会話・強制ムービー・操作停止なし（すべてログ1行＋既存音）／予告マスを覆わない（盤面非描画）／無音・RM で識別可（ログ・バナーは静的）／強化・報酬増量・戦闘機構変更を含まない／**「既存ボスの戦術差が実プレイで伝わるか」は A の FB で同時に聞く**（質問票 §5.2）。

### Phase B｜単調さの層（配置差・web 側 overlay・`genFloor` 非改変・golden 不変を狙う）

**B1 関所の個体（Area Denial の翻案）。** `routeGuards` は最短路へ 2〜4 体を再配置している。これを延長し、**うち1体を「部屋と通路の接点」に立たせ、`Monster.warden?: fossilId` を付ける**。追加スポーンなし。オリジナル要素＝置換対象を**その階の化石と縁のある個体**にし、初視認で一行（縁があれば名を出す）。年代記に残った先代の因縁が「通せんぼ」として盤面に出る。実装は `enterFloor` の後加工（`setupEchoBoard` と同じ型）に置いて golden 不変を狙う。**適用帯＝D9〜D16 で試す**（Codex の比較単位）。

**B2 深蝕の帯（Cargo Convoy の翻案）。** 移動するのは敵ではなく**霧**。`Floor.drift?: { axis, pos, width, speed }` を置き、`endTurn` で数手ごとに一方向へ進める。帯内の @ は `addExp(小)`。避けて回り込むか、突っ切って先回りするか、**同じ階が二度と同じにならない**。表示は既存 `miasma` の hz クラス（新色不要）。発火＝D9 以上・階ごと 35%（調整候補）。進入時ログ「霧が、東から流れている。」 RM＝離散移動なのでアニメ不要。オリジナル要素＝原作は取りに行く任意イベントだが、本作では**逃げる対象**にした（パワークリープを起こさない・深蝕という本作固有の資源に接続）。

**B3 封じられた間（Treasure Machine の翻案）。** 既存 chest を1つ「封」にし、**鍵＝その階の化石への干渉**（鎮魂か継承のいずれかを選ぶ）。報酬＝装備でなく**その先代が修めていた術**（`Fossil.spells` から1つ・習得済みなら形質）。物語と報酬が同じ一つの操作に乗る。使う器＝`chests`／`interventions`／`learnSpell`。**適用＝D16 以降・化石が置かれた階のみ**。

B の順序＝**B1 → B2 → B3**（安い順・engine への近さ順）。各1PR。

### Phase C｜生成器の帯別パラメータ（Hauberk 型・golden `genFloor` 意図的再生成・承認事項）

大部屋率 0.16・ループ率 0.2・通路幅 1 を `depthBand` で分岐する。例＝mid はループを減らして袋小路を増やす（幅1通路の緊張）／deep は大部屋率を上げて通路を減らす（開けた空洞・薙刀と槍の三択が変わる）。**新しいアルゴリズムは書かない。** 縮小はしない（Codex と同意見）。Phase A/B の FB を見てから。

### Phase D｜深層の能力（D24 以降にしか出ない `ability` を2種・golden 動く・後回し）

数値を上げず「別のことをさせる」。charge／reach／形つき予告の追加型が確立済み。Phase C の後。

---

## 5. 評価（楽しさの判断と回帰検査を分ける）

### 5.1 機械で出す（決定論・CI 外・`tools/pacing-report.ts` を新設）

§1 の表を**before/after を同 seed で並べて出す**ツール。Codex の指標のうち機械で出せるもの＝〈移動のみの手数 ≈ 最短歩数〉〈初の判断場面までの手数 ≈ `stairsUp` から最寄りの敵・宝箱・化石までの BFS〉〈判断点密度 ＝ (敵＋宝箱＋化石＋ノード) ÷ 床〉を採る。Phase B は golden 不変を狙うので、**このツールが「配置差が実際に出たか」の唯一の機械検査**になる。

### 5.2 実機FB（質問票・新規ワールド normal・どの世界か明記）

1. D8 に降りる前、ボスがいると分かったか（A1）。
2. ボスを初めて見た時、通常敵と違うと感じたか。**何で**分かったか（グリフ／色／ログ／音）（A2）。
3. ボスの溜め大技・覚醒・眷属は「対処を変えた」か、それとも通常敵と同じ対処で終わったか（Codex の留保）。
4. 越えた後、何かが変わった実感はあったか（A3）。
5. D9〜D16 で「先が気になった瞬間」があれば、何が起きた時か（B の前後で比較）。
6. 寄り道した・撤退した・武器を持ち替えた・消耗品を使った場面があれば、どの階で何に対してか。

---

## 6. 採らないもの

- **storylet の量産**＝951本あり発火率が D12 で 0.55 に頭打ち。**本数を増やしても遭遇率は変わらない。**
- **フロアの一括縮小・敵数増加**＝Codex と同意見。歩数 172（D50）は多すぎるが、縮めるなら Phase C 以後に深層だけの逓減を別途判断。
- **DCSS 型の支道**＝縦一本の形を崩す。導入するなら「深度が進まない任意の横道」限定で別 RFC。
- **PoE 型の修飾選択**＝難易度モードと二重になる。
- **長編ストーリー・強制ムービー**＝ユーザーの守る条件と衝突。

---

## 7. 未決（ユーザー判断）

1. **A1 で「8の倍数」という規則を実質的に明かしてよいか**（気配の文言だけなら規則は明示しないが、繰り返せば気づく）。私見＝明かしてよい。節目の階を知って備えられることが緊張を生む。
2. **B の適用帯**＝D9〜D16 限定で試すか、最初から全深度か。私見＝限定で試し、FB で広げる。
3. **Phase C の順番**＝B の途中で前倒しするか、B 完了後か。
4. **A4（BGM）を A に含めるか**＝私見＝含めない（切り戻す単位が大きい）。
5. **B1 の「縁」の定義**＝`pickBossSource` と同じ重み（絆・未完因縁・doom/fall・元相棒）を流用するか、化石の有無だけで足りるか。

---

## 8. 触らないもの（本 RFC の全 Phase を通じて）

`FODDER_CLUSTER_SIZE`／武器数値／`SAVE_VERSION`／`monsterHardcap`／難易度係数／街の描画・迷宮のセルサイズ・視界（論点クローズ済み）／U2（Swift）。
