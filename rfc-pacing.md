# RFC：先への期待と単調さ — ボスの山場と深層の起伏

**状態＝提案（未実装）。** 2026-09-11 作成。ユーザーの実機FB「気に入っているが、深く進んでも先が気にならない・単調」と、Codex の申し送り2件（2026-09-10「ボスの山場強調」「探索の期待と単調さ」）を受けて、実測とリサーチのうえで**段階化した計画**にまとめた。コードは一切変更していない。**各段階の着手には個別にユーザーの明示承認が要る。**

守る条件（ユーザー明示・不変）＝硬派なローグライク・ハックアンドスラッシュの形を崩さない／難易度を下げない／敵HP・攻撃・数を安易に上げ下げしない／プレイヤー一律強化のパワークリープ回避／じっくり攻略は罰しない／静かな雰囲気（過剰UI・強制ムービー・頻繁な操作停止なし）／予告マスを覆わない／無音・Reduce Motion でも識別できる／決定論・セーブ互換・Swift 移植性。

---

## 1. 実測＝何が増えて、何が増えないか

`genFloor` を同一シード8本（seed 1000〜1007）の平均で深度別に測った（2026-09-11・main `43787ca`）。

**★難易度で数が変わるので両方を載せる。初版は easy だけで測っており、ユーザーの実機FB は normal の世界から来ているため、normal を主の列にする**（`newWorld` は `difficulty` 未設定＝`migrateWorld` が easy へ grandfather するので、素朴に測ると easy になる）。

**normal（ユーザーの遊んでいる難易度＝主）**

| 要素 | D1 | D8 | D16 | D24 | D37 | D50 | 頭打ち |
|---|---|---|---|---|---|---|---|
| 床（歩けるマス） | 427 | 588 | 847 | 1,125 | 1,680 | 2,218 | なし |
| 階段→階段の最短歩数 | 42 | 64 | 87 | 83 | 125 | 172 | なし（4.1倍） |
| 敵の数 | 13 | 30 | 45 | 62 | 63 | 63 | **D24 前後**（下記※） |
| 床 ÷ 敵（薄いほど空） | 32.8 | 19.6 | 18.8 | **18.1** | 26.6 | 35.1 | U字＝D50 は D1 より薄い |
| 敵の**種**の数 | 2 | 9 | 19 | 26 | 34 | 33 | なし（D44 で 37 が最大） |
| 敵の**能力**の種類（実能力数） | 1(0) | 4(3) | 7(6) | 10(9) | 10(9) | 10(9) | **D24** |
| 地形ハザードの種類 | 0 | 3 | 3 | 3 | 4 | 4 | **D25** |
| storylet の発火率 | 0 | 0.40 | 0.55 | 0.55 | 0.55 | 0.55 | **D12** |

**easy（参考）**＝敵の数 13→50（D24 で頭打ち）／床÷敵 32.8→22.5（D24）→44.2（D50）／能力 1(0)→9(8)（`burst` が easy 除外のため normal より1少ない）。**U字と頭打ちの形は同じで、normal のほうが密度が高い。**

※**敵数の頭打ちは `MONSTER_HARDCAP` ではない**（初版の誤り・Codex 指摘）。初期配置は `count = min(面積式, countCap, monsterHardcap − bossSlots)` で、**深度50以下では `countCap = 42` が拘束する**（`dungeon.ts:446`）。表の値はこれに fodder を足した総数。`MONSTER_HARDCAP` は easy 60／normal・hard は深度20超で最大80（`dungeon.ts:66`）で、主に breeder の暴走を抑える上限＝初期配置には効いていない。

**能力の種類は「能力なし」を1つとして数えている。** 実能力の初出深度は `venom` D6／`ranged` D7／`charge` D7／`leech` D11／`arc` D12／`breeder` D14／`slam` D18／`burst` D20／`beam` D24。**D24 の `beam` が最後**で、次は `reflect` D54・`curse` D56＝深淵（D38 以降）より深く、通常プレイでは届かない。

### 1.1 結論（Codex 指摘を受けて限定した言い方）

**支持できるのは「敵の戦術カテゴリと生成地形のトポロジーが頭打ちになる一方、移動距離が伸びて遭遇密度が下がる」まで。**

- 戦術カテゴリ（＝対処法を変えさせる `ability`）は **D24 で止まる**。D24〜D50 の26階で新しい対処法は増えない。
- ただし**敵の「種」は増え続ける**（D24 で26種 → D44 で37種）。`gloom`(D27,t5)・`troll`(D28,t5)・`drake`(D35,t5)・`colossus`(D40,t5)・`horror`(D42,t5) など **tier 5 の新顔**が入る。装備も `大身槍` D25・`夜叉薙` D26・`淵穿ち` D30 が解禁される。**初版の「新しい問いを一つも与えられない」は言い過ぎだったので取り下げる。** 新しいのは「強さと数」であって「対処法の種類」ではない、が正確。
- 幾何は同型＝**矩形部屋＋幅1のL字通路というトポロジー**が全深度で同じ（大部屋率 0.16・ループ率 0.2・通路幅 1 が定数。深度が動かすのは寸法 `36+depth × 42+depth` だけ）。**初版の「D1 と D40 は色調と帯名以外は同じ絵」も言い過ぎなので、「地形の幾何が同型」に限定する**（敵の顔ぶれ・装備・ハザード・残響は変わる）。
- 密度は **U字**。normal で D24 の 18.1 が最も濃く、D50 は 35.1 で **D1（32.8）より薄い**。歩数は 4.1 倍。**これが「単調」の機械的な形＝判断と判断の間隔が深層ほど開く。**

### 1.2 ボスの現状（Codex の三段で棚卸し）

| 段 | 既にあるもの | 不足 |
|---|---|---|
| 接近 | 術「先見」だけがボス階を判定し「主の気配。節目の山場が待つ」を出す（`main.ts foresee`） | 術を修めていなければ**予告が一切届かない**。降り階段のプロンプトは固定文（`stairsPrompt`） |
| 対峙 | グリフ Ω・専用色・peek の「（主）」・下り階段の傍に配置（必ず通せんぼ） | **初視認の告知がゼロ**（ログも音もない）。進入バナーは共通文。**BGM は通常曲のまま**（`BgmScene` に boss なし） |
| 戦闘 | 溜め大技の形つき橙予告・覚醒・眷属召喚（`announceBossCues`） | 告知が全部ログ行。HP の段階表示なし |
| 決着 | 縁があれば3択シート・`boss_down`・年代記・印・帰還の扉・秘宝 | 縁がないボスはシートなし（★ログ・音・年代記・ドロップはある） |

**深淵帯だけが「その型」を持つ**（進入時 `sfx("boss")`＋専用ログ・`main.ts:4735`）。D8/D16/… にはそれが無い。「ボスとしてフォーカスされていない」という FB はこの非対称そのもの。

---

## 2. 他作品の解法（2026-09-11 リサーチ）

| 型 | 実例 | 要点 | 出典 | 適性 |
|---|---|---|---|---|
| 生成器のつまみを area で振る | Hauberk（Bob Nystrom） | 「**All of this needs to be tunable.** Many roguelikes have one huge multi-floor dungeon where depths vary in difficulty but not much else」「coding a new dungeon generator from scratch for *every* area is a huge time sink. Instead, I want the generator to have a bunch of knobs and levers I can tweak」＝**生成器を作り直さず、同じコードのつまみを area ごとに変える**。記事はつまみの実例として部屋数・最大部屋サイズ・wiggle 係数を挙げる（cramped〜spacious という具体的な出し分け表現は一次資料で確認できなかったので取り下げた） | [journal.stuffwithstuff.com](https://journal.stuffwithstuff.com/2014/12/21/rooms-and-mazes/) | 高 |
| 同じ地形に**可動イベント**を重ねる | Cogmind「Cargo Convoys」 | 主要マップが samey だった問題を、「**optional high risk-reward content moving through the main complex**」で解いた。**地形の生成は変えていない** | [gridsagegames.com](https://www.gridsagegames.com/blog/2021/09/spicing-up-primary-maps-1-cargo-convoys/) | 非常に高 |
| 敵を**増やさず置換**する | Cogmind「Area Denial」 | 「**In terms of bot quotas, Heavies were _not_ included as raw additions to the original inhabitants of each map**」「Heavies are usually placed in high-value junctions」＝**総数への追加でなく既存スポーンの置換**を高価値の分岐点へ | [gridsagegames.com](https://www.gridsagegames.com/blog/2021/09/spicing-up-primary-maps-2-area-denial/) | 非常に高 |
| 報酬を「状況」にする | Brogue「Treasure Machine」 | 鍵保管室＋報酬部屋の二部構成。報酬そのものでなく**報酬を得る状況**を作る | [brogue.fandom.com](https://brogue.fandom.com/wiki/Treasure_Machine) | 高 |
| 進入前に危険を告げる | Angband「level feeling」 | 進入時に危険と財宝の総量を一言で告げる。弱点＝階段を上下して引き直す **level scumming** | [roguebasin.com](https://www.roguebasin.com/index.php/Level_feelings) ／ [angband.readthedocs.io](https://angband.readthedocs.io/en/latest/dungeon.html) | 高。**本作は `floorCache` が潜行内の再生成を止め、潜行をまたぐと `diveCount` がシードに混ざる＝引き直せない。弱点が構造的に無い** |
| 枝分かれで別ルール領域 | DCSS の支道 | 支道ごとに主題と敵が違う。縦一本の単調さを構造で割る | [crawl.chaosforge.org](http://crawl.chaosforge.org/Dungeon_branches) | 中。縦一本の形を崩す＝要判断 |
| プレイヤーが難度と報酬を選ぶ | Path of Exile の map mods | 修飾を自分で選ぶ所有感 | [pathofexile.fandom.com](https://pathofexile.fandom.com/wiki/List_of_map_mods) | 中。難易度モードと二重になる |

**共通の教訓＝効いている手法の大半は「生成器（地形）を作り替える」ではなく「同じ地形の上に別の層を重ねる」。** ただし**それは実装をどこに置くかの話ではない**（Codex 裁定2）。Cogmind の Heavy も Cargo Convoy もゲームルールとして本体に入っており、描画層の後加工ではない。本作でも、**地形生成を変えないこと**と**ロジックを engine に置くこと**は両立する。先例＝`routeGuards`（主経路ガード・v0.152.0）は「追加スポーンなし・RNG 非消費で既存の敵を最短路へ再配置する」を `genFloor` の中で決定論的にやっている。`Floor.echo`／`Floor.hazards` の web 側 seeded 配置は**演出・content 層**の型であって、敵の位置と戦術には使わない。

---

## 3. 方針（4原則）

1. **地形の幾何は最後に触る。** まず同じ地形の上に層を重ね、それでも足りない時に Phase C で帯別パラメータへ進む。**これは「実装を web 側に置く」という意味ではない**（Codex 裁定2を受けて初版から改めた）。
2. **ゲームルールは engine に置く。** 敵の位置・戦術・報酬を決めるものは `dungeon.ts` か、そこから呼ぶ決定論的な純関数へ。**golden 不変を設計目的にしない**＝必要なら意図的に再生成し、配置の不変条件テストで守る。理由は Swift 移植（B柱1＝`main.ts` から純粋エンジンへ抽出する方向）と逆行させないため。演出・文言・音・content の seeded 配置だけが web 側。
3. **追加ではなく置換。** 初期配置の総数（`countCap` 42＋fodder）は動かさない。既存スポーンの一部を「役のある個体」に変える（Cogmind の流儀・`routeGuards` の延長）。
4. **「表示だけ」と「配置差」を分けて出し、分けて測る。** Phase A は表示だけ、Phase B は配置差。効いたのがどちらかを実機FBで切り分けられるよう、**同じ PR に混ぜない**。攻撃SFX の差し替えも Phase A に混ぜず独立の小PRにする（Codex 裁定1）。

Codex 案との差＝Codex は「次の節目の危険と報酬を予告して回収する」を第一手に置いた。本 RFC は**回収先（Phase B）を予告（Phase A の A1）と同じ優先度に置く**。予告は「先が気になる」に効くが、回収先の中身が同じなら二度目から効かないため。ただし A1 は数行で済むので先に出す。

---

## 4. 計画

### Phase A｜ボスの山場（表示だけ・web 限定・engine 非改変・golden 不変・SAVE_VERSION 据置）

| # | 中身 | 発火条件 | 文言案 | 再遭遇・再開 |
|---|---|---|---|---|
| A1 | 降り階段の一行 | `stairsPrompt("down")` で次深度が `isBossDepth`（`>=8 && %8===0`）。判定式は `foresee` と共有する純関数へ | 「下り階段がある。深度8へ降りるか？」＋「──下から、重い気配が上がってくる。節目の層だ。」 | 帰還の扉は階段プロンプトを通らない＝再放送なし。断定はせず気配だけ。報酬は予告しない |
| A2 | 初視認の一度きり告知＋進入バナーの副題 | ボスが**視界に入った最初の1手**。**`Floor.bossIntroSeen?: true`**（任意・`DiveSnapshot` は `floor` を直列化するので再開後も一度きり。**視界の一般状態〔`explored`〕と紛れない名にする**＝Codex 裁定4） | ログ「${名}が、降り階段の前に立っている──この層の主。」＋既存 `sfx("boss")`。バナー `深度 8 ─ 浅層 ・ 節目の層` | `bossEnragedSeen` は `enterFloor` でクリアされ往復で再放送される＝**同じ型にしない**。floor 側に持たせる。盤面には何も描かない（予告マスを覆わない・無音でもログとバナーで識別） |
| A3 | 決着後の「越えた」一行 | `handleBossResolve`／`rewardKill` の直後・area ボスのみ | 共通「主を越えた。」＋ D8 のみ「ここから下は、浅層ではない。」（`depthBand(8)=shallow → 9=mid` の境界と一致） | 縁なしボス（シートなし）にも同じ一行が出る＝縁の有無で焦点の差が縮む |
| A4 | ボス専用 BGM | **Phase A に含める（2026-09-11 ユーザー決定「音楽は重要」）。** 発火＝A2 の初視認と同時に `setBgm("boss")`、決着（討つ/鎮める/名を呼ぶ）で `dungeon` へ戻す | `BgmScene` に `boss` を追加。既存の迷宮曲に深淵帯の最低域を薄く重ねる（音源ファイル不要） | 帰還の扉で街へ出れば `town`、戻れば `bossIntroSeen && boss.hp>0` なら `boss` を再点火。`DiveSnapshot` からの再開も同じ判定＝**状態は floor から導出し、BGM 側に持たない**。`e2e-settings` 相当の実走で往復・再開を検査 |

### 4.1 追加要望｜攻撃のエフェクト音（2026-09-11 ユーザー「プレイしてて心地よくない」）

ユーザーFB＝現行の攻撃音が「プレイしてて心地よくない」。対象は `audio.ts` の `hit`／`crit`／`kill`。**Phase A には混ぜず独立の小 PR にする**（Codex 裁定1＝効いたものを切り分けるため）。音源ファイルは使わない（全てプロシージャル合成）＝engine 非改変・golden 不変。

**第1巡（Claude 案）＝不採用。** `tools/sfx-audition.html` に現行＋候補5種（A〜E）を並べてユーザーに試聴してもらったが、**現行を含む6つすべてが不採用**（2026-09-11）。

**第2巡（Codex 案）＝不採用。** ユーザー指示「Claude からは具体的な指示を出さず、このゲームに最適な効果音として Codex に作ってもらう」に従い、Claude は音の方向・周波数・層構成・質感を一切指定せず、診断も伏せて依頼した。Codex は Claude の試聴ページを読まずに、発音原理の異なる4系統〈断鉄／骨響／裂帛／刻印〉を新規制作（`sfx-audition-codex.html`）。ユーザー評価は「手応えがない」。これを受けて Codex が3案〈食い込む斬撃／重い打撃／硬い破砕〉を作り直した（`sfx-audition-astra.html`）が、こちらも採用に至らなかった。

**★打ち切り（2026-09-11 ユーザー判断）＝「効果音はもういいです。難しそうですね」。** 攻撃音の追加制作・選定・`audio.ts` への移植を**見送る**。**現行の `hit`／`crit`／`kill` は変更しない。** Codex が一時復元した試聴ページは revert 済み。Claude 側の `proto/tools/sfx-audition.html` は第1巡の記録として残す（現行音のベースラインを含むので、将来また触るときの出発点になる）。**実装順から攻撃音の小PRを外す**（下記 §7 の実装順を参照）。

**この論点を再開するときの申し送り**＝Claude 案6つ・Codex 案7つの計13案がいずれも通らなかった。**合成の方向を変える前に、「何が足りないか」をユーザーの言葉で1つに絞ること**（第1巡は理由を聞かずに作り直し、第2巡でようやく「手応えがない」が出た）。試聴ページは `sfx-audition.html` が残っている。

**Codex の留保への対応**＝長い会話・強制ムービー・操作停止なし（すべてログ1行＋既存音）／予告マスを覆わない（盤面非描画）／無音・RM で識別可（ログ・バナーは静的）／強化・報酬増量・戦闘機構変更を含まない／**「既存ボスの戦術差が実プレイで伝わるか」は A の FB で同時に聞く**（質問票 §5.2）。

### Phase B｜単調さの層（配置差・**engine 側**・golden は必要なら意図的更新）

**適用範囲＝全深度**（2026-09-11 ユーザー決定。初版の「D9〜D16 で試す」は撤回）。ただし各機構に固有の下限は残す（下記）。

**B1 関所の個体（Area Denial の翻案）。** `routeGuards` は最短路へ 2〜4 体を再配置している。これを延長し、**うち1体を「部屋と通路の接点」に立たせ、`Monster.warden?: fossilId` を付ける**。追加スポーンなし。

- **置き場所＝engine**（Codex 裁定2）。`genFloor` の中、または `genFloor` から呼ぶ決定論的な純関数へ置く。**`enterFloor` の後加工（web）には置かない**＝敵の位置と戦術を変えるゲームルールであり、Swift 移植で `main.ts` から剥がす対象になるため。**golden `genFloor` は意図的に再生成**し、代わりに配置の不変条件テスト（階段と重要地点を塞がない・`countCap` を超えない・同一 seed で同一配置）を受理ゲートへ入れる。
- **「縁」の定義（Codex 裁定3＝確定）。** `pickBossSource`（`dungeon.ts:744`）を**そのまま流用しない**。理由＝基礎重み 1 で無関係な他人も必ず候補に入り、「その階の化石」に限定されない。**重み計算だけを純関数 `fossilRelationWeight(world, fossil)` へ抽出し、ボスと B1 で共有する**（絆 +5+value／未完の因縁 +5／doom・fall 終端 +6／元相棒 +4／基礎 1）。B1 の候補は**現在階に配置された化石**（`floor.fossils`）に限定し、**重み > 1 の個体だけを「縁あり」**として `warden` の名と一行に使う。**該当が無ければ関所は汎用名で置き、縁を捏造しない。**
- **下限＝D5 以上**（中ボスと同じ。序盤の手触りを変えない）。
- **QA の置き場（Codex 裁定・2026-09-11）＝既存 `proto/tools/qa-route-guard.ts` を拡張し、あわせて `npm run check` へ登録する。** **このQAは存在するが受理ゲートに未登録**（`package.json` の `check` に `qa-route-guard` が無いことを実測で確認）＝「既存を拡張する」だけでは検査が走らない。検査項目＝`warden` の D5 下限／同 seed 同配置／追加スポーンなし（`routeGuards` と同じく総数不変）／床上かつ重複なし／階段と重要地点を占有しない／縁ありは**現在階の化石**かつ `fossilRelationWeight > 1`／縁なしは汎用名／`countCap`・hardcap 以内。

**B2 深蝕の帯（Cargo Convoy の翻案）。** 移動するのは敵ではなく**霧**。`Floor.drift?: { axis, pos, width, speed }` を置き、`endTurn` で数手ごとに一方向へ進める。帯内の @ は `addExp(小)`。避けて回り込むか、突っ切って先回りするか、**同じ階が二度と同じにならない**。表示は既存 `miasma` の hz クラス（新色不要）。発火＝**D5 以上**・階ごと 35%（調整候補）。進入時ログ「霧が、東から流れている。」 RM＝離散移動なのでアニメ不要。オリジナル要素＝原作は取りに行く任意イベントだが、本作では**逃げる対象**にした（パワークリープを起こさない・深蝕という本作固有の資源に接続）。

- **置き場所＝engine（Codex 裁定・2026-09-11）。** 霧は敵の位置を変えないが、**ターン進行と深蝕という資源を動かすゲームルール**なので B1 と同じ扱いにする。`Floor.drift` の**初期生成を `genFloor`** に置き、**進行は純関数 `advanceDrift` 相当**として engine に置く。web の `endTurn` は**呼び出しと表示だけ**。状態は `floor` へ直列化し、**save/resume 同値を検査**する。

**B3 封じられた間（Treasure Machine の翻案）。** 既存 chest を1つ「封」にし、**鍵＝その階の化石への干渉**（鎮魂か継承のいずれかを選ぶ）。報酬＝装備でなく**その先代が修めていた術**（`Fossil.spells` から1つ・習得済みなら形質）。物語と報酬が同じ一つの操作に乗る。使う器＝`chests`／`interventions`／`learnSpell`。**適用＝全深度だが、化石が置かれ、かつ `Fossil.spells` を持つ階に限る**（構造的な制約＝適用帯の制限ではない）。縁の判定は B1 と同じ `fossilRelationWeight` を使う。

B の順序＝**B1 → B2 → B3**（B3 は前提条件が揃う階でしか発火しないので最後）。各1PR。

### Phase C｜生成器の帯別パラメータ（Hauberk 型・golden `genFloor` 意図的再生成・承認事項）

大部屋率 0.16・ループ率 0.2・通路幅 1 を `depthBand` で分岐する。例＝mid はループを減らして袋小路を増やす（幅1通路の緊張）／deep は大部屋率を上げて通路を減らす（開けた空洞・薙刀と槍の三択が変わる）。**新しいアルゴリズムは書かない。** 縮小はしない（Codex と同意見）。Phase A/B の FB を見てから。

### Phase D｜深層の能力（D24 以降にしか出ない `ability` を2種・golden 動く・後回し）

数値を上げず「別のことをさせる」。charge／reach／形つき予告の追加型が確立済み。Phase C の後。

---

## 5. 評価（楽しさの判断と回帰検査を分ける）

**楽しさの proxy は CI に載せない**（Codex 裁定4）。受理ゲートへ入れるのはハード不変条件だけ＝決定論（同 seed→同結果）・到達可能性・`countCap`／hardcap を超えないこと・階段と重要地点を塞がないこと・save/resume 同値。

### 5.1 機械で出す（決定論・**CI 外**・手動・`tools/pacing-report.ts` を新設）

§1 の表を**before/after を同 seed・同難易度で並べて出す**ツール。Codex の指標のうち機械で出せるもの＝〈移動のみの手数 ≈ 最短歩数〉〈初の判断場面までの手数 ≈ `stairsUp` から最寄りの敵・宝箱・化石までの BFS〉〈判断点密度 ＝ (敵＋宝箱＋化石＋ノード) ÷ 床〉を採る。**難易度を引数に取る**（初版の easy 限定測定を繰り返さないため・既定は normal）。**「密度が下がらない」のような閾値 assert は置かない**＝良し悪しの判断であって不変条件ではない。

### 5.2 実機FB（質問票・新規ワールド normal・どの世界か明記）

1. D8 に降りる前、ボスがいると分かったか（A1）。
2. ボスを初めて見た時、通常敵と違うと感じたか。**何で**分かったか（グリフ／色／ログ／音）（A2）。
3. ボスの溜め大技・覚醒・眷属は「対処を変えた」か、それとも通常敵と同じ対処で終わったか（Codex の留保）。
4. 越えた後、何かが変わった実感はあったか（A3）。
5. 潜行中に「先が気になった瞬間」があれば、**何階で**何が起きた時か（B の前後で比較。深度を必ず添えてもらう）。
6. 寄り道した・撤退した・武器を持ち替えた・消耗品を使った場面があれば、どの階で何に対してか。

---

## 6. 採らないもの

- **storylet の量産**＝951本あり発火率が D12 で 0.55 に頭打ち。**本数を増やしても遭遇率は変わらない。**
- **フロアの一括縮小・敵数増加**＝Codex と同意見。歩数 172（D50）は多すぎるが、縮めるなら Phase C 以後に深層だけの逓減を別途判断。
- **DCSS 型の支道**＝縦一本の形を崩す。導入するなら「深度が進まない任意の横道」限定で別 RFC。
- **PoE 型の修飾選択**＝難易度モードと二重になる。
- **長編ストーリー・強制ムービー**＝ユーザーの守る条件と衝突。

---

## 7. 未決 → 決定（2026-09-11 ユーザー回答）

| # | 論点 | 決定 |
|---|---|---|
| 1 | A1 で「8の倍数」の規則を実質的に明かすか | **明かしてよい** |
| 2 | B の適用帯 | **最初から全深度**（D9〜D16 限定の試行はしない。ただし B1／B2 は D5 未満に置かない＝中ボスと同じ下限。B3 は化石と `Fossil.spells` が要る＝構造的制約） |
| 3 | Phase C の順番 | **Claude に一任**。判断＝**A → B1 → B2 → C → B3 → D**。理由＝B3（封じられた間）は化石が置かれた階に依存し効く頻度が低いので、生成器の帯別パラメータ（C）を先に入れて「絵が変わった」を早く出す。C は golden `genFloor` が動くので着手前に別途承認を取る |
| 4 | A4（BGM）を A に含めるか | **含める。音楽は重要** |
| 5 | B1 の「縁」の定義 | **決定済み（ユーザー委任 → Codex 裁定3）。** `pickBossSource` を丸ごと流用せず、**重み計算だけを純関数 `fossilRelationWeight` へ抽出してボスと共有**。B1 の候補は**現在階の化石に限定**し、**重み > 1 の個体だけを「縁あり」**とする。該当なしなら汎用名で置き、縁を捏造しない。Claude の当初案（`pickBossSource` をそのまま流用）は**基礎重み1で無関係な他人も候補に入る**ため取り下げ |

**実装順（決定・2026-09-11 更新）**＝**(A) A1〜A4 を1PR → (B1) → (B2) → (C・要承認) → (B3) → (D)。** 各 PR の後に実機FB（§5.2）を1回挟む。**攻撃音は §4.1 のとおり打ち切りのため実装順から外した**（初版の (0) を削除）。

## 7.1 Codex 検収（2026-09-11）への対応

| 指摘 | 判定 | 対応 |
|---|---|---|
| [P1] 中心診断が言い過ぎ（D24 後も敵種・装備は解禁される／「敵50＝hardcap」は誤り／能力10 は「能力なし」を含む／「D1 と D40 は同じ絵」は過大） | **全て妥当。再測して確認** | §1 を全面改訂。**さらに自分で重い誤りを見つけた＝初版の表は easy で測っており、ユーザーの実機FB は normal の世界から来ている。** normal を主の列に差し替え、easy は参考に落とした。頭打ちの原因を `countCap = 42`（`dungeon.ts:446`）と明記し、`MONSTER_HARDCAP` との取り違えを是正。結論は「戦術カテゴリと地形トポロジーが頭打ちになる一方、移動距離が伸びて遭遇密度が下がる」に限定 |
| [P2] B の適用範囲が文書内で矛盾（§7 は全深度・§4 と質問票は D9〜D16） | **妥当（決定の反映漏れ）** | §4 Phase B の見出しと B1／B2／B3、§5.2 の質問5 を全深度へ統一 |
| [P2] 他作品調査に出典を付ける／Hauberk の要約は裏が取れなければ弱める | **妥当** | §2 に出典列を追加（7件すべて URL）。Hauberk は**一次資料から直接引用できる2文に差し替え**、「cramped〜spacious」の具体表現は**取り下げた** |
| [裁定1] Phase A は A1〜A4 を1PR。攻撃SFX は独立小PR | **賛同** | §3 原則4・§4.1 に明記 |
| [裁定2] B1 を web overlay で golden 温存しない。engine か決定論純関数へ置き、golden は意図的更新＋配置不変条件テスト | **賛同。初版の原則①が Swift 移植（B柱1）と逆行していた** | §3 の原則を書き換え（原則2 を新設＝ゲームルールは engine・golden 不変を設計目的にしない）。§2 の教訓も「地形を変えないこと」と「実装をどこに置くか」を分けた。§4 Phase B の見出しから「web overlay・golden 不変を狙う」を削除 |
| [裁定3] B1 の「縁」＝`fossilRelationWeight` を抽出して共有・現在階の化石に限定・重み>1 だけを縁あり・該当なしは汎用名 | **賛同（当初案より良い）** | §4 B1 と §7 の表5に反映 |
| [裁定4] `bossIntroSeen?` を推奨・SAVE_VERSION 据置でよい・楽しさ proxy は CI 外・CI はハード不変条件のみ | **賛同** | §4 A2 の名を `Floor.bossIntroSeen?` へ。§5 冒頭に CI へ入れるものの範囲を明記し、§5.1 から閾値 assert を排した |

### 7.2 Codex 再検収（2026-09-11・head `7f69f67`）の結果と追加裁定

**再検収＝合格（P0/P1/P2 なし）。** Codex 側でも seed 1000〜1007・D1/8/16/24/37/50・normal/easy を再計測し、§1 の床・8近傍最短歩数・敵数・密度・敵種・能力種が丸め誤差内で一致したとの報告。**文書計画としての承認であり、Phase A〜D の実装と merge はユーザーの明示承認待ち。**

| 追加裁定 | 判定 | 反映先 |
|---|---|---|
| **B2 も engine 側**（`Floor.drift` の初期生成を `genFloor`・進行は純関数・web は呼び出しと表示だけ・floor へ直列化し save/resume 同値を検査） | **賛同。** 霧は敵の位置を変えないが、ターン進行と深蝕という資源を動かすゲームルールなので B1 と同じ扱いが筋 | §4 B2 |
| **B1 の QA は `qa-route-guard.ts` を拡張し、あわせて `npm run check` へ登録する**（現在このQAは存在するが受理ゲートに未登録＝「既存拡張」だけでは不十分） | **妥当。実測で確認した**（`package.json` の `check` に `qa-route-guard` が無い） | §4 B1 |

---

## 8. 触らないもの（本 RFC の全 Phase を通じて）

`FODDER_CLUSTER_SIZE`／武器数値／`SAVE_VERSION`／`countCap`（42）と `monsterHardcap`／難易度係数／街の描画・迷宮のセルサイズ・視界（論点クローズ済み）／U2（Swift）。
