# AIローグライク 設計スナップショット

> 本ドキュメントは「現時点の合意と未決事項を記録するスナップショット」です。
> 議論の進行にあわせて追記・更新していく、生きた設計ドキュメントとして扱います。

最終更新: 2026-06-13

---

## 0. プロジェクト概要

NetHack / Angband に代表されるローグライクを、**AIによる物語・キャラクターの動的生成**で現代化するプロジェクト。

- グラフィックの向上が主目的ではない。
- AI生成の素材とプロシージャル再構成で、攻略情報を覚えても予測しきれない体験を作ることが狙い。**予測不能性の源泉は「組み合わせ空間の広大さ × プレイヤーごとに固有の蓄積史」**（言葉の無限の新規性ではない＝4-9）。
- 基本はハック&スラッシュ。
- 地理的な核は「一つの街」＋「その下に広がる深く巨大な迷宮」。街は迷宮の上に人々が集まってできたもの。
- **世界の根っこ：** ある日、世界に突如ダンジョンが出現した。中でも最も深いものは、いまだ誰も底に到達できていない。その周りに冒険者と商人が自然と集まり、街ができ、栄えた。
- 『メイドインアビス』からは **「街と深い迷宮」という構造のみ** を採用し、それ以外は独自設定とする（寄りすぎるとパクリになるため）。
- **【AIの使いどころ＝制作時（4-9で確定）】** 「AIによる動的生成」は *実行時にLLMを呼ぶ* 意味ではない。LLMは**制作時**に高品質な素材を大量生成し、実行時はそれをプロシージャルに組み替える。**実行時LLMは排除**（理由・コスト・構造は 4-9）。

---

## 用語集（共通語彙）

- **化石化** … AIが生成した“その場限りの存在”（出会った探索者・自キャラ・事件）を消滅させず、世界の恒久史に一件の記録として焼き付け、以後「時間×深度」で変質し、後のキャラが再発見できる状態にすること。**保存ではなく「変わりゆく地層に埋める」こと。**
- **化石** … 恒久史に書き込まれた一件の記録（出自・死に様・深度・生成世代・関与度・干渉履歴）。
- **昇格** … 刹那の生成物が化石へ格上げされるイベント（＝化石化）。
- **再発見** … 化石を入力に、変質後の“現在の姿”を**エンジンが生成**して提示すること（化石レコード＋鋳造所断片の再構成。実行時LLMは使わない＝4-9）。
- **変質** … 化石が「時間×深度」で変わっていくこと（方向＝4-2、時間軸＝4-1）。
- **予備記録** … 深く関わった“生存中”の相手に残る弱い記録。死ねば本格的な化石へ格上げされる。
- **追跡対象（tracked）** … 匿名NPCと化石の中間。運命の弧を持つ少数の重要存在（4-7）。
- **運命の弧** … 追跡対象がたどる原型的な軌道。世代をビートに進み、観測時に解決される（4-7）。
- **lazy（遅延・遡及）生成** … 裏で常時シミュレートせず、プレイヤーが知る瞬間に辻褄を合わせて生成する方式。
- **深蝕（しんしょく）** … 深層の理に蝕まれた度合い（旧称：被曝。2026-06-13改称）。深く潜るほど蓄積し、魔法・スキルの代償として支払い、奇癖を生み、死後の化石の変質強度に直結する。**強さと人間性の取引の通貨。**

---

## 1. 永続性の 4 層モデル（世界の階層分解）

「世界のどの層を不変にし、どの層を可変にするか」という観点で、世界を 4 層に分解する。

| 層 | 内容 | 方針 |
|----|------|------|
| **第1層：世界の法則** | 物理法則、迷宮の存在理由、潜ると人体・精神に何が起きるか | 最も不変。AIに触らせない完全固定。戦略の土台。→ 内容は **4-3 にてほぼ確定**。 |
| **第2層：街と中心人物** | 拠点の街、地理、主要NPC（武具屋・酒場の主・ギルド長など） | ほぼ不変。骨格は固定、セリフ等の表面はプロセダルに動的生成（素材は鋳造所。二重構造）。→ **固定マップ＋骨格NPC4人＋街⇄迷宮往復制を 4-4B にて確定。** |
| **第3層：迷宮の構造とイベント** | 階層レイアウト、敵、アイテム、出会う探索者・遺物・事件 | 可変。AI生成の主戦場。潜るたびに違う物語が立ち上がる。 |
| **第4層：その瞬間の会話と描写** | 戦闘描写、NPCの一言、フレーバーテキスト | 最も可変。毎回フレッシュに生成される表層。 |

> **生成境界との対応（4-5B）：** 第3層（迷宮構造・イベント・出会う探索者）＝階層生成バッチ／第4層（会話・描写・伝説の語り口）＝要所の動的生成。化石の変質は層をまたぐ（恒久史に固定された記録を、再発見時に第3〜4層として描画する）。第1層・第2層は AI に触らせない固定骨格。
> ※**4-9 で実行時LLMは排除。** 第3〜4層の“動的生成”は「制作時LLM鋳造所＋実行時プロセダル」で実現する（実行時はLLMを呼ばない）。

---

## 2. 永続性モデルの選択肢（検討記録）

「AIが生み出した一瞬の出来事・人物を、どの単位で“歴史”に昇格させるか」を巡る 4 案。

- **モデルA：刹那の世界** — 永続性ほぼゼロ。毎回新品。純粋なローグライク。物語が積み上がらず使い捨て。→ 狙いと逆。
- **モデルB：個人の年代記** — プレイ（キャラの一生）単位で永続。死ぬと全消去。パーマデスの虚しさが直撃。
- **モデルC：堆積する世界（シングル）** — ✅ **採用**。条件を満たした生成物が「化石化」して恒久史に昇格。キャラの死を越えて残る。死が世界を豊かにする側に回る。
- **モデルD：共有される深淵** — Cをプレイヤー横断に拡張。NetHackのbonesの論理的極限。世界観とは好相性だが、サーバー・モデレーション・コスト負荷が大。→ **Cの将来構想として温存**。

> 先例：NetHack の **bones ファイル**（死亡したキャラの亡骸・装備・亡霊が後のプレイに再出現する仕組み）を、AIによる物語生成で大幅に拡張するのが本プロジェクトの核。

---

## 3. 確定した設計の背骨

- 永続性は **モデルC（堆積する世界・シングルプレイ）** を採用。
- 世界は **プレイヤーごとに独立した一つの宇宙**。自分の世界は自分だけのもので、他人とは混ざらない。
- **キャラの死は世界のリセットではない。** 死んだキャラ・出会った探索者・起きた事件は「化石化」して世界の恒久史に昇格し、次に作るキャラの冒険の中で再発見される。
- **昇格した生成物は、放置された時間ぶんだけ変質する。** 変質ルール（4-1×4-2）が状態を進め、表層は鋳造所断片で描画する。深い迷宮で人やモノが変わり果てていく様を、永続性の仕組みそのもので表現する（亡骸・手記・廃墟・亡霊・伝説・敵性化など）。
- 結果として、世界は **すべての生命の痕跡が重なった羊皮紙（パリンプセスト）** になり、プレイヤーは「自分だけの壮大な歴史」を一枚ずつ塗り重ねていく。
- **【重要原則】「物語の深まり」の源泉は蓄積データであり、（実行時）LLMから独立。** 「プレイするほど深まる／プレイヤーごとに固有」を生むのは化石レコードの蓄積と因果還流（モデルC）という *データ構造* であって、毎回AIが文章を書くことではない。ゆえに実行時LLMを排除し生成表層をテンプレ化しても、この体験の根は無傷で保たれる（→ 4-9）。

**一言要約：** NetHack の bones を「個人の永続する歴史書」へ昇華させたもの。

### 昇格の単位とトリガー（現時点の方針）

- **単位** ＝「一人の探索者／一つの遺物の“運命（末路）”」。floor単位やworld-state丸ごとではなく、人やモノの末路をひとかたまりとして結晶化させる。
- **トリガー（段階制）：**
  - **本格的な化石化＝「死」。** 探索者・自キャラ・遺物の末路が確定した時、一件の記録として結晶化・昇格する。
  - **副次＝「関与」。** 深く関わった“生存中”の相手は、死ぬ前でも**弱い予備記録**として残る。後にその相手が死ねば本格的な化石へ格上げされ、関与度は変質の濃さ（4-2）に効く。
- **lazy 末路解決：** 別れたNPCの生死は裏で常時シミュレートしない。次に関わる状況（再会・噂・捜索）が来た瞬間に「経過世代×深度×気質」から“その間どうなったか”を遡って生成する。死んでいれば予備記録を化石へ格上げ。プレイヤー体感の「知らぬ間に世界が動いた」とコスト最小を両立させる。

---

## 4. 設計詳細（確定事項と残課題）

> ※ 当初は「Open Questions」だったが、4-1〜4-7 は ★確定★。本セクションは確定した設計詳細の集積であり、真に未決の項目は §5 に集約している。

### 4-1. 変質の「時間軸」— ★確定★
4-2 の駆動式 `歪み = 深度 × 時間` の「時間」項を確定する。「時間」を **“化石が理に晒され続けた総量”** と定義し、**主クロック＋副クロックの複合**で測る。

**(A) 主クロック＝キャラ世代数（粗・大ジャンプ）**
- 死＝化石化のトリガーなので、世代をまたぐと過去は一気に変わり果て、地層が一枚増える。「自分の歴史が積もるごとに過去が地層化する」というモデルCの核を担う。

**(B) 副クロック＝“世界の呼吸”（細・深度比例のドリフト）**
- 生きている間も、深層の化石だけはゆっくり歪み続ける。深い場所ほど進みが速く、浅層は生者がいる間ほぼ止まる。
- 法則②と整合：**深い＝理が緩い＝放っておけば生死に関係なく勝手に歪む。** これにより「キャラが死なないと世界が完全静止する」問題を解消し、深層ほど“目を離した隙に化け物になっていく”緊張を生む。

> **採用理由（懸念への対処）：** 世代数クロック単独だと、1人のキャラで粘る間は世界が静止してしまう。副クロックでこれを補う。あわせて §3 の副次トリガー（出会い・関与の記録）により、新しい歴史の刻まれ方も「死だけ」に限定されない。

**(C) 時計の起点＝干渉で遅延・リセット可能**
- 基本は生成時起点だが、4-2 の干渉動詞（**鎮魂・継承・供養**）が関わると時計が遅くなる／巻き戻る。**放置こそが変質を進める**構図になり、干渉が明確な意味を持つ。

**(D) 初期史のシード＝薄く仕込む**
- 世代数クロックの「初期キャラの世界が空っぽ」問題への対処。名高い冒険者・老兵の伝説や古い亡骸を少数だけ事前配置し、初回から“誰かの歴史”を感じさせる（4-4 接続）。以後はプレイヤー自身の歴史で上書きされていく。

**この4要素が一つの式に収まる：** 時間は世代交代で大きく刻まれ、深層では生存中も滲むように進み、干渉で止まる/巻き戻る。初期はシードされた歴史が薄く下敷きになっている。

### 4-2. 変質の「方向性」— ★確定★
放置・昇格された生成物が「何へ向かって変わるか」。法則②「深いほど世界の理が緩む」から導出し、別個のルールを足さずに一本化する。**基本構造は「単一軸＋例外分岐のハイブリッド」。**

**(A) 主軸＝「変質強度」** — 原型からどれだけ遠ざかったか。変化を 2 系統に分離する：

- **朽ち（自然エントロピー）＝ 時間のみで進む。** 深さに関係なくどこでも起きる。白骨化・退色・錆。原型は読める範囲。
  - **時間単位：** 朽ちは主クロック（世代数）で進む。浅層の亡骸は生者が潜っている間ほぼ止まり、世代交代ごとに一段ずつ朽ちる（世代＝地質時間）。※浅層にも深度非依存の生存中エントロピーを足す案は、質感調整が要れば導入する将来フック。
- **歪み（理の漏れ）＝ 深度 × 時間。** 深い場所でしか始動しない“あり得ない”変化。深度がゲート（強度係数）、時間が浸漬の蓄積量。
  - 深×短 → まだ新しいのに既に歪んでいる／浅×長 → ただ朽ちて読める／深×長 → 原型を留めない。

**歪みの段階（デフォルト経路）：**
1. **風化・朽ち**（浅×短）— 白骨化、色褪せた手記、錆びた装備。トーン＝無常・郷愁。
2. **異形化の兆し**（中）— 理の漏れが形に滲む。亡骸が結晶を吹く、手記の文字が独りでに書き換わる、廃墟の幾何が歪む。トーン＝不気味・畏れ。
3. **別物への到達**（深×長）— 原型をほぼ留めぬ“あり得ないもの”へ。ここでトーンが両極に分岐する。

**(B) 例外分岐＝トーンの出し分け（三相）** — 高強度域に達したものは、質的トリガーでどちらの極へ転ぶかが決まる。**主軸が“どれだけ”を、トリガーが“どちらへ”を担う。**

- **既定（トリガー無し）＝喪失・無常の極** … 意志なき亡霊、形を失った残響、敵意なき異物。静かに人ならぬ何かへ。
- **神話・驚異の極（正トリガー）** … 偉業・特異な遺物・プレイヤーが深く関わった対象 → 伝説／守護者／聖遺物へ昇華。街に名を残す（4-4 接続）、迷宮の道標、入手困難な恩寵。
- **敵性化・怨念の極（負トリガー）** … 無念・裏切り・非業の死 → 明確な敵意ある存在へ。怨霊、プレイヤーを名指しで狙う異形、呪われた遺物。最も危険で最も濃い物語。

**トリガーの役割分担：**
- **死に様（無念／偉業／非業）＝ どちらの極へ向かうか**を決める。
- **プレイヤーの関与度＝ どれだけ“濃く・名指しで”再登場するか**（強度の特別ブースト）。深く関わった相手ほど、その死に様が指す極へ濃く結晶化する。

**設計原則（必須制約）：**
- **出自の痕跡を必ず残す。** どれだけ変わり果てても、元が誰／何だったかを推測できる手がかり（装備の残骸・繰り返す仕草・名の断片・筆跡）を残す。プレイヤーが末路を逆算で物語化できることがモデルC体験の核。読めなければただのランダムモンスターに堕し、パリンプセストが崩壊する。
- **干渉の余地を残す（読むだけにしない）。** 変質物にはプレイヤーが働きかける動詞を用意する：**鎮魂**（怨霊を鎮め末路を“閉じる”）／**継承**（死んだ探索者の未完の目的を引き継ぐ・聖遺物を奪還）／**浄化・供養**（敵性化を解き中立な道標へ戻す）。モデルCの情緒的ペイオフが宿る場所。
- **両極は稀に保つ。** 既定の大多数は静かな喪失。神話／怨念は条件を満たした少数だけに出し、希少性と特別さを守る。

**法則②からの導出（重要）：** すべての変質は「理の緩んだ場所（深度）に長く置かれた（時間）結果」であり、②以外の新ルールを足していない。トリガーは“方向の選択”だけを担い、変質の発生自体は②から自動的に出る。

**付随して残る小決定：**
- 放置時間を何で測るか → **4-1 で確定済み**（世代数＋深度ドリフト）。
- 正／負トリガーの具体的判定条件 → **方向確定：「死の瞬間の一手」（4-10B）。** プレイヤーの最終選択＋状況が極を方向づける。細部の数値化は実装時。
- **（将来構想）同じ場所での「重ね書き」** — 古い変質物の上に新しい末路が置かれると、両者が干渉・融合して複合変質が生まれる。パリンプセストの語義そのもの。Dモデル（プレイヤー横断）とも接続しうる芽。

### 4-3. 世界の根本法則（第1層）— ★ほぼ確定★
オリジナリティの生命線。NetHack 的な「薄く硬い前提」を志向し、以下を **ほぼ確定** とする（より良い案が出れば差し替え可）。

**確定要素：**
- **① 突如出現・底は未到達** — ある日、世界に突如ダンジョンが出現。最深のものは誰も底に到達できておらず、**底に達して帰還した者は一人もいない**。底があるのかすら誰も知らない（世界最大の謎を永久に開いたままにし、AI生成の余白とする）。
- **② 深いほど世界の理が緩む（唯一の根本物理法則）** — 迷宮は世界の異物であり、深く潜るほど“外の世界の常識”が効かなくなる。浅層はほぼ普通の洞窟、深層では時間・空間・生死・物の在りようが歪む。これにより (a) 深いほど高リスク高リターンの階層構造に必然性、(b) 放置物の変質を自動的に説明、(c) どれだけ突飛なものを生成しても世界が受け止められる。**生者への作用（第1層の「潜ると人体・精神に何が起きるか」）は 4-10C「生者の深蝕」で定義。**
- **③ 異物が街唯一の輸出品／各国が狙うが冒険者が抑止力** — 迷宮から得られる宝・異物だけがこの街の富の源泉で、街はその交易で栄える。富と異物ゆえに各国が街を狙うが、街には名高い冒険者が多数住み着いており、迂闊に手が出せない。
- **④ 街は迷宮に経済的に依存** — 商人・鍛冶・酒場すべてが潜る者の上前で成り立つ。「危険なのになぜ人が集まるか」への回答。

**あえて決めない事項（謎のまま据え置く）：**
- 誰がダンジョンを作ったか／出現の理由／底に何があるか。決めた瞬間に世界が小さくなるため、AI生成の余白として温存する。

> ②「深いほど理が緩む」が、4-2（変質の方向性）を自動的に回収する構造になっている。

> **【ロードベアリング注記】** ②は現在、設計全体の土台。変質（4-1／4-2）・英雄の弧（4-6）・再会と実装（4-7／4-5）・生者の深蝕（4-10C）すべてが②に依存している。差し替える場合は、これら全項目への波及を要確認。

> **【先例注記】** ①〜④の構図は『路傍のピクニック』（ストルガツキー兄弟／S.T.A.L.K.E.R. の原作）とほぼ同型（突如現れたゾーン／用途不明の異物が唯一の輸出品／ゾーン経済の街／深部ほど物理が壊れる）。差別化点を自覚するための参照として明記する。「異物＝用途不明・副作用つき」の質感は 4-10E で輸入。探検隊の手記の質感は『全滅領域』（サザンリーチ）も参照。

### 4-4. 名高い冒険者とプレイヤーの関係 — ★確定★
街を守る「名高い冒険者」を、**世界が用意したもの（シード）とプレイヤーが生み出したもの（昇華）の二源**で構成し、世代を経るほど後者へ比重が移る。

**(A) 構成＝混合（徐々に自分の史へ）**
- 初期はシードされた伝説（4-1-D）が街を守るが、プレイヤーが偉業を遂げて散らせた旧キャラ（4-2 神話極）が伝説に加わるほど、街の英雄譜は“自分の歴史の英雄”へ置き換わっていく。パリンプセストが街の人物にも及ぶ。

**(B) 偉大さの二つの末路（どこで会うか）**
- **生還して引退＝街の生者NPC（守護者・助言者）** … §4-3③の抑止力の正体。もう深くは潜らず街に居る。
- **深部で斃れた＝迷宮の伝説の化石（4-2 神話極）** … 亡霊・守護者・聖遺物として、潜る者が再発見する。
- 「生還」と「戦死」で末路を分けることで、英雄が街と迷宮の両方に立ち現れる。

**(C) プレイヤーとの関係性**
- **メンター・遺産の継承（主）** … 助言・技を授け、遺された目的や遺物を継承できる（4-2「継承」動詞と接続）。
- **旧キャラが後継に反応（主・情緒的ペイオフ）** … 自分の旧キャラが街の英雄となり、次の新キャラ（後継）に反応する。像や亡霊が語りかけ、街は血統・系譜を認識し、恩寵を与える。「かつて自分が死なせたキャラが伝説として街に残る」の最も輝かしい形。
- **雇用・同行／憧れ・ベンチマーク（従・任意のフレーバー）** … 一時的な助っ人、超えるべき頂としての記録。主軸ではないが選択肢として残す。

**(D) 抑止力（§4-3③）の扱い**
- 当面はフレーバー（街に英雄が多い＝各国が手を出せない）に留める。「英雄の数＝街の安全度」をメカニクス化する案は将来フックとして温存。

**(E) 冒険者の等級＝金属6段階 — ★確定★（2026-06-15 ユーザー承認）**
- ギルドが認定する等級を **素材型6段階**に統一する：**アイアン（鉄・新参）／ブロンズ（青銅・駆け出し）／シルバー（銀・一人前）／ゴールド（金・精鋭）／プラチナ（白金・英傑）／ミスリル（秘銀・神話）。**
- **ミスリルは原則として死後にのみ与えられる称号**（神話極の化石＝(B)の戦死ルート）＝`legendApprove`（伝説化承認）で授かる英雄譜の最上位。**例外的な生存ミスリルは設定上ごく少数（`adventurers.md` M4 等）。**
- **プレイヤー等級＝レベル×実績の両ゲート（★改訂 2026-06-23 ユーザー承認★。旧「レベル帯のみ・Lv12でプラチナ飽和」を廃し Lv50 スケールへ再配分＋ギルドが偉業を認定）：** 段kへ上がるには **最低レベル（`PLAYER_LV_GATE=[1,7,16,28,42,50]`）と最低実績スコア（`PLAYER_ACH_GATE=[0,0,2,5,10,20]`）の両方**を満たす。**実績スコア＝達成依頼 + 印×2 + 伝説×2 + クリア×4**（World 蓄積＝世代越え）。**プラチナ（英傑）＝クリア近辺**（Lv42＋実績10）。**プレイヤーは生きてミスリルへ至れる特別路あり＝クリア後限定（厳しめ）**＝`Lv50＋実績20＋ascended≥1`（生存ミスリルの少数に並ぶ）。相棒/NPCは従来どおりプラチナ止まり（`LIVING_GRADE_CAP=4`・`companionGradeFor`）。
- **昇格イベント（4-4E・2026-06-23）：** ギルドで等級が上がると段ごとに演出（`checkRankUp`/`rankUpScene`・`recognizedGrade` で既読管理）＝**銅銀＝簡素／金＝賞賛／白金＝英傑（荘重）／秘銀＝ギルドマスター登場の小イベント**。年代記（金以上）に刻む。
- 下ほど人数が多い**ピラミッド型**。種・中核は生存と偉業で1段ずつ**昇格（⤴）**し、上が空けば新参が**外から流入（🆕）**する「生きた名簿」。世代を越えた等級の上下動が「あの新人が今や精鋭」「英雄が帰らず化石に」という時間の重み＝本作の壮大さの核（4-1 主クロック）を生む。
- **登場人物の素材：** 冒険者150余名の設定集を **`adventurers.md`** に格納（中核★50＋種100＋ミスリル）。性格・目的・運命の弧（4-6）・メイン/サブ区分つき。制作時の素材であり、engine がプロシージャルに街・ダンジョンへ配置する（実行時LLMなし＝4-9）。
- 実装：`companion.ts playerGrade/worldPlayerGrade/playerAchievement`（レベル×実績の両ゲート）・`rankLabel`（index→ラベル）／`legendApprove`（死後ミスリル＝伝説化）／`web/main.ts heroRoll`（等級・英雄譜表示）・`checkRankUp/rankUpScene`（昇格イベント）。実績シグナル＝`World.questsDone`（`claimQuest` で+1）/`seals`/`tracked(player_legend)`/`ascended`。

### 4-4B. 街の固定マップと骨格NPC（第2層）— ★確定★（2026-06-14）
第2層（街と中心人物）を実体化する。街は「メニュー」ではなく**ダンジョンと同じく `@` で歩く場所**として持つ。骨格は完全固定（=AIに触らせない第2層の不変部）、表面（セリフ）のみプロセダルに動かす二重構造（§4-5/4-9整合）。

**(A) 街＝固定マップ（自動生成なし）**
- 街はダンジョンとは別の「場面」として持つ**手書き固定マップ**（procgen しない）。`@` の操作系はダンジョンと共通（スワイプ／タップ一歩／自分タップ待機）。
- **FOV なし＝全面可視**（街は安全・既知のため霧なし）。ダンジョンの FOV はそのまま。
- レイアウトは手書き固定（`content/town.json` 等の**データ駆動**：タイル＋建物＋封鎖ゾーン定義）。**恒久施設として拡張前提**＝建物追加はデータ1エントリ、区画解禁は封鎖フラグを外すだけ。広め（モック＝56×40）でカメラ追従スクロール＋全体図。
- **区画構成（ゾーン）**：①**商店街**（門の近くに店を集約＝潜行前の導線）／②**少し離れた地区**（住宅＋謎の施設）／③**貴族・統治者エリア**（封鎖・将来解禁）。
- **グリフ規約**：建物の看板＝**漢字**／街路の群衆＝**ラテン記号**（c/$/n/t/f）／景物＝**漢字**（井/碑/木）。ダンジョンの `@`/`r`/`†` と整合。言語は**日本語で進行**（グリフ/UIも日本語・漢字。国際化は将来課題）。
- 街の空気は §4-3③④を踏襲＝**欲と弔いが同居する、迷宮の上の街**。

**(B) 建物に入る方式＋常駐NPC（店・施設・住人）**
街路の**看板（漢字グリフ）に乗る → 屋内シーンに切替、中に固定NPC（店主／施設主）が常駐**（古典RPG式。街路はすっきり、屋内に宝・掲示板・依頼ボード等を将来置ける）。各NPCは安定id付きアクター記述子（§4-12G）、identity 不変・セリフ表面はプロセダル分岐（二重構造）。**役割の深さ（品揃え・サービスの中身）は NPC拡充と同じタイミングで content として詰める**（本節は配置・骨格・接続のみ確定）。名は仮。
> **挨拶の動的化＋店主目線の小イベント（2026-06-27・web）：** ①固定NPCの挨拶 `keeperGreeting` を世代/系譜/名声/状態で動的選択（初代で「先代の働き」と言う辻褄崩れを是正）。②**話者整合**＝店主目線で書かれた街 storylet（受付/店主/女将が語る）を `speaker:"keeper"` でタグ付けし、**雑踏（talkCrowd）から除外／店主に話しかけた時 talkKeeper が本人として配信**（時々メニュー前に挟む・`selectKeeperStorylet`）。雑踏NPCがランダム冒険者ヘッダーで店主台詞を喋る矛盾を根絶。店主には異名/装備/口癖が無いため keeper storylet のスロットは `#origin_name#`（店主名）のみ（受理ゲート＋event-check 4b で機械担保）。

**商店街（門の近く・潜行前の導線を集約）：**
| id | 施設 | 役割・機構の接続 |
|---|---|---|
| `fix_smith` | 武具屋（鍛冶ヴァロ「炉の番人」） | 装備の売買・修理／**異物鑑定**（4-11E/4-10E） |
| `fix_store` | 道具屋（ハル）★追加確定 | 消耗品の売買・携行品の補給 |
| `fix_guild` | 回収業ギルド（ギルド長） | **依頼/回収業**（4-10G）・等級・英雄譜（4-4）・系譜の恩寵 |
| `fix_tavern` | 酒場〈最後の一杯〉（女将マレン） | **噂のハブ**・情報／休息／深蝕を案じる |
| `fix_yen` | 書記の館（老書記イェン・既存 `witness_yen`） | **年代記**（4-10A）・**伝説化の承認**（4-4）・系譜 |

**少し離れた地区（住宅＋謎の施設）：**
| id | 施設 | 役割・機構の接続 |
|---|---|---|
| `fix_cult` | 謎の教団〈深淵を讃える者たち〉 | 深蝕＝**祝福**とする怪しい救済。危険な恩恵（4-10C/4-3②） |
| `fix_oddments` | 異物商〈奇物堂〉 | **いわく付き**＝用途不明・副作用つきの異物/武具（4-10E） |
| `fix_shrine` | 慰霊堂〈鎮魂の堂〉 | 化石の**鎮魂**（4-2）・深蝕の浄め |
| `fix_healer` | 薬師 | **深蝕の進行を診る／緩和・治療**（4-10C の対の弁） |
| （民家） | 住人 | 街の密度・背景（固定idは任意） |

> 昇華NPC（旧キャラ→街の生者・4-4(B)生還）は本節の外＝**次々段**。`witness_yen` は `fix_yen` に同定（id整理は実装PRで確定）。

> **(B-5) 街帰還の回復＝世界観で明示（2026-06-24 ユーザー承認・宿は作らない）：** 「生還で HP 全回復は RPG 的に不自然」というFBへの対応。**機構は現状維持**（生還で HP 全快＝`surfaceReturn`／深蝕は薬師が有料で祓う）とし、不自然さは**設定で解消**＝「この地が覆せないのは『死』だけ。それ以外（傷・疲れ）は街で無かったことになる」を**ゲーム開始の強制説明イベント**（初回のみ・`maybeIntro`・`intro_seen` で冪等）で迷宮／深蝕／薬師と併せて語る。専用の宿は作らない（HP動線は門周りの既存施設で足り、深蝕治療は薬師に集約）。バランス不変。

**(B-2) 街路の群衆（背景アクター）** — 一般人・商人・貴族・悪人・冒険者が街路を徘徊（プロシージャル配置、アクター記述子§4-12Gで生成）。背景の生命感＋軽い情報源。**固定idを持たない使い捨て**（グリフは c/$/n/t/f）。
- **同一来訪内の重複防止（2026-06-15・FB反映）：** `meetActor` の再会（既知NPC再抽選）＋断片プールの小ささで「同じ人物が同時に何人も居る」現象が出ていた。是正＝①断片プール拡張（`actor_name` 6→36 ほか role/epithet/gear を 6→21）＋②**来訪ごとに `sceneActorKeys` で見た目の素性（epithet+name+role）を記録し、既出なら引き直す**（再会は世代を越えてのみ起こる）。屋内の常連も同じ仕組み。閉じる語は場面依存（酒場の屋内＝「席を立つ」／立ち話＝「話を切り上げる」）。

**(B-3) 封鎖ゾーン（将来解禁フック）** — 貴族・統治者エリアは**現時点では未実装**。壁＋閉ざされた門＋**門番NPC**で封鎖し、解禁は将来の拡張（封鎖フラグを外すだけ＝データ駆動）。

**(B-4) 屋内アクターと町の動線の改修（2026-06-15 ユーザー承認・プレイFB反映）** — 公開版のプレイ指摘を受けた骨格の調整。配置のみで設計思想は不変。
- **ダンジョン門を中央広場へ。** 旧＝街の最南端（店から遠い）。新＝商業区のすぐ北の中央広場（門は店々に囲まれ、生還後すぐ売買できる＝潜行前後の導線を集約という(A)④の趣旨を強化）。
- **個人宅を小型化・多数化。** 民家を 3×3 の小屋に縮小し軒数を増やす（南側の空き地も活用）。今後の住人イベント拡充の器。
- **武具屋に店主2人（武器担当＝ヴァロ／防具担当＝甲冑師）。** 旧 `smithBuy` は全スロット混在で「武器が並ばない回」があった。是正＝**武器は武器担当が必ず武器のみ・防具は防具担当が必ず防具のみを商う**（`rollItemOfSlot`）。拾い物の売却（袋）は武器担当に集約。世界観：武具は地上で購える。
- **店内に雰囲気アクター（一般人/冒険者）。** 店主以外の常連を屋内に配置（話しかけ＝街路群衆の storylet を流用）。通常店は1〜2人、**ギルド・酒場・教団は内部を拡張して複数人＋テーブル等の調度**を置く。屋内は店主1人固定から **`Interior.actors`（副店主＋常連）＋`furniture`** へ一般化。
- 実装＝`townscene.ts`（`buildInterior(kind,data)`・`Interior.actors/furniture`・`InteriorActor`）／`content/town.json`（`gate`/`start`・`buildings`・`keepers.smith_armor`・`interiors` 設定）／`web/main.ts`（`drawInterior`/`interiorAct`/`talkKeeper` のロール対応・`smithBuyKind`）／`items.ts rollItemOfSlot`。セーブ非破壊（街は web 限定・ephemeral）。

**(C) 街⇄ダンジョンの往復制（新設）**
- 街にダンジョンへの**門／階段タイル `>`** を置く。踏むと潜行開始（現行の開始導線をここへ移設）。
- **生還で街へ戻る**（往復制）。死→世代交代後は**街から再開**＝拠点が街になる。系譜・年代記は街の書記（`fix_yen`）／ギルド（`fix_guild`）に接続。

**(D) 4-12 との接続**
- 街コンテキスト（4-12F「town」）のアンカーは、本節の骨格NPC（`fix_*`）＋長命の証人（4-10F）＋追跡対象の弧（4-6）。動詞：聞く／受ける／応じる。
- 拡充順（4-12H）への影響：**(H)-2 アクター記述子**の実装と、本節の街マップ＋店/施設NPCの結線をもって、(H)-3「街・依頼・生者の一気拡充」を解錠する。各店・施設の品揃え/サービス詳細も、この拡充タイミングで content として詰める。

### 4-5. 提示形態とプロトタイプ方針 — ★確定★（※生成のAI配置(B)は 4-9 で置換済み）
ここまでの設計（変質・化石・動的NPC）を「いつ・どこでAIを呼ぶか」に落とし込み、コストと実装負荷を制御する。

> **【方針転換・重要】** 本節(B)の「実行時にAIを呼ぶ」前提は **4-9 で排除**した。実行時ライブLLMは月額サブスクとして成立せず、オンライン依存・信頼検証・継続コストの負債も抱えるため、生成を「制作時LLM鋳造所＋実行時プロセダル」へ移し、**実行時LLMは使わない**。本節は“もし実行時にLLMで作るなら”の参考設計として残すが、**確定方針は 4-9**。

**(A) 提示形態＝ターン制テキスト/ASCII**
- NetHack/Angband 継承。ターンごとに時間が止まるため AI 生成の遅延（数秒）を自然に吸収でき、生成テキストをそのまま提示物にできる。実装・コスト負荷が最も軽く、本企画のAI生成と相性最良。

**(B) AI 呼び出しの配置（毎ターンは呼ばない）**
3つの境界でのみ生成し、戦闘ごとの逐次描写など毎ターン生成は採らない：
1. **化石の変質・昇格（再発見時の lazy 生成）** — 常時シミュレートせず、プレイヤーが再遭遇した瞬間に「**最後の干渉/生成起点からの経過世代 × 深度**」（4-1。干渉でリセットされた時計を起点にする）からまとめて生成（NetHack bones 式）。コストを再発見イベントに圧縮。
2. **階層・イベント構造の生成** — 階層レイアウト／出来事／出会う探索者を、階生成時にバッチで生成（第3層）。
3. **NPC・フレーバーの動的生成** — 主要NPCのセリフ、出会いの描写、伝説の語り口（第4層）。要所で呼ぶ。

→ 毎ターンの戦闘描写はテンプレート＋軽量置換で賄い、AI は“構造”と“節目”に集中させる。

**(C) スコープ＝個人プロトタイプ（まず検証）**
- 目的は「面白さの検証」。架構は軽量・実験優先、AIコストは自負担前提。配布・サーバー・モデレーション・コスト最適化（モデルD含む）は検証後。

**(D) コスト設計の指針（プロトタイプ）**
- 生成を境界イベントに限定する設計（B）自体がコスト制御。さらに頻度の高い軽いフレーバーは安価・高速なモデル、化石の変質や伝説昇華など“山場”は高性能モデル、と**モデルを使い分ける**（ティアリング）。
- lazy 生成＋生成結果のキャッシュで、同じ化石への再訪は再生成しない。

**(E) 最小実装ループ（検証対象）**
潜行 → 死/帰還 →（死なら）化石化・昇格 → 世代交代 → 次キャラで再発見（経過世代×深度で変質を lazy 生成）→ 干渉（鎮魂・継承・供養）。**この一周が情緒的に刺されば、設計の核は正しい。**

### 4-6. 存在の3ティアと lazy な世界の動態 — ★確定★
「lazy 生成で、どうやって“世界が勝手に動いている”を表現するか」への回答。

**(A) 存在の3ティア**
- **匿名** … 記録なし/弱い予備記録。会う時だけ生成。
- **追跡対象（tracked）** … 運命の弧を持つ少数の重要存在。**昇格元は3つ**：①シードされた有名冒険者/パーティー（4-1-D）②プレイヤーの伝説級の旧キャラ（4-2 神話極）③因縁の相手（関与度の高い予備記録）。
- **化石** … 末路が確定し恒久史に固定。再発見で変質生成。

**(B) 運命の弧（latent trajectory）**
- **レール（原型）をエンジンが抽選し、タグ整合スロットで細部を充填する**（完全創発ではない＝制約付き生成。素材は鋳造所）。原型例：引退の弧／破滅の弧／堕ちる弧／（プレイヤー由来）伝承変質の弧（美化・忘却・史実の歪み）。
- 進行＝世代がビート。観測しない間も“進んだことになる”が、解決・描画は観測時（lazy）。
- **法則順守：** 弧の変化も 4-1（時間×深度）・4-2（方向）に従う。**英雄も深部に関われば歪む（「最も偉大な者すら深淵に変えられる」）。**

**(C) “世界が動く”の見せ方（観測経路）**
- **不在に比例した変化**（放置が長いほど再会時の落差が大きい＝lazyの副産物）。
- **又聞き・噂・痕跡**（酒場の噂・訃報・掲示・手記を境界で生成）。直接見ていない出来事を伝聞で知る。
- **街の恒久的な見た目の差分**（像が建つ・店が畳む・慰霊碑・常連の入れ替わり）。
- **本人との再会**（制約付き生成。出自の痕跡を必ず残す＝4-2原則）。

**(D) 化石・街への合流**
- 破滅/堕ちる弧の終端で死ねば 追跡対象 → 化石へ昇格（既存パイプライン合流）。
- 引退の弧なら 街の生者NPCへ（4-4 生還＝街）。

> 要点：背景で誰も計算していなくても、**届く情報が常に辻褄が合っている**から世界は生きて見える。lazy は「動かさない」ではなく「観測時に動いた結果を見せる」。

### 4-7. 再会・遭遇の重み付け — ★確定★
化石/追跡対象/NPC の再登場を、純ランダムでも全スクリプトでもない **重み付き確率** で決める。土台は 4-5(B)-2（階層生成時のバッチ）。

**(A) 重み因子（高いほど出やすい・4つを合算）**
- **未完の因縁（最重視）** — やり残し（救えなかった／借りがある／決着がついていない）ほど再会しやすい。物語的な引きが最も強い。
- **絆・関与度** — 深く長く関わった相手ほど。関与度は変質の濃さ（4-2）にも効き二重に意味を持つ。
- **深度近接** — 現在の潜行深度が、その存在の死亡/活動深度に近いほど。
- **不在の長さ** — 長く出会っていない存在ほど番が回る（偏り防止）。lazy の“放置ほど激変”と噛み合う。

**(B) プレイヤーの能動性＝能動＋受動の両方**
- **受動** … 向こうから来る（重み付き出現）。
- **能動** … 死亡地点へ戻る／街で噂を集める／捜索する、で手繰れる。4-2「鎮魂・継承」の動機づけと直結。

**(C) 偏り対策＝クールダウン＋高絆は保証**
- 一度会った相手は暫く出にくくして連発を防止。
- 絆・因縁の強い相手は“いずれ必ず再会する番”が来るよう保証（重要な相手を取りこぼさない）。

**(D) 実装の乗り場・場所性**
- 階層Nの生成時、深度N付近に紐づくプールから重みでサンプリングして配置。街は街入場時に同様判定。
- 化石は基本「死亡深度の近く」に出やすい（場所性は出自の一部）。

### 4-8. その他の決着・温存事項
- **死の扱い** — モデルC採用により「死＝化石化のトリガー」として大枠解決。詳細演出は今後。
- **モデルD（プレイヤー横断の共有世界）** — 将来構想として温存。

### 4-9. 実装・商用化アーキテクチャ（オフライン二層＋制作時LLM鋳造所）— ★確定★
**決定：実行時（リアルタイム）LLM は排除する。** 全生成を実行時LLMで賄うと月額サブスクが成立せず（無妥協プロファイルで ≈$1.75/時間、ヘビー層は青天井）、オンライン依存・信頼検証・継続コストの負債も抱える。よって LLM は**制作時のみ**使い、実行時は**完全オフライン**にする。

**(A) 生成タイミング：制作時LLM鋳造所＋実行時プロセダル（実行時LLMゼロ）**
- LLM は build/更新時に動かし、高品質な断片（名前・墓碑銘・装備描写・死に様・手記・噂・変質descriptor・アーク/beat）を大量生成。**トーン極／深度帯／原型でタグ付け**して同梱する。
- 実行時は安価なプロセダルエンジン（**生成文法＋ストーリーレット＋タグ整合スロット充填**）が再構成。**実行コスト$0・オンライン不要。**
- 「出自の痕跡を残す／canon準拠」は**レコード値の差し込みで機構的に保証**。断片は制作時に人間がQA済 → **破綻文が出る経路が原理的に無い**（信頼を構造で担保）。
- 先例：Caves of Qud（文法ベースの伝説・歴史生成）、Wildermyth（テンプレによる運命アーク＝4-6 の直接先例）、Fallen London（ストーリーレット）、Dwarf Fortress（伝説モード）。

**(B) load-bearing（屋台骨）は二層**

| 層 | 内容 | 実行時コスト | 信頼リスク |
|----|------|------|------|
| **永続データ層** | 化石の蓄積・因果還流（モデルC） | $0 | なし |
| **テンプレ層（鋳造所）** | 断片の再構成＋**山場用の高品質“予約セットピース”** | $0 | なし（QA済） |

**(C) 山場＝予約した最高品質テンプレで賄う（実行時LLMではなく）**
- 50時間育てた英雄の死など最重要の節目こそ、賭けの生成ではなく**著者の保証**が要る。制作時に人間（＋LLM支援）で“最上級の手作りセットピース”を山場の型ごと（例：「自分の旧キャラが伝説として帰還」「救えなかった相手が怨霊化して名指しで襲う」）に書き溜め、スロットだけ差し込む。**craft・canon・情緒の調律を担保しつつ、実行コスト$0・破綻ゼロ。** 山場はライブLLMより予約手作りが勝つ領域。

**(D) 「深まる物語」はデータ層に宿る** — §3 重要原則どおり、深みの源泉は蓄積状態と因果還流であり実行時LLMから独立。実行時LLMを排除しても、所有・固有の星座・因果の糸は無傷。失うのは「言葉の無限の新規性」のみ。

**(E) ビジネスモデル：完全オフライン買い切り**
- 本編＝**オフライン買い切り**。**継続コスト$0・信頼リスク$0・オンライン不要・プロバイダ非依存。**
- 更新で鋳造所を再実行して断片を拡張＝**月額課金なしで世界が新鮮に**（無料アップデート）。
- ※実行時ライブAI（BYO-key 等）は今回**明確に排除**。将来どうしても“真の新規性”を実測検証したくなった場合の遠い選択肢としてのみ温存（本筋ではない）。

**(F) 制作時LLMのモデル選定（鋳造所の質）**
- 鋳造所・セットピース執筆は最高品質で行う：**Fable 5／Opus 4.8／Sonnet 4.6** を制作時に使用。これは**開発コスト（一度きり／更新時）であり、ユーザー単価ではない。** 構造化出力で断片スキーマを揃える。料金は Haiku $1/$5・Sonnet $3/$15・Opus $5/$25・Fable $10/$50（入力/出力・100万トークン、2026-06時点）。

**コスト整理：** 実行時 $0。LLM 費用は**制作時のみ＝小さな一括開発費**。サブスク不要、買い切りで成立。

### 4-10. 追加採用システム（他作品リサーチ由来）— ★確定（方針）★
通し点検（2026-06-12）で採用を決めた拡張。いずれも既存の骨格（モデルC・変質・オフライン二層）に接続する形で定義する。

**(A) 年代記（クロニクル）** — *Dwarf Fortress 伝説モード／Wildermyth*
- 自分の世界の歴史書がゲーム内で自動編纂される：歴代キャラの生涯・出会った探索者の末路・伝説の系譜・干渉の記録。
- 化石レコードがそのままデータ源（実装コスト小）。**蓄積した歴史を「読み返せる」ことで、パリンプセストの深みを知覚可能にする必須UI。**

**(B) 死の瞬間の一手** — *Hades／墓碑銘の伝統*
- 死亡時、プレイヤーが**最後の行為を一つ選ぶ**（遺品を抱いて守る／迷宮を呪う／後継への遺言を書く／静かに受け入れる…）。
- これが 4-2 の**死に様トリガーへの入力**となり、神話/怨念/喪失の極を方向づける（「正/負トリガーの数値化」の解）。
- パーマデスの瞬間が「ロスト」から「**自分の化石の作者になる瞬間**」へ反転する。モデルCの哲学の完成形。

**(C) 生者の深蝕** — *Darkest Dungeon／S.T.A.L.K.E.R.*
- **生きている者も深度に晒されるほど「歪み」を蓄積する。** 浅層は無害、深く長く潜ると奇癖・変調・小さな異形化（リスク管理の中毒性）。
- **死亡時、生前の深蝕量が化石の初期変質強度に加算**（深層で長く生きた者ほど死後激しく変わり果てる）— 4-1×4-2 と一本の式で接続。
- これにより**第1層の「潜ると人体・精神に何が起きるか」が初めてルール化**され、4-6(B)「英雄も歪む（堕ちる弧）」の機構的裏付けになる。
- **即時の牙（2026-06-15 ユーザー承認・実装済み Web）：** 深蝕は「死後の怨念化（後払い）」だけでなく**在りし日の生存圧**にもなる。**深蝕 ≥ 1.2（2番目の変質閾値・怨念寄りライン）を超えると、歩く（手番が進む）たびにHPを失う＝深みに蝕まれる。** 量は **1HP/手・深蝕+1.0ごとに+1**（1.2–2.2→1／2.2–3.2→2／…）逐増。HP0は通常死＝高い深蝕が強い怨念化に直結する正帰還。ダンジョンのみ（街は安全地帯）。実装＝`web/main.ts endTurn`（`CORRUPTION_DRAIN_FROM=1.2`）。
- **対の弁（深蝕を退ける手段）：** ①薬師（金貨・−0.6）②慰霊堂（鎮魂/祈り）③**深蝕回復イベント**＝深蝕が溜まった時だけ出るダンジョン storylet（澄んだ湧き水/澄んだ空気/岩盤の裂け目の陽）で −0.4〜−0.6。④**携行できる消耗品**（道具屋・下記 持ち物システム）。教団（深蝕を上げる）と非対称に拮抗。実装＝`storylets.ts pickByContext` に `minExposure` ゲートを追加（深蝕を引数で受ける）／`content/storylets.json` の `dg_mid_springwater`・`dg_deep_breath`・`dg_deep_skylight`。

**持ち物システム（4-10G／2026-06-15 ユーザー承認・3段階で実装）：** H&S のキモ＝持ち物。**容量がレベル/装備で増え、自宅に保管できる**を骨子に、段階的に実装する。
- **Phase 1（実装済み Web）＝消耗品＋持ち物＋容量＋道具屋。** 消耗品（装備とは別系統＝`Character.inventory: {key,qty}[]`・同種はスタック・容量は枠数で数える）。第1弾2種＝**鎮静の薬**（深蝕 −0.6・携行できる薬師・16金）／**治癒の膏薬**（最大HPの半分回復・潜行中専用・12金）。容量＝`carryCapacity = 6 + ⌊level/2⌋`（Lv1→6, Lv4→8, Lv10→11）。**道具屋ハル**（`fix_store`）の3動詞を結線＝買う/売る（半値）/携行品を整える（使う・捨てる）。潜行中は **「品」ボタン**で使用＝**一手消費**（敵が動く＝戦術的）。HPは潜行ごとに全快（`startDive`）＝治癒は潜行中のみ意味を持ち、深蝕薬は持続するので街でも有効。データ型＝`items.ts CONSUMABLES`／`progression.ts carryCapacity`／`types.ts InventorySlot`。セーブは任意フィールド＝**version 8 据え置き**（非破壊）。**付随修正：** `endTurn` が `updateStatus()` を呼んでおらず HP/深蝕バーが毎手更新されなかった既存バグを修正。
- **Phase 2（実装済み Web）＝鞄。** 「装備で容量が増える」＝**4つ目の装備スロット `bag`**（`Item.capacity`＝持てる枠+／`Equipment.bag?` は任意＝旧セーブ非破壊で **migrate不要・version 8 据え置き**）。`carryCapacity = 6 + ⌊level/2⌋ + 鞄`。鞄3種＝革袋(+3)/探索者の背嚢(+5)/深淵の嚢(+8)を装備テーブルに追加＝**ドロップ・武具屋に自動で乗る**（既存 `rollItem`／装備プロンプト／売却／メニュー表示を流用）。データ＝`items.ts TEMPLATES`／`progression.ts carryCapacity`／`types.ts ItemSlot+Item.capacity+Equipment.bag`。
- **Phase 3（実装済み Web）＝自宅の保管庫＝武具庫。** 民家1軒を**自宅**（`town.json` で `kind:"home"`・看板「宅」）にし、**消耗品も装備（武器/防具/遺物/鞄）も**預ける／引き出す／検める。保管先＝**`World.stash`（消耗品）/`World.stashGear`（装備）**（Character でなく World ＝**世代を越えて残る**＝前の自分／血統が遺した備え）。装備は「外して納める／荷物へ引き出す」＝自宅が**armory**。**★引き出しの改修（v0.120.0・2026-07-05 テストプレイFB「引き出しで装備と交換になり不便」）＝** 旧仕様は「取り出して即装備＝今の同種を武具庫へスワップ」だったが、意図せず装備が入れ替わり不便だった。**引き出し＝荷物（`gearBag`）へ移すだけ**に変更（迷宮の戦利品と同じ流れ・装備は「装備・荷物を見る」から任意に）。荷物が満杯なら整理を促す（消失なし）。預ける側は元から `gearBag`↔`stashGear` 対称ゆえ往復が素直に。
  - **容量（ユーザー指定・H&Sリサーチで決定）：** 総容量＝消耗品スタック＋装備の合計で `STASH_CAP=60`（現代H&Sの1タブ相当＝D4 50／D3 70の中間。Diablo級の数百枠はリスト型UIで非現実的なため不採用）。**世代交代で次代へ遺せるのは消耗品・装備それぞれ `STASH_INHERIT=4` 枠まで**（残りは失われる＝`fossilizeCurrent` で各 slice 切詰め）。「生前は溜め込める／遺せるのは一部」で収集欲と喪失の緊張を両立。引き出し（消耗品）は持ち物容量を尊重。**★世代全引き継ぎへ変更（v0.136.0・2026-07-08・テストプレイFB「H&S ゆえ世代交代で保管庫を全て引き継ぎたい」→ユーザー承認〔案A〕）＝** `STASH_INHERIT` の相続枠切詰めを撤去し、**保管庫（stash/stashGear）は世代を越えて全て次代へ受け継がれる**。保管上限 `STASH_CAP`（60/館100）は預け入れ時に別途担保。**難易度の核は不変**＝雪だるま防止はキャラ本体のステ/術/Lv 継承（`applyLineage` 功績比例）が担い、保管庫の永続はそれと別軸／深部の敵は深度スケールで追随（装備は横ばい）。留意＝新Lv1が深層装備を引き出せば序盤フロアは楽になる（自己申告・周回QoL寄り・実害小）。engine（world.ts の切詰め撤去）＋UI 文言＝golden 8/8 完全不変。`STASH_INHERIT`/`STASH_INHERIT_MANOR` は退役（未使用・progression.ts に定義は残置）。
  - 実装＝`progression.ts STASH_CAP/STASH_INHERIT`／`types.ts World.stash・World.stashGear`／`world.ts`（init・migrate・切詰め）／`web/main.ts homeDeposit/homeWithdraw/homeView`／`townscene.ts`（home の屋内）。セーブは任意フィールド＝**version 8 据え置き**（非破壊）。
  - **入手＝銀昇格で「倒れた冒険者の家を継ぐ」（2026-06-24 ユーザー承認・強制イベント）：** 自宅は初期未所持＝最初から持っているのは不自然、という指摘への対応。**銀（grade 2＝Lv16＋実績2）に昇格した瞬間**（`checkRankUp` が g===2 を跨ぐ＝ギルド入場で発火）、ギルド長が「還らなかった先達（化石があればその冒険者／無ければ名簿の故人）の空き家」を継がせる強制イベント（`grantHomeScene`）で解禁。それまで自宅の戸は施錠（`enterBuilding` で「空き家」表示・利用不可）。状態＝`World.homeUnlocked`（新規=false 明示／**旧セーブは migrate で true＝grandfather**・SAVE_VERSION 据置）。「死・継承・堆積」のテーマに合致＝先に逝った者の生活の場を継ぐ。
- **Phase 4（実装済み Web・2026-06-15 ユーザー承認）＝拾った装備の袋と売却経路の是正。** 旧仕様は装備ドロップを**その場（無人の迷宮）で即・満額換金**でき、没入を損ねていた（ユーザー指摘）。是正＝**拾った装備は「袋」に持ち帰り、売る相手に売る**。
  - **拾った装備の袋＝`Character.gearBag: Item[]`**（未装備の拾い物を潜行中に携行＝世代内。死亡で失われる＝H&Sの緊張）。容量＝**レベル連動＋道具で増加**＝`gearCapacity = GEARBAG_BASE(3) + ⌊level/2⌋ + 鞄(bag.capacity)`（消耗品容量と同じ鞄を共用＝「良い鞄ほど何でも多く持てる」）。
  - **売る相手は2つ**：①**街の武具屋**（鍛冶ヴァロ）＝確実・高値（売値＝`itemValue×0.6`）。武具屋の動詞を「買う／売る」のハブ化。②**ダンジョンの行商人**＝潜行中に稀に出くわす出会い（売値＝`itemValue×0.45`＝安いが便利・持ち帰り前に現金化）。**「その場で誰もいないのに売れる」は廃止**（装備プロンプトの選択を「装備する／袋にしまう／見送る」に）。
  - 実装＝`types.ts Character.gearBag`／`progression.ts gearCapacity/GEARBAG_BASE`／`world.ts`（init・migrate backfill）／`web/main.ts`（`equipPrompt` 改修・`gearBagPush`・`smithShop/smithSell`・`maybeMerchantEncounter`）。セーブは任意フィールド＝**version 8 据え置き**（非破壊）。

**(D) 系譜の形質** — *Rogue Legacy／Crusader Kings*
- 新キャラ作成時に**先代との関係**（血縁・弟子・無関係）を選択。関係に応じて形質・因縁・初期評判を継ぐ。
- 4-4(C)「街は血統・系譜を認識」のメカニクス化。「無関係」も選べるので自由度は保つ。
- **二択リワーク（v0.55.0・2026-06-27 ユーザー承認・テストプレイFB「弟子が一方的に有利」是正）：** 旧＝血縁が術2/弟子が術3で弟子が純上位互換（形質ラベル差は無効果）だった。新＝**血縁＝大器晩成／弟子＝先行逃げ切り**の明確な二択。
  - **血縁**＝継ぐ術2つを**自分で選ぶ**（回復/帰還を最初から持てる質）＋**恒久ベース加算**（先代Lvに応じ最大+5・Lv上限で無系譜を確実に上回る）。
  - **弟子**＝継ぐ術4つを**自動**（多芸だが選べない）＋**開始レベルが上がる**（先代Lvに応じ最大+8・その分ステ即付与＝前借り型＝Lv上限では無系譜と同地力）。
  - ステ配分は**先代の得意ステに寄せる**（化石に死亡時 Lv/ステを記録）。実装＝`world.ts createCharacter`／`web/main.ts pickBloodSpells`。数値はテストプレイ調整候補。

**(E) 異物の質感** — *『路傍のピクニック』*
- 迷宮の異物（輸出品③）は**用途不明・副作用つき**を基本とする。鑑定・実験の楽しみと、ゾーン経済の手触りを輸入（4-3 先例注記参照）。

**(F) 長命の証人NPC** — *葬送のフリーレン*
- 第2層の固定NPC（ギルド記録係 or 酒場の主）を**歴代キャラ全員を個人的に覚えている長命の証人**とする。
- 「あの子と同じ目をしている」— 年代記(A)の語り部であり、世代を跨ぐ感情の連続性のアンカー。

**(G) 回収業（依頼経済）** — *ダンジョン飯／不思議のダンジョン系*
- 先代の亡骸・遺品の回収を、自分で行く／他の探索者に依頼できる。③④の経済に接続し、干渉動詞（鎮魂・継承）への経済的導線になる。
- **✅ 依頼の多様化＝供給増＋型追加＋データ駆動（v0.100.0・2026-07-02・テストプレイFB「Lv15で受注できるのが2件・型も乏しくギルドの存在意義が薄い」→ユーザー承認・engine＋web＋content・golden 8/8 不変）：** 少なさの主因は**盤面のハードキャップ**（`main.ts` の `limit=max(0,2-(done+active))`）だった。是正＝盤面を **`GUILD_BOARD_SIZE=5` まで多様に掲示**＋同時受注 **`GUILD_ACTIVE_CAP=4`**。**新クエスト型3種を追加**（既存 descend/reclaim/slay に加え）＝**bounty**（`MONSTER_KINDS` の特定種を N 体討伐・`onKillMonster` を web の `rewardKill` から呼ぶ）／**rescue**（手負い delver を救助＝`rescueScene` で `onRescueDelver`）／**fetch**（指定スロットの武具/遺物を N 点持ち帰り納品＝盤面で所持判定→受取時に消費）。**escort（護衛・同行）は後続スライス**（同行者束縛が最重）。**データ駆動**＝`content/quests.json` の型別テンプレ（`#depth#/#count#/#monster#/#fossil#/#actor#/#item#` スロット）を `generateOffers(…, opts)` に渡す。**★golden 安全の要**＝`generateOffers` の新型は **opts 有り（web）時のみ**生成＝CLI/`stress-multigens`（opts 無し）は descend/reclaim のみで**従来と完全一致・id 一意も不変**。新ヘルパ（`onKillMonster`/`onRescueDelver`）は純エンジンだが **web からのみ呼ぶ**＝決定論経路に漏らさない。SAVE_VERSION 据置（`Quest.source/targetKind/need/have` は任意＝`migrateWorld` で bounty の `have??=0`）。報酬＝descend `depth*9`／reclaim `laidDepth*6+12`／bounty `need*(tier*3+4)`／fetch（遺物 `level*3+30`／武具 `(level*2+12)*need`）／rescue `(level+6)*8`＝いずれ調整候補。**残＝escort 型・酒場の第2依頼源（PR2）・balance 実測。**
- **✅ escort 型＋酒場の4役割（v0.101.0・2026-07-02・上記の残スライス＋酒場リワーク・ユーザー承認・web+engine(opts限定)+content・golden 8/8 不変）：** **escort（護衛）**＝依頼人を companion スロットで指定深度まで生かして連れる（受注で同行〔`Companion.escortQuestId`〕・到達で離脱＝done・戦死/解散で失敗＝`failEscortQuest` 取り下げ・相棒雇用中は掲示されない・報酬 `depth*14`）。新型候補（bounty×2/fetch/rescue/escort）は**シャッフルして残枠に詰める**＝盤面の顔ぶれが毎回変わる。**酒場の4役割**（「ほぼ存在意義が無い」FBへの根本対応）＝①**仕事の貼り紙**（`questBoard("tavern")`＝人に接地した裏仕事のみ・`TAVERN_BOARD_SIZE=3`・descend/reclaim はギルド専売＝店の分業）②**門出の一杯**（`Character.pendingDiveBuff`＝金貨で次の潜行の最初のフロアに小バフ〔武勇=攻+5／加護=被ダメ−4・10手〕・適用は `startDive` の `enterFloor` 後＝バフリセット後・1潜行1回）③**相棒を探す**（常連〔縁ある生者〕優先＋一見の顔→既存 `offerCompanion` 流用）④**裏取引**（高性能・超高額 `itemValue×4` の訳あり品2点＝gold sink・品揃えは seed×世代の専用 rng で固定＝リロール漁り防止・購入済みは世代フラグで棚から消える・`enchantUp×2` で+2保証）。酒場の既存3動詞（噂/尋ねる/休む）は据え置き＝計7動詞。数値（`TAVERN_BOARD_SIZE`/`SENDOFF_TURNS=10`/門出価格 `12+level`/`BLACKMARKET_MUL=4`/escort 報酬係数14）はテスト調整候補。
- **✅ 酒場専用のグレー依頼＝盗掘・密売（v0.119.0・2026-07-04・テストプレイFB「酒場の依頼がギルドとほぼ同じ／酒場にはギルドに無いグレーな専用を」→ユーザー承認・engine(opts限定)+web+content・golden 8/8 不変）：** 旧・酒場ボードはギルドと同じ型のサブセット（bounty/fetch/rescue/escort）だけで独自性ゼロだった。**「街の裏面」＝ギルドは生者と名誉ある仕事／酒場は死者の持ち物と禁制品を扱う署名なき口**、という住み分けを実体化。**①盗掘（`plunder`）**＝回収（reclaim）の闇の鏡＝既知の化石を名指しで暴き遺品を剥ぐ。フック再利用＝化石再発見（`onPlunderFossil`）。**冒涜の代償（シビア寄り・毎回）＝深蝕＋`PLUNDER_EXPOSURE(0.4)`＋その化石を怨念（grudge）へ傾ける**（`tonePole="grudge"`／`bondAtDeath≥3`／`lastTouchedGeneration` リセット）＝後世で宿敵として還りやすくなる＝「金のために自分の未来の敵を作る」（4-2 と直結）。報酬 `laidDepth*8+20`（reclaim×6+12 より厚い＝グレー割増）。**②密売（`contraband`）**＝納品（fetch）の闇の鏡＝**未鑑定の異物（呪物）**を N 点、鑑定せず故買屋へ。フック再利用＝袋の納品判定（`questItemMatch` の predicate だけ差替＝`it.unidentified`）。**代償＝呪物1点ごとに深蝕＋`CONTRABAND_EXPOSURE(0.08)`**（納品時 need 点ぶん）。報酬 `(level*3+24)*need`（禁制品ゆえ高額）。**両者ともギルドには一切出さない**（`generateOffers` で `informal`＝酒場時のみ生成・小ボードを優先で埋める）＝CLI/opts 無しは従来完全一致＝**golden 8/8 不変**（新型は bounty/fetch と同じ opts ゲート）。`content/quests.json` に plunder/contraband テンプレ各4。検証＝ヘッドレス（ギルド5000件にグレー混入0・酒場1000板は毎回 plunder+contraband・スロット未充填0）＋実ブラウザE2E（対象化石を暴く→深蝕＋0.4・化石 grudge 化・bond3・依頼 done・例外0）。数値（`PLUNDER_EXPOSURE`/`CONTRABAND_EXPOSURE`/報酬係数）はテスト調整候補。

**(H) ペーシング・ディレクター** — *RimWorld のストーリーテラー*
- 4-2「両極は稀」・4-7 の重みの上に、緊張と緩和のリズムを監督する薄い調停層（山場の連発防止／凪が長すぎたら因縁を浮上）。実装は後期、概念のみ予約。
- **✅ 第一層 実装（Web・2026-06-21・ユーザー承認／イベント設計見直し P1）：** ①**序盤スロープ**＝迷宮環境イベントの発火率を深度で逓増（`maybeDungeonEvent`＝深度1は出さず、深度2≈0.19→上限0.55）。②**1降下1イベント**＝降下時に「迷宮の気配」と「行商人」を同時に出さない（前者が出れば後者を抑制）。③**直近重複回避メモリ**＝context横断のリング `recentEvents`（長さ6）を全選択（dungeon/town/chest/encounter）に渡し、一般プールから直近の話を除外（弧/固有ビートは無視＝確実に前進・候補が尽きたら全体に戻す＝`pickPreferArc(pool,rng,avoid)`）。
- **✅ 第二層 実装（Web・2026-06-21・ユーザー承認）：** ①**山場連発防止**＝山場（`legend_return`/`grudge_hunt`）を見せたら `setPieceCooldown`（既定12手・**ダイブを跨ぐ**・`endTurn` で毎手減算）が切れるまで、次の山場演出を抑え通常遭遇に落とす（大マップで化石が複数並んでも山場が立て続けに起きない）。②**凪検知（ピティタイマー）**＝降下してイベントが一度も起きていない回数 `quietDescents` に応じて `maybeDungeonEvent` 発火率を加点（`+quietDescents×0.15`・上限0.9）し、長い無風を自己補正（**第一層の初回手触りは不変**＝q0は旧と同率／深度5で「5降下連続無風」が旧17%→0.4%）。化石遭遇/宝箱/行商人/迷宮の気配いずれかが起きれば `quietDescents` を0に戻す。③**因縁を浮上**＝凪が `PITY_BOND_AT`（既定2）降下続き、未完の絆を抱えていれば `enterFloor` の化石数を+1（`rollEncounter` の未完因縁+3.0重みを再利用＝因縁化石が盤上に出やすくなる）。カウンタは `DiveSnapshot` に保存＝途中閉じでリセットさせない（抜け道封じ）。数値（`SETPIECE_COOLDOWN`/`PITY_STEP`/`PITY_BOND_AT`）はテストプレイ調整候補。

**(I) 化石の残響召喚** — *Elden Ring の遺灰*
- 継承の発展形：神話極の化石から**先代の残響を一時的な共闘者として喚べる**。稀・代償つき前提（乱用すると世界観が軽くなる）。

### 4-11. 戦闘の設計 — ★確定★
NetHack の深さは戦闘の外（準備・資源・判断）にあり、戦闘そのものに意思決定が無い。本作は **「読める盤面 × 深蝕との取引」** で毎ターンに意思決定を入れ、差別化する。（2026-06-13 議論で確定）

**(A) 基礎＝読める盤面（決定論コア）**
- 通常攻撃は**確定命中・確定ダメージ**（miss無し。Into the Breach 系）。負け＝読み違え＝自分のミスと納得できる。
- **敵は次の行動をテレグラフ**（移動先・攻撃予告が見える）。位置取り・通路への誘い込み・待機での釣りが戦術になる。UIは現状のスワイプ/タップのまま。
- **★位置取りの通貨（2026-07-04 ユーザー承認・web限定・golden 不変・v0.113.0 実装済み）：** 「体当たりが単調＝盤面のどこに立つかが勝敗に影響しない」FBへの対応。**新しい入力は増やさず、既存の移動・体当たりの「意味」を増やす**2機構（テンポ不変が大原則）。①**挟撃**＝味方（相棒/召喚/raid共闘者）と敵を挟んだ近接は `FLANK_MULT` 倍＝回り込む1歩に価値（`flankedByAlly`）。②**ジャスト見切り→反撃**＝テレグラフを退いて空振りさせた直後は「反撃の好機」（`COUNTER_WINDOW(2)`手・HUDピル表示）＝次の近接が `COUNTER_MULT` 倍の会心（一撃で消費・フロアを跨がない）。ボスの溜め大技を見切った時が最高の好機＝(A)テレグラフ読みの能動的な報酬。実装＝`web/main.ts meleeWithPositioning`（moveOrInteract/raidMoveOrAttack の近接に適用・engine 非改変）。**★倍率の再調整（2026-07-04 テストプレイFB「見切り反撃が強すぎ・シビアな難易度が崩れる」→ユーザー承認・v0.117.0）＝** 旧 `FLANK_MULT=1.3`／`COUNTER_MULT=1.75`（重なると 2.28×）は、狙って作れるバースト＋相棒AI改善（v0.116）で挟撃が常態化したことで終始シビアを崩していた。**`FLANK_MULT=1.15`／`COUNTER_MULT=1.2`**（重なっても 1.38×）に抑制＝手応え（気持ちよさ）は専用エフェクト（v0.115＝斬光/特大数字/シェイク）が担うので倍率は控えめでよい、という判断。数値はテストプレイ調整候補（さらに絞るなら 1.1 も可）。
  - **★戦闘の「回避」を厚くする B/C/D/E（2026-07-05 モックで体験・ユーザー承認・v0.122.0 実装済み・web限定＝engine は area ボス限定の分岐のみ・golden 8/8 不変〔monsterAI 51d3744d〕）：** 確定命中・読める盤面（4-11A）の柱を守りつつ「正しく動けば被弾を抑えて捌ける」パズル性を足す4要素。確定命中の契約は不変（どこかで受ける前提は残る＝別途「無傷で切り抜ける」設計は継続検討）。
    - **B｜脅威の可視化：** この手番あなたを殴れる敵の数を HUD（`#stBuff` の帯）に常時表示（`⚔ 殴れる敵 N`／0＝`✔ 安全`緑）。@ を予告している敵タイルに薄い赤の破線枠（`.threat-src`）。dive/raid 両対応・純表示（`draw` が intent を集計→`threatHud`→`updateStatus`）。
    - **C｜見切り反撃＝押し出し：** 会心（`counterTurns>0` の反撃）で命中したとき、敵を @ から見た直線方向へ1マス突き飛ばす（`pushEnemy`）。空き床＝移動／壁・盤外＝叩きつけ追加ダメ `PUSH_WALL_DMG(4)`／他の敵＝両者に `PUSH_COLLIDE_DMG(2)`・詰まる。押しても敵の予告した一撃はこの手番に確定解決（intent 温存）。ハザード地形（裂け目）は現迷宮に無いので排除ケースは未使用。踏み込み（E）では会心が乗らない＝押し出しも発動しない。
    - **D｜ボスの形つき確定範囲（area ボス限定）：** 既存の溜め大技（`MonsterIntent.heavy`・単点）を「形つき」へ発展＝直線3／扇（前方3方向×2）／薙ぎ（周囲8）を大技ごとに巡回（`Monster.bigShape`・`bossShapeCells`＝rng 非使用の決定論）。予告は形の全マスを橙で1手前に表示（`.tele-boss`）。resolve で @ が形マス集合に含まれれば全弾ヒット（`BOSS_HEAVY_MULT`）・含まれなければ確定空振り→見切り成立。**engine 改変は必ず `m.boss==="area"` 分岐内に閉じ込め＝通常敵の数値経路は1バイトも不変**（golden の gGenFloor/gMonsterAI は 8の倍数深度を踏まない＝area ボス不在＝指紋不変を裏取り）。中ボス/エリートは対象外。
    - **E｜踏み込み攻撃：** D-pad 横の「踏」ボタン（`#lungeBtn`・設定で表示オンオフ `LUNGE_KEY`＝既定オン）。タップ=次の一撃だけ arm（金点灯）／長押し `LUNGE_HOLD_DELAY(280)`ms=構え固定（強発光＋HUD ピル `➶ 踏み込み構え`）／キーボード Shift+方向=単発。近接で敵を撃破・押し出しで空いたマス、または敵背後の空きへ貫き移動（`lungeThrough`＝攻撃＋離脱が1手・塞がりはその場攻撃にフォールバック）。**枷＝会心が乗らず `counterTurns` を消費しない（窓は残る）・C 押し出しも発動しない**＝「貫いて離脱」と「刺し込む会心」の択。dive/raid 両対応・ephemeral（フロアを跨がない）。
    - 定数（`PUSH_WALL_DMG`/`PUSH_COLLIDE_DMG`/形の種別/`LUNGE_HOLD_DELAY`）はテストプレイ調整候補。
  - **★戦闘の最後の要素 A｜群れ増量（fodder・2026-07-05 ユーザー承認・v0.123.0 実装済み・engine `genFloor`＝golden〔genFloor〕意図的再生成 1857a403→f3486769・他7指紋不変〔monsterAI 51d3744d〕）：** 敵を「増やす」ことで盤面に散った圧を作る。設計方針＝**①全難易度で増量**（easy 含む・難易度ゲートにしない）／**②増やすのは fodder のみ**＝`tier≤2` の低HP/低火力雑魚を通常配置数×`FODDER_MUL` だけ追加＝**強敵（tier3-5）の数・個体スケールは不変**（個体の脅威曲線を保つ）／**③地形で流入制御を保証**＝既存の幅1 L字通路＋部屋間2マス岩間隔がチョーク＝**同時隣接を1〜3に絞れる逃げ場を確保**（sim 指標D で敵数42→50 でも隣接ピークは 2〜3 のまま＝実測裏取り・床の36%が幅≤1通路）／**④golden(genFloor) を意図的再生成**（設計変更＝Swift 照合の新基準・fodder は末尾で rng 消費＝既存の敵/ボス/宝箱/回復ノード配置は不変＝指紋差分は fodder ぶんのみ／monsterAI 照合は `fodderMul:0` 固定で指紋不変を死守＝AI アルゴリズムは無変更）／**⑤sim（`tools/sim-combat.ts` の指標D＝実 genFloor 踏破）で実測検証**＝1フロア踏破のHPコスト%がベースライン比 **+25%以内**・DEATH率が既存帯から逸脱しないことを深度(3/8/15/25/35/50)×難易度(easy/normal/hard)で確認。**採用値 `FODDER_MUL=0.2`**（開始 0.5→実測で 0.5/0.4/0.3/0.25 は easy D25 の生存閾値セルが+25%を超過→0.2 で全セル合格・最大 Δ+20%＝easy D25）。深度1〜2は増やさない（序盤の手触り・チュートリアル性）。総数は `MONSTER_HARDCAP=60` を厳守（深淵帯の countCap 緩和と相乗）。定数（`FODDER_MUL=0.2`/`FODDER_MIN_DEPTH=3`/`FODDER_MAX_TIER=2`）はテスト調整候補。
  - **★武器クラス〈槍〉（2026-07-06 ユーザー承認・v0.124.0 実装済み・web 中心＝engine 非改変・items golden 意図的再生成〔TEMPLATES 追加〕・他7指紋不変・SAVE_VERSION 据置）：** 剣（＝全武器の既定挙動＝8方向・隣接）に対する新武器クラス。①**攻撃範囲＝十字4方向・射程2**（上下左右の直線、距離1と距離2）。②**貫通**＝直線上の距離1・距離2の敵に同時ヒット。手前（primary）には会心・挟撃・武器 proc・siphon がフルに乗り、**2体目（secondary）は基礎ダメージのみ**。③**斜め攻撃不可**＝斜め入力で隣接に敵がいても攻撃しない（**手番非消費**でメッセージのみ・移動先が空いていれば普通に移動）。④**踏み込み（E）不可**＝槍装備中は「踏」ボタン非表示・Shift+方向も無効。⑤**攻撃力は同深度の剣より −1 目安**（低 dmg テンプレで表現）。**剣＝万能近接／槍＝間合いと地形（幅1通路のチョークで真価・開所では弱い）の二択**。`Item.reach`（基テンプレ由来＝銘/+N と独立・未設定=1）。基テンプレ＝木槍(minDepth2)/刺突槍(8)/長槍(12)/萎えの槍(15・sap proc)/十文字槍(19)/大身槍(25・rend proc＝深層)/淵穿ち(30・攻4・異物＝深蝕+・深淵の刃の槍版)＋伝説槍三柱（exclusive＝深層レアドロップ/大命褒賞のみ）＝〈布都の神槍〉(v0.125.0・d20・攻4・stun〔当て止め＝鎮めの神威〕・深蝕−0.015/手＝携えるだけで深蝕を薄める)／〈迦具土の焔槍〉(v0.126.0・d20・攻3・blast〔爆炎＝半径2の余波〕・深蝕+0.02＝破壊の代償)／〈天照の大御槍〉(v0.126.0・d20・攻5＝全武器最高の別格・cleave〔光の薙ぎ〕・reach2。※v0.131.0でバランス是正＝浄性(深蝕−0.02)を撤去〔代償ゼロの是正・欠点ゼロではないが最強キットとして残す〕)。実装＝`items.ts`（`Item.reach`／TEMPLATES／`itemPower` 表示）＋`web/main.ts spearStrike`（moveOrInteract/raidMoveOrAttack の冒頭に reach>=2 分岐・engine 非改変）＋`updateLungeBtn` の槍ガード。数値（各槍の dmg／−1 幅）はテスト調整候補。 **★移行の落とし穴修正（v0.134.0）＝** v0.124（槍 reach）／v0.127（薙刀 sweep）より前に作られたセーブの武器は当該フラグを持たず、装備しても槍/薙刀として機能しない（距離2へ突けず前進・貫通なし＝実プレイFBで判明）。`migrateWorld` が保存武器（equipment.weapon／gearBag／stashGear）の reach/sweep を `backfillWeaponClass`（items.ts・baseName→TEMPLATES 引き／無ければ itemByName 復元）で再導出＝冪等・非破壊・golden 8/8 不変。
  - **★武器クラス〈薙刀〉（2026-07-06 ユーザー承認・v0.127.0 実装済み・web 中心＝engine 非改変・items golden 意図的再生成〔TEMPLATES 追加〕・他7指紋不変・SAVE_VERSION 据置）：** 剣・槍に続く第3の武器クラス＝**弧の薙ぎ払い**。①**攻撃形状＝bump した方向とその左右隣（±45度）の計3マスを同時に薙ぐ**（例：上入力→左上・上・右上／右上入力→上・右上・右）。②**ダメージ**＝bump先（primary）には会心・挟撃・武器 proc・siphon がフルに乗り、**側方セルの敵（side・最大2体）は基礎ダメージのみ**（槍の secondary と同じ）。③**★会心の薙ぎ＝まとめて吹き飛ばす**＝見切り反撃の会心時、生き残った primary と side を全員 @ から離れる向きへ突き飛ばす（壁叩きつけ／玉突きは既存 `pushEnemy` が処理・通常時は押し出しなし＝C の会心限定原則を維持）。④**斜め攻撃可・射程1**（剣と同じ入力感覚）。⑤**踏み込み（E）可**（既存の枷どおり＝lunge 中は会心が乗らない＝吹き飛ばしも発動しない）。⑥**攻撃力は同深度の剣より −1 目安**（槍と同じ曲線）。**剣＝万能近接／槍＝幅1通路のチョーク／薙刀＝開所で囲まれた時**の三択。`Item.sweep`（基テンプレ由来＝銘/+N と独立）。基テンプレ＝薙鎌(minDepth3・攻1)/薙刀(10・攻2)/大薙刀(17・攻3)/夜叉薙(26・攻3・stun proc＝深層＝当て止めが−1を補償)。実装＝`items.ts`（`Item.sweep`／TEMPLATES／`itemPower` 表示）＋`web/main.ts naginataSweep`（moveOrInteract/raidMoveOrAttack の sweep 分岐・engine 非改変・side 撃破を proc 適用より先に確定＝spearStrike v0.126.0 の二重発火ガードと同一流儀）。数値（各薙刀の dmg／−1 幅／会心吹き飛ばしの強度）はテスト調整候補。
  - **★剣＝受け流し（パリィ）／薙刀＝会心薙ぎの体勢崩し（stagger）（2026-07-08 ユーザー承認・v0.139.0 実装済み・web 限定＝engine 非改変・golden 8/8 完全不変・SAVE_VERSION 据置）：** テストプレイFB「槍は立ち回りでダメージ無しにできるが、剣・薙刀は常に被弾して弱く感じる」への是正。**根本原因＝確定命中（miss無し）＋敵は「あなたの今いるマス」を予告する構造上、被弾0の唯一の道は“予告マスから自分が退く”こと。槍は射程2で間合いを保ったまま殴れるので一方的に無傷にできるが、剣・薙刀は隣接して殴る＝必ず反撃を受ける。** そこで剣・薙刀に**それぞれ固有の“被弾回避 idiom”**を与える（槍のカイトと対等化・ただし“常時無傷”は与えず難易度を保つ）。**①剣＝受け流し**＝剣クラス限定の守勢アクション（D-pad 横「受」ボタン／キー g・一手消費）。構えた次の敵手番、当たる**隣接の通常近接「1撃だけ」を無効化＋反撃の好機**（`counterTurns`）。**枷（難易度維持）＝**(a)無効化は1撃のみ（複数の同時攻撃は残りを喰らう）(b)**遠隔・ボスの溜め大技(heavy)には無効**（それらは退いて見切る既存ルート）(c)予告が来なければ一手の丸損（読み違いの罰）。**囲まれても地の利を捨てず捌く**＝剣が最も弱かった状況を活かす。**②薙刀＝会心の薙ぎで体勢を崩す**＝既存の「会心の薙ぎ＝吹き飛ばし」に**体勢崩し（`m.stunned=1` 流用＝planMonsters が `intent="wait"` に変換）**を付与＝吹き飛ばした敵は**次の1手番 wait**。**確定命中ゆえ“既に来ている今手番の攻撃”は消せない**が**次の安全窓**を作る＝「囲まれても一度のカウンターで群れごと制圧する」薙刀 idiom。**枷＝会心限定**（会心＝見切り〔退いて空振り〕の前提投資が要る＝ばら撒けない）・side は基礎ダメのみ＝崩しても倒せない（群れの初撃の脅威は残る）。実装＝`web/main.ts`（`guardArmed`／`weaponIsSword`／`guardTap`／`updateGuardBtn`／endTurn・raidEndTurn の被弾ループ冒頭にパリィ判定＋末尾で構えクリア／naginataSweep の会心分岐に stagger・全て ephemeral〔フロア跨ぎ/死でリセット〕）＋`index.html`（`#guardBtn`＋設定トグル `GUARD_KEY`）。検証＝`npm run check` 全緑（golden 8/8 不変）＋実ブラウザ E2E 11/11 pass（受けで隣接1撃無効化+反撃の好機・囲まれると1撃のみ・大技/遠隔は無効・会心薙ぎで被薙ぎ敵が全員 wait＝次手 追加被弾なし・非会心では stagger なし・例外0）。数値（`NAGINATA_STAGGER=1`手／受けの完全無効化・連続受け可）はテスト調整候補。**★敵配置の是正（“多くの敵がいてこそ輝く”）は別途ユーザー承認後に着手＝Sonnet 定量検討で「敵数増でなく“部屋クラスタ配置”が真のレバー」と判明（総量を変えず薙刀/パリィが輝く開所遭遇を作る）。**
  - **移行：旧セーブの武器 reach/sweep を baseName から再導出（v0.134.0・テストプレイFB「槍で距離2の敵を攻撃すると前進するだけでダメージが入らない」→調査→修正）＝v0.124.0（槍 reach:2）／v0.127.0（薙刀 sweep）より前に鍛造・保管された武器 Item は当該フィールドを持たないまま永続化されており、`migrateWorld` がそれを補完していなかった（槍/薙刀の名前・見た目のまま reach:1 の通常武器として振る舞い、距離2への突き・貫通・薙ぎ払いが一切発動しない不具合）。`items.ts` に `backfillWeaponClass(item)`（baseName、無ければ name から `itemByName` で再構成し reach/sweep を補完・冪等・武器以外は no-op）を追加し、`world.ts migrateWorld` が装備中の武器・荷物(gearBag)・自宅武具庫(stashGear) の全武器 Item に適用。SAVE_VERSION 据置（非破壊バックフィル）。**
  - **★地形ハザード（2026-07-06 ユーザー承認・v0.128.0 実装済み・web 中心＝engine 非改変・golden 8/8 完全不変・SAVE_VERSION 据置）：** 盤面に「踏むと危険なマス」を導入＝位置取りの通貨（挟撃/見切り/押し出し/薙刀の吹き飛ばし）に**「敵をハザードに叩き込む」新しい使い道**を与える（Into the Breach の方程式）。**★敵AIはハザードを避けない**（単純さ維持・プレイヤーだけが地形を武器にできる非対称）。**4種**＝①**業火床**（進入＋その上で手番を終えるたび燃焼ダメ `max(2,round(2+depth×0.25))`・プレイヤー/敵同式）／②**毒の沼**（進入で毒＝プレイヤー3手／敵は既存 `Monster.poison` 3手・毒ダメは `venomDmgAt` のキャップ尊重）／③**崩れかけの床**（乗ると軋む〔テレグラフ〕→**次の手番終了時に崩落**＝その上の者へ大ダメ `max(4,round(4+depth×0.5))`・以後は普通の床＝一回きり）／④**蝕の霧だまり**（**プレイヤーのみ**：手番終了ごと深蝕 `+0.02`＝`addExp` 経由で難易度係数が乗る・敵には無効）。**配置**＝`Floor.hazards?`（任意フィールド＝engine 非使用＝golden 安全・`aurelSite` と同じパターン）に、web の `enterFloor` 初訪が seeded 配置（`floorCache` に乗り同一潜行内で決定論）。深度4以上・約45%のフロアに 1〜2 クラスタ（3〜6 マスの連結塊・fire/venom=d4+／crumble=d8+／miasma=d25+）。階段/回復ノード/宝箱/化石/帰還扉/縦糸/スポーンとその隣接には置かず、部屋の内部（8近傍に床6+）を優先＝**通路を塞がない**。深淵帯・街防衛戦（raid）には置かない（v1 は通常 dive 限定）。**本命シナジー**＝押し出し（`pushEnemy`＝会心/薙刀/槍の吹き飛ばし共通）の着地点がハザードなら即適用＋専用ログ「○○を業火に叩き込んだ！」。実装＝`dungeon.ts Floor.hazards` 型のみ＋`web/main.ts`（`placeHazards`/`applyHazardToEnemy`/`onPlayerEnterHazard`/`resolvePlayerHazard`/`tickCrumble`/`armCrumble`・`endTurn` の移動敵判定〔位置が変わった敵だけ〕・`moveOrInteract` の進入時・`autoTravel`/`dpadStep` はハザード進入で停止）＋`index.html` CSS（床の色＝グリフは置かない）。数値（燃焼/崩落ダメ・霧の深蝕・配置率45%・種類の深度ゲート）はテスト調整候補。
  - **★投擲ハザード壺＝火/毒/凍の3種（2026-07-07 ユーザー承認・v0.133.0 実装済み・web 中心＝engine 非改変・golden 8/8 完全不変〔消耗品は指紋対象外〕・SAVE_VERSION 据置）：** 旧・火炎瓶/業火の壺は「プレイヤー周囲（半径1）の敵に即ダメージ」で投擲なのに手元でしか炸裂しなかった。これを**手動照準で離れたマスへ投げ、着弾3×3にAoE＋地形ハザードを残す**に作り替え、毒・凍の新規壺を追加。**術の業火床/凍霧（深蝕コスト）との住み分け＝深蝕ゼロの消耗品＝「清いが有限」。****照準＝盤上のクロスヘア**（地図モードは使わない）＝射程 Chebyshev≤`THROW_RANGE(4)`・視界内・壁不可（山なりで壁越し可）。方向パッド/矢印/タップでクロスヘアを動かし「投げる」で放つ／「やめる」は非消費・非手番。初期照準＝視界内の最寄りの敵。**3種**＝**火炎瓶/業火の壺（fire・dmg 8/16）**＝着弾3×3の敵に即ダメージ＋**業火床**を残す（着弾3×3にプレイヤーがいれば自分も焼ける＝足元へ投げる読み合い）／**毒の壺（venom）**＝着弾の敵に毒（`VENOM_TURNS`・`venomDmgAt`）＋**毒の沼**を残す（直接ダメージなし＝継続ダメ＋地形拒否）／**凍てつく壺（frost）**＝着弾の敵を鈍らせ（`slowed`）＋**凍霧**を残す（ダメージなし＝純制御・プレイヤーには凍霧無効）。着弾は必ず成立（敵ゼロでもハザードは残る＝地帯制圧）。地形は `layHazardField`（fire=4手／venom・frost=5手）で敷き `tickHazardTtl` で自然消滅。dive/raid 両対応（raid でも `floor.monsters` のみ着弾＝市民は巻き込まない）。実装＝`items.ts ConsumableDef.use.throw`（旧 `burst` を撤去）＋`web/main.ts`（`throwConsumable`/`resolveThrow`/`throwAimValid`/`setThrowAim`/クロスヘア描画・`dirMove`/keydown/`dpadStep`/touchend の throwActive 分岐・`bagBtn` の throw 振り分け）＋`index.html`（`#throwBar`＋`.throw-aim`/`.throw-blast` CSS）。数値（射程4・各 dmg/turns・slow量）はテスト調整候補。

  - **★魔法カタログ拡充＝新5種＋崩落の実地形化（2026-07-06 ユーザー承認・v0.130.0 実装済み・web 中心＝engine 非改変・golden：spells のみ意図的再生成〔14cdc14b→19f291e8〕・他7指紋 byte 一致・SAVE_VERSION 据置）：** ユーザー要望3軸（①攻撃後に地形が変化する術／②遠距離攻撃／③敵の状態変化）に対応。術カタログ 32→37 種。**ハザードに寿命（`turns?`）を追加**＝術で敷いた地形は毎手デクリメント→0 で消滅（自然配置＝`turns` 無し＝恒久・非破壊）。**①業火床（`firefloor`・攻・cost 0.22・minLv14）**＝最寄りに `spellBase×0.6` ＋その敵の周囲3×3を fire ハザード化（`turns:6`）＝自分も焼く読み合い。**②凍霧（`frostmist`・制・cost 0.22・minLv18）**＝最寄り中心・半径2に**新 kind "frost"** を `turns:6` で敷く＝霧上の敵を毎手 `slowed=2` 上書き（`tickFrost`）・**プレイヤーには無効**（miasma の逆の非対称）。自然配置には出さない（術専用地形）。**③崩落の実地形化（既存 `collapse`）**＝従来の AoE ダメ（`spellBase×0.7`・3×3）に加え、対象の周囲リングから決定論で最大3マスを壁（`tiles=0`）に＝瓦礫で即席チョーク（槍/薙刀と地形シナジー）。**★安全ガード＝壁化後に @ から上り/下り階段の両方へ物理フラッド（`tiles===1`）で到達可能か検査し、不能なら全て取り消す**（退路封鎖の防止）。tiles 変異は web 内で完結（`floorCache`/`DiveSnapshot` に tiles ごと乗り永続）。**④弾き（`parry`・移・cost 0.18・minLv24）**＝`parryTurns=3`（ephemeral・v0.131.0でバランス是正＝4→3手）＝持続中、遠隔の狙撃（ranged）がプレイヤーに命中する時ダメを0化し射手へ**半減**（`max(1,ceil(dmg×0.5))`・v0.131.0で全弾反射から弱体化）を撃ち返す。**近接には無効**（still_eye 等と役割分離）。engine 非改変で実装（`MonsterHit.monster` の射手参照＋射手が非隣接＝ranged と判定）。**⑤先見（`foresight`・識・cost 0.12・minLv12）**＝`genFloor(world, depth+1)` を一時生成して読み捨て（world/web の rng を汚さない＝determinism 1550 が担保）、次階の敵の多さ・ボス階か・深度帯・大物の有無を要約表示。深淵帯では「底の気配は読めない」。実装＝`spells.ts SPELLS`（+5・崩落 desc 更新）＋`web/main.ts`（castSpell 5分岐＋`tickFrost`/`tickHazardTtl`/`collapseWalls`/`layHazardField`/`spellHazardExcluded`/`foresightScene`・`parryTurns` の HUD ピル/reset・dive+raid 両対応）＋`index.html`（`.hz-frost` CSS）。数値（各コスト/minLevel・fire×0.6・frost 半径2・崩落の最大3マス・parry 3手〔v0.131.0是正〕/寿命6手）はテスト調整候補。

**(B) 魔法・スキル＝深蝕を支払い、確定した盤面を「曲げる」**
- この世界の魔法＝**深層の力を引く行為**。ゆえに燃料は MP ではなく**深蝕（人間性）**。スキルも同一資源（別資源は作らない）。
- 例：**歪撃**（確定大ダメージ）／**影渡り**（位置交換）／**静止の眼**（敵停止＝テレグラフ無効化）／**理崩し**（壁を抜く）／**反転**（敵の一手の向きを返す）。
- 深蝕は既に〔奇癖 → 死後の化石の変質強度〕に直結している（4-10C）。**戦い方がそのまま「自分がどんな化石になるか」を決める**——強さ・物語・末路が一手に同居する、本作最大の戦闘的差別化。

**(C) ステータスとレベル（職業は廃止）**
- **職業選択は廃止。** 全員が攻撃・魔法・スキルを使え、**ビルド（選択成長）と系譜（4-10D）が「職業」の代わりに人格を作る**。
- ステは4種：**体**（HP）／**力**（近接火力）／**理**（術の威力）／**心**（深蝕耐性＝奇癖・変質の付きにくさ）。
- レベルアップ＝「ステ上昇 or 新しい呪文/技の習得」の**選択式成長**。心を伸ばせば深くまで人間のまま潜れる、等のビルド分岐。
- 系譜：先代の形質・覚えた術の一部が初期値に滲む（成長がモデルCへ還流）。

**(D) 敵もまた誰か（元・人の敵のみ鎮め筋）**
- 獣・異形＝倒すのみ。**元・探索者系の敵（敵性化した化石・怨霊）のみ**、弱らせれば**鎮める（非殺の決着）**が選べる。
- 殺す／鎮めるの選択が年代記・噂に残る＝**戦闘版の干渉動詞**。希少だから重い。

**(E) 装備（ハック＆スラッシュの核）**
- 最小スロット：**武器／防具／遺物**。ドロップは深度スケール。
- **異物（4-10E）＝用途不明・副作用つきの装備**として実装（鑑定・実験の楽しみ）。
- **死亡時に装備していた武器が化石の origin に刻まれる**——「錆びた長剣を握った亡霊」の剣が、文字通りプレイヤーの選んだ剣になる。**継承＝先代の武器の奪還**。装備がパリンプセストの痕跡素材になる。

**(F) 実装順（PR分割の指針）**
①テレグラフ＋決定論戦闘 → ②ステ4種＋レベル/経験値＋職業廃止 → ③深蝕魔法・スキル → ④装備＋ドロップ＋異物 → ⑤鎮め筋

**難易度軸＝終始シビア（2026-06-17 ユーザー承認・確定）：** 各レベル・各深度が試練（古典ローグライク寄り）。設計アンカー＝**Lv ≈ 深度**（深度Dを攻略する頃はおおよそ Lv D／敵は常に Lv≈D の探索者を脅かす強さ）。死は現実的だが世界は堆積し続ける（世代交代）。脅威の主源は**包囲**（雑魚が同時2-3体隣接しうる＝被ダメ手数が実質1/2〜1/3）。XP曲線・敵スケール・壁ビルド抑制・術の深蝕コストは、すべてこの軸（常に背伸び・facetank を許さない）に従って数値化する。

**②の実装方針（2026-06-13 ユーザー承認・確定）：**
- **ステ4種＝体/力/理/心、初期値2**（progression.ts）。**体→最大HP**／**力→近接ダメージ**（=力+1、力2で3）／**心→深蝕の染み込みを低減**（係数=max(0.3, 1−(心−2)×0.12)。毎ターンの passive 深蝕にのみ係る。イベント由来の深蝕には係らない）／**理→③（深蝕魔法）の威力＋構え数の素養**。
- **壁ビルド抑制（終始シビア・2026-06-17 ユーザー承認）：** 体→最大HP は**逓減**＝`maxHp = 6 + 3×min(体,16) + 1×max(0, 体−16)`（体2で12＝従来値・均整ビルドは不変。体14=48）。体16を超えると伸びが鈍り、極端な体全振りのみ約半減（体51: 159→89）＝「硬いが不死身ではない」。facetank を許さず、脅威源を包囲に残す。
- **成長＝撃破XPでレベルアップ**（敵の堅さぶん xp）。**次レベル必要量＝`round(8 + 4L + 0.8L²)`**（案B・2026-06-17 ユーザー承認。Lv50到達総XP≈37.6k。旧 `6+lv×6`＝Lv50総≈7,644 は浅すぎたため steepen。レベルが深度を追い越しにくく常に背伸び＝終始シビア）。**昇級でステ1つを選んで+1**（術習得は二者択一ではなく無制限＝③参照）。
- **職業選択（流儀）はUIから撤去**（Character.archetype は "wanderer" 固定で据え置き＝セーブ互換）。**セーブ版数 version=2 を導入**しマイグレーションで旧セーブにステ/レベルを補完（横断D）。
- キャラシートは ≡ メニューに最小実装（横断C）。CLI は抽象戦闘のため XP は潜行/被弾で代替付与。

**難易度モード（4-11H・v0.56.0・2026-06-27 ユーザー承認・テストプレイFB「序盤で死なない」是正）：** 終始シビアの単一曲線に**薄い乗数レイヤー**を被せる（別コード経路なし＝決定論・golden・Swift 移植に優しい）。定義の正＝`src/difficulty.ts`（`DifficultyMods`＝enemyHp/enemyDmg/**dmgFloor（序盤の火力床＝深度係数が小さい序盤から噛む）**/exposure/xp/townHeal/lineage）。**新規ワールド開始時に一度だけ選び固定**（途中変更なし）。**easy＝×1.0＝従来数値＝golden 不変**（旧セーブは easy に grandfather）。選択肢＝easy/normal/hard（**death は枠のみ＝後日調整・UI 非表示**）。波及＝`scaleKind(k,depth,mods)`＋`genFloor` が `floor.diff` に焼き込み→雑魚/エリート/ボス/追手/眷属/街防衛の全スポーン・XP・潜行入口の回復（hard で<満タン＝消耗）・深蝕・系譜ボーナス（death 用に無効化可）。数値はテストプレイ調整候補。
**★オーバーレベル対策＝XPの深度係留＋比例チップ（v0.121.0・2026-07-05 テストプレイFB「深度12で敵を1〜2撃・被ダメ1〜2＝飽きる〔normal・Lv25・greed使用〕」→3体の解析エージェントで定量診断→ユーザー承認）：** 実測診断＝症状の正体は深度でなく**「プレイヤー成長（Lv・装備）が深度を追い越すドリフト」**。①XP経済は全踏破だけで Lv=深度+6〜+8（greedで+9〜+12）に膨らみ、「Lv≈深度」は50%間引き＋greed無しの細い条件でしか成立していなかった（再潜行XP farm も未封鎖）。②Lv≈2×深度＋良装備では**深度4〜24の全域で TTK2・被ダメ2%** に平坦化（＝8階まで楽しいのはLvが未追い越しだっただけ）。③敵攻の伸び（+0.18/深度）は平坦で、防具（軽減6〜8）が丸ごと打ち消し下限 `max(1,…)` で被ダメ1固定＝防具飽和。**是正＝`DifficultyMods` に新3係数**（易度ごと・easy は全て無効）：**(a) `xpTether`**（normal0.35/hard0.4）＝撃破XP倍率 `1/(1+xpTether×max(0,Lv−深度−2))`＝格下狩り逓減。深度相応で戦う限り不変・全踏破/再潜行farmでも **Lvが深度+4前後に自然収束**（実測）・過剰Lvセーブも「浅場では育たない→深く潜ると手応えが戻る」自己修復（Lv25@d12→13フロアでギャップ半減を実測）。**(b) `greedMul`**（easy1.5/normal+1.2）＝greed遺物の恒常+3Lvを是正。**(c) `chipFrac`**（normal0.15/hard0.18）＝被ダメ下限を `max(1, ceil(素の攻×chipFrac), 攻−軽減−バフ)` に＝防具飽和の「被ダメ1固定」退化を封じる（浅層の素の攻では従来どおり1＝序盤不変）。**★easy の公式な位置づけ（ユーザー確定 2026-07-05）＝「丁寧に進めると気持ちよく無双できる快適モード」**＝オーバーレベルを容認する仕様（新3係数すべて無効＝従来挙動を完全維持＝golden 不変）。実装＝web の授与一本化ヘルパ `killXpFor`（rewardKill/ボス鎮め半量/raidKill の3経路・sonnet監査で漏れゼロ確認）＋被ダメ2経路（dive/raid）。CLI/demo 据え置き。数値（0.35/0.4・1.2・0.15/0.18・猶予+2）はテストプレイ調整候補。

**③の実装（深蝕魔法・2026-06-13 ユーザー承認＝基盤／2026-06-17 ロードアウト制へ大改訂・ユーザー承認）：** 燃料＝深蝕。**術の定義（名称・種類・効果・コスト）の正は `proto/src/spells.ts`**（`SPELLS` 配列＝データ駆動。追加もここ）。**詠唱UX＝「術」ボタン→構え一覧→自動対象で発動**（手動ターゲットなし）。詠唱＝そのターンの行動（`endTurn()` に合流）＋**専用SE＋エリア点滅エフェクト**。③はWeb専用（CLIはステ成長のみ・`spells`空）。

**ロードアウト制（2026-06-17 ユーザー承認・確定）：** キャラビルドの自由度＝個人ごとに別物の戦い方を組ませる芯。
- **取得＝無制限**（`Character.spells`＝習得図鑑。深淵boon・教団・発見等から自由習得。**旧「レベルアップでステ+1と二者択一」を撤廃**＝昇級は常にステ+1、術習得は成長枠を食わない）。
- **構え＝`Character.loadout`（10スロット固定）**。戦闘で撃てるのは構えている術だけ。**入替は街・篝火など安全地帯のみ**（戦闘中は固定＝構成へのコミットが戦術になる）。
- 10構え／40プールなので全部は積めず、攻め重視・妨害術師・機動暗殺・召喚使い・探索特化…と分岐する。

**深蝕コスト階梯（終始シビアの肝＝強い術ほど化石化が進む）：** 識 0.05–0.1／制・移 0.15–0.2／攻 0.15–0.3／召・回復 0.3–0.4／断罪・死戸 0.5+。

**術プール＝40種カタログ（設計の正・効果は DCSS/NetHack/ToME/Diablo から翻案・名称は漢字テイスト）：** ★＝既存実装済み。

| 系統 | 術（効果） |
|---|---|
| 攻(10) | ★歪撃(最寄り確定・威力＝理×2+2)／裂界(直線貫通)／崩落(着弾範囲)／氷棺(単体高+短凍結)／雷霆(放射状連鎖)／腐喰(継続深蝕=毒)／凍霧(範囲に霧を残す・地形継続)／業火床(足下に火を設置)／痩身(現在HP割合削り)／断罪(高深蝕・単体即死級・上位) |
| 制(8) | ★静止の眼(可視2手停止)／鈍り(数手スロウ)／惑乱(混乱)／畏れ(範囲恐慌で遠ざけ)／微睡(単体睡眠・被弾解除)／眩耀(範囲盲目=命中↓)／縛鎖(単体を根付かせ移動不可)／不和(範囲を同士討ち) |
| 移(6) | ★影渡り(最遠視界へ転移=逃)／霞足(短距離・制御ブリンク)／迫り(直線の敵へ突進し一撃)／万象斬(視界全敵へ順次転移斬撃)／引閘(範囲の敵を一点へ引寄せ)／退き戸(自分と一点を繋ぐ門=退避) |
| 援(8) | 癒し(HP回復=理+体)／蝕み(対象の攻撃↓)／吸命(与ダメの一部をHP変換)／硬鱗(数手防御↑)／疾走(数手加速)／弾き(飛来物を逸らす)／死戸(数手無敵・回復不可・反動)／焦躁(一時攻撃↑だが深蝕増) |
| 召(4) | 残響召喚(過去の自分/亡霊を一時召喚・既存 `echo_summon` と接続)／蝕兵(倒した敵から眷属)／廻刃(自律で撃つ刃を侍らせる)／影分け(影武者で攻撃分散) |
| 識(4) | 看破(敵HP・種別・テレグラフ可視化)／地相(フロア地図と階段感知)／嗅ぎ(化石・財宝・遺物の在処)／解呪(死亡刻印/異物の呪いを一時抑制) |

> **実装段取り：** Phase A＝ロードアウト制（`loadout` 型＋構え入替UI＋撃てるのは構え分のみ）＋全系統から代表12–16種 → 以降 40 まで充填（横断A）。数値・効果はバランス調整で動く（実装の正は `spells.ts`）。新術はここに行を足し、`SPELLS` に定義＋盤面効果（`web/main.ts castSpell`）を足す。
>
> **✅ Phase A 実装済み（Web・2026-06-17）：** `Character.loadout`（構え・上限 `LOADOUT_CAP=10`）／`activeLoadout`・`learnSpell`（習得＝図鑑へ追加し空き構えに自動装填）／「術」ボタンは構えからのみ詠唱／**構え入替＝≡メニュー「術を構える」＝街・安全地帯のみ**（`mode!=="dive"` でゲート）。**レベルアップは常にステ+1＋別枠で術を1つ任意習得（二者択一を撤廃）。** **実装済み 35/40種（横断Aで充填中）**＝攻8（歪撃/裂界/崩落/雷霆/氷棺/痩身/断罪/腐喰）・制6（静止の眼/鈍り/畏れ/惑乱/微睡/縛鎖）・移6（影渡り/迫り/万象斬/引閘/霞足/退き戸）・援8（癒し/蝕み/吸命/硬鱗/疾走/焦躁/死戸/解呪）・識3（地相/看破/嗅ぎ）・召4（蝕兵/廻刃/残響召喚/影分け）。状態異常は `Monster.slowed/fear/confused/rooted/weak`（鈍り＝1手おき／畏れ＝逃走／惑乱＝ランダム移動／縛鎖＝移動不可・隣接攻撃可／蝕み＝攻撃−`WEAK_AMT`、`planMonsters` で消費）。**プレイヤーバフ計時**＝`armorBuffTurns/attackBuffTurns/hasteTurns/deathDoorTurns`（`main.ts` モジュール変数・`endTurn` で毎手減算・フロアを跨がない＝ephemeral／硬鱗＝被ダメ−`ARMOR_BUFF`・焦躁＝近接+`ATTACK_BUFF`・疾走＝敵手番スキップ・死戸＝無敵+回復不可+明けに深蝕揺り戻し）。**召喚＝一時味方**（`SummonEntity[]` ＝ `main.ts` モジュール ephemeral・`resolveSummons` で隣接敵を毎手討つ/最寄りへ寄る・寿命で霧散・菫色 `g-summon`／蝕兵=眷属2体・廻刃=@追従の刃・残響召喚=強めの一体／**echo_summon(4-10I) とは別物＝術側は一時味方に割り切り、世界イベント側はM3で別途**・2026-06-17 ユーザー承認）。影分け＝`shadowGuard`（敵の一撃を肩代わり）。継続ダメ＝`Monster.poison/poisonDmg`（腐喰・`endTurn` で毎手 tick・死亡は `downOrKill`）。解呪＝`cleanseTurns`（装備由来の毎手深蝕を抑える）。退き戸＝上り階段の傍へ退避・霞足＝半径3の短ブリンク。**術UI補助＝ステータスバーに `stBuff`**（潜行中のみ・硬鱗/焦躁/疾走/死戸/影/解呪の残量＋召喚数を `《…》` で一目に・`updateStatus`）。セーブは `loadout` 任意フィールド＝非破壊バックフィル（`migrateWorld`：未習得/超過を除去）。

> **★術カタログの整理（v0.129.0・2026-07-06・監査→ユーザー指示「似た術を整理して不要なものは削除」・工程PR3・web＋spells.ts＋world.ts・golden は spells 指紋のみ意図的変化〔0e91b2dc→14cdc14b〕・他7指紋 byte 一致・SAVE_VERSION 据置）：** 冗長・重複の4術を**削除して 36→32 種**＝**万象斬（omni_strike・移）**＝雷霆と重複（可視全敵ダメージ）かつ移動しないのに移学派／**微睡（slumber・制）**＝静止の眼の下位互換／**霞足（miststep・移）**＝影渡りの下位互換（半径3限定なのに解禁が後）／**廻刃（orbblade・召）**＝echo の弱い版（召喚は蝕兵→上位1本の2段で十分）。**改名＝残響召喚→「幻刃」**（表示名/desc のみ・キー `echo` 不変＝セーブ移行不要。世界機構「残響の遺灰」〔world.echoes/deployEcho〕との同名衝突＝プレイヤーが「似た術が2つ」と誤認する主因を解消）。**再調整＝** haste コスト 0.25→0.40（フロア全敵停止に対し割安すぎ）／condemn 0.50→0.40（wither との競合是正＝終盤の主砲）／wither にダメ上限 `min(round(現HP×0.4), round(spellBase×2.5))`（ボス溶かし抑制・中型は従来どおり）／enfeeble を school 援→制（実態は制御術）／corrode の毎手毒ダメに上限 `PLAYER_POISON_CAP=8`（理特化ビルドの無限スケール予防）／desc の実装乖離是正（影分け＝回数で消える・看破＝敵位置も地図に印す・嗅ぎ＝下り階段も嗅ぐ）。**★セーブ移行の強化（最重要）＝** 削除キーが習得済みセーブ・化石・`Lineage.chosenSpells` に残ると構え枠（LOADOUT_CAP=10）を黙って食い潰し継承で蘇るため、`migrateWorld` と `applyLineage`（血縁/heir/pupil の3経路）で `spellByKey` 存在チェックによりフィルタ（world.ts が spells.ts を import＝循環なし）。移行テスト（死にキー混入セーブ→除去・化石継承→learn されない・正常キー保持）で裏取り。数値（haste/condemn コスト・wither/corrode 上限）はテスト調整候補。

**術習得のペーシング（プレイFB 2026-06-17）：** レベルアップでの習得は**毎レベル→`LEARN_EVERY=4`レベルに1度**に変更（毎レベル習得は難易度を下げ、深淵/教団の意味も薄れるため）。さらに `SpellDef.minLevel`（基本1→序3-6→中9-13→高16-22→極26）で**高効果の術は高レベルでのみレベルアップ選択に出る**（断罪/死戸=26・残響召喚=22・疾走/影分け=20 等）。**深淵boon/教団は minLevel 不問**＝早く欲しければ対価を払う別ルート（取得は無制限のまま）。**残り5種の後続：** 業火床・凍霧（持続効果タイル＝地形ハザードの器）・弾き（遠隔逸らし）は **v0.130.0 で実装済み**（上記 4-11(B) 拡充バッチ・地形ハザードに `turns` 寿命を追加した器の上で実現）。**残る2種は確定命中モデル/敵AIの再設計が要るため後続：** 眩耀（命中↓＝確定命中モデルと要再設計）・不和（敵同士の同士討ち＝敵AIの再標的）。
>
> **将来タスク（記録のみ・未着手）：** **深蝕コストを下げる手段のバリエーション拡張**（鎮静薬以外の緩和ルート＝術乱発のトレードオフが現状ほぼ一方向なので、緩和手段を増やすことが終始シビアの調整弁になる）。

**敵の強さの可視化と段階（2026-06-13 ユーザー承認・実装済み Web）：** 「ほとんど一撃で倒せて緊張感がない」への対応。**記号＝種類／色＝強さの段（tier 1〜5：灰白→黄→橙→赤→紫発光）**で危険度を一目に（NetHack式・実装の正は `dungeon.ts` `MONSTER_KINDS`）。上位種を追加（鎧蟲＝硬い壁／怨霊／石鬼ほか）し、深いほど強い種が混じる。攻撃予告中は tier色のまま強く明滅。撃破XPは敵の堅さ比例なので強敵ほど旨い。**バランス（威力・コスト・敵密度・レベル速度）は最終通し調整（横断E/G）。**

**④ 装備（v1 実装済み・Web。2026-06-13 ユーザー承認）：** 3スロット **武器（攻撃+）／防具（被ダメ−・下限1）／遺物（パッシブ：深蝕レート減 calm／理+1 reason／撃破XP増 greed）**。実装の正は `items.ts`（`TEMPLATES`／`rollItem`）。**入手＝宝箱（深度スケール・15%罠）＋ボスドロップ（エリア確定/エリート70%）**。拾得時に装備プロンプト（現装備と比較）。**異物＝未鑑定**で落ち、装備すると判明（一部に「毎ターン深蝕+」の副作用）。**死亡時に装備中の武器名を化石 `origin.gearTags` に刻む**（4-11E「○○を握った亡霊」）。**再会の「遺されたもの（継承）」で、その武器を実際に奪還して装備できる**（`itemByName` でテンプレから復元。既定装備名など対象外は従来どおり形質を継ぐ）。再発見テキストにも武器名が出る（`#origin_gear#`）＝検証で死亡→次世代再会→武器奪還の連鎖を確認。派生値は `progression.ts`（meleeDmg に武器加算・armorReduce・effectiveReason・xpMul・equipExposure・heartFactor に calm）。セーブ version=4。≡ にスロット表示。**鑑定ショップ（プレイFB 2026-06-17）：** 拾った未鑑定品を**装備せずに**鑑定できる場が無かった件に対応＝**奇物堂〈古物商クオ・`oddments`〉act0「未鑑定品を鑑定する」**を結線（`appraiseShop`＝`gearBag` の未鑑定を料金 `APPRAISE_MUL=0.4×鑑定後価値` で正体開示）。装備での判明（賭け）と、料金を払う安全鑑定の二択になった。次段：遺物バリエ増。
- **✅ 戦闘中の装備持ち替え＝一手コストで解禁（v0.135.0・2026-07-08・ユーザー承認・web限定・engine 非改変・golden 8/8 不変）：** 旧仕様は視界に敵がいると武器/防具/遺物の換装を**一切不可**（`canSwapNow()`）＝武器クラス（剣/槍/薙刀）や防具/武器 proc が多彩になった後も「状況に合わせて持ち替える」戦術が使えなかった。**非戦闘（視界に敵なし）は従来どおり無償で自由。戦闘中（dive＝視界内に生存敵／raid＝街防衛戦は常時戦場とみなす）は武器・防具・遺物の持ち替えを解禁するが、確認のうえ一手消費**（持ち替え確定→メニューを閉じて `turnPass()`＝dive は `endTurn`／raid は `raidEndTurn`＝敵が1手動く。消耗品使用と同じモデル）。**鞄（`bag` スロット＝容量拡張の装備）は戦闘無関係ゆえ戦闘中も無償のまま**。**術の構え（`manageLoadout`）は据え置き＝安全地帯のみ**（今回のスコープ外・変更なし）。実装＝`web/main.ts`（`inCombatNow`＝dive視界判定∨raid常時／`isCombatGearSlot`＝weapon/armor/relicのみ対象／`swapSlot`・`manageBagGear` が `Promise<boolean>`（一手消費）を返すよう変更・`gearSheet`/`charScreen` が伝播してループを抜け `turnPass()`）。数値（確認UIの文言・一手コストの是非）はテスト調整候補。
- **✅ ルートシステム＝銘×基×+N（H&S 収集拡充・実装 2026-06-21・v0.13.0・ユーザー承認／リサーチ：NetHack 強化/識別・Diablo2 接辞・Wizardry 呪い・Brogue runic）：** 固定テンプレの頭打ち（深度16で武器打ち止め）を解消＝**3要素モデル**。①**基(base)** 増量（武器9/防具7）。②**銘(affix・1枠)** ＝`items.ts AFFIXES`（恵/蝕/浄の3クラス・スロット非依存で対称）。**蝕**＝禍々しい銘＝威力大だが `exposureAdd>0`（深蝕を帯びる別枠の危険クラス・武器も防具も・低頻度・未鑑定の賭け）／**浄**＝聖的な銘＝`exposureAdd<0` で深蝕を軽減（深蝕を軽減する武器／効果高く深蝕を帯びる防具、の両方向）／**恵**＝純正・フレーバー豊富（切れ味の良い/戦士の/騎士の/神々の/閃光の…）。③**強化(+N)** ＝深層ほど高い+Nがドロップ＋**武具屋「打ち直し」**（`smithReforge`・gold sink・`REFORGE_MAX=9`）。**値は `fromTemplate` で最終 dmg/reduce/exposurePerTurn に焼く**＝`progression.ts`/`itemValue` 無改修で性能・価格に自動波及（二重計算しない）。**未鑑定は銘・+N・性能を伏せる**（`itemLabel`/`itemPower` でマスク）。**継承/化石の往復**＝`fossil.origin.gearTags` に合成名（例「切れ味の良い長剣+2」）を刻み、`itemByName` が末尾+N→既知の銘を長い順に前方一致で剥がし基へ照合して復元。**安全網＝`src/item-check.ts`（全 基×銘×+N=1981通りの合成名↔分解の往復一致を機械保証・`npm run check` 同梱）＝曖昧ゼロ・旧セーブ後方互換（無銘の基名/+N名も復元）**。数値（接辞効果量・蝕/浄の `exposureAdd`・+N 曲線・打ち直しコスト・出現率）はテストプレイ調整候補。
- **✅ 基抽選の深度加重＝深いほど上位基（H&S 醍醐味・実装 2026-07-01・v0.92.0・ユーザー承認）：** ルートシステムの銘/+N は元から深度スケールしていた（付与率・等級・+N 曲線）が、**基(base) の選択は解禁済み候補からの均等抽選**で、深度20の宝箱でも最弱の短刀が最上位基と同確率で出る＝「深く潜れば良い物」が基のレベルで効いていなかった。是正＝`items.ts pickBaseByDepth`＝`weight = 1/(1 + max(0, depth−minDepth)×BASE_DEPTH_FALLOFF)`（`BASE_DEPTH_FALLOFF=0.2`）で **minDepth が現在深度に近い基ほど重く引く**（浅い基もゼロにはせず変異は残す＝ローグライクらしさ維持）。**宝箱/討伐ドロップ（`rollItem` 非ボス）と店頭（`rollItemOfSlot`）の両方に適用**（深い街ほど上位基が並ぶ）。ボスの top-3 バイアスは従来どおり（さらに上に偏る）。乱数は `next()` 1回＝均等版と同数。**実測（各4000回）**＝深度5は短刀〜6%（穏やか）／深度20で最弱基（手斧/胸当て）が〜1.1%に低下し上位は全身鎧/双刃/新tier遺物／深度40は浅い基を最下位に抑制（深淵帯は寄せが緩むが銘/+N が支配的で実害なし）。**items golden 再生成**（c8718049→47d18c3e・**他7指紋は完全一致＝装備抽選のみの変更で波及なしを裏取り**）。`BASE_DEPTH_FALLOFF` はテストプレイ調整候補（0=均等・大きいほど尖る）。
- **✅ 発動効果つき防具（防具 proc・実装 2026-07-01・v0.93.0・ユーザー承認）：** 武器 proc（命中時・cleave/stun/rend/sap）に対し、**防具に「被弾時」発動の proc** を導入＝防具が「reduce の大小」だけだった機械的多様性を補う（武器14種と対称に防具 9→14）。既存の状態異常/反射機構を流用＝新規エンジン機構ゼロ・**完全 web 限定**（`ArmorProc` 型＝`types.ts`／新5基＝`items.ts`／適用＝`web/main.ts` の `armorBlock`＋`applyArmorProc`）。5種＝**block**（受け＝被弾ごと `ARMOR_BLOCK_CHANCE=0.3` でその一撃を `×0.5` 軽減＝被ダメ確定前）／**barbs**（棘＝近接被弾の `ARMOR_BARBS_FRAC=0.4` を反射＝遺物 thorns と別枠・重複可）／**cleanse**（清め＝被弾ごとに `cleanseExposure(0.06)`＝深蝕を薄める＝深蝕テーマの防御・烙印/永続汚染より下へは祓えない既存ルール尊重）／**stagger**（怯み＝`0.35` で殴った敵を鈍化＝`Monster.slowed`）／**daunt**（威圧＝`0.25` で殴った敵を恐慌＝`Monster.fear`）。proc は基テンプレ由来＝銘/+N と独立（往復復元は `item-check` 21360 pass で機械保証）。`itemPower` の防具表示にも proc を併記（武器と対称）。**items golden 再生成**（47d18c3e→f1e0de5d・**他7指紋一致＝防具プールだけの変更で波及なし**）。数値（各 proc 係数・新基 minDepth）はテスト調整候補。
- **✅ 遺物・防具拡充（v0.13.1）：** **遺物効果 `RelicKind` を3→8**（既存 calm/reason/greed＋**might**=近接+1／**vigor**=最大HP+6／**ward**=被ダメ−1／**fortune**=拾う金貨×1.5／**mending**=潜行中ゆっくり回復）。might/vigor/ward は `progression.ts`（maxHp/meleeDmg/armorReduce・web/CLI両対応）、fortune は `rollKillLoot`・mending は `endTurn`（web）。**遺物の基を4→13**（kind×深度で分散・一部は強いが蝕む異物＝餓狼の爪/強欲の眼）。**防具の基を7→9**（武器と同数＝鋲革鎧 d4／全身鎧 d20 reduce4＝深層最上位）。**`itemPower` を `RELIC_DESC` 全網羅マップ化**（新 kind の誤表示を予防）。**`fromTemplate` をスロット限定化**（dmgAdd=武器/reduceAdd=防具/capacityAdd=鞄のみ焼く＝死に値を解消・王の/神々の が各スロットで正しい方だけ乗る）。後続＝鑑定の巻物・消耗品拡充・接尾(suffix)2枠化・セット/固有。
- **✅ 4-11G モンスター特殊能力（Phase 1 実装 2026-06-23・v0.29.0・ユーザー承認・web限定）：** 旧来の敵は全種「近づいて殴るだけ」で違いは hp/dmg/tier/erratic だけ＝戦術が一様だった。**テレグラフ準拠・決定論のまま挙動を多様化する枠組み**を導入＝`MonsterKind.ability`（任意＝未指定は素の近接で後方互換）。Phase 1 の4能力＝**`ranged`**（射程`RANGED_MAX=4`から狙撃・接近されたら間合いを取り直す＝接近が弱点。FOV7>射程4で必ず視界内＝@赤明滅で読める。保留だった術「弾き」の前提が揃う）／**`venom`**（命中でプレイヤーに毒＝`poisonTurns`/`venomDmgAt(depth)=max(1,round(d×0.08))`・毎手継続ダメ・死戸中は無効・web の `endTurn` で tick）／**`leech`**（命中ぶん自己回復＝しぶとい・engine内完結）／**`breeder`**（`BREED_CHANCE0.16`＋`BREED_CD6`＋総数`MONSTER_HARDCAP60`でたまに隣へ弱い眷属＝雑魚の35%HP/50%dmg＝数で圧すが個は脆い）。実装の正＝`dungeon.ts`（`planMonsters`/`resolveMonsters`＝CLI/demo は両関数を呼ばない＝**完全に web 限定**で CLI 無影響）＋`web/main.ts`（毒の配管・HUD「毒N」・敵図鑑に能力タグ）。**方針＝素の dmg は据え置き、テクスチャを足す**（監査で整えた数値曲線を崩さない）。seed 種4（吐酸蟲7/毒牙蛇6/吸血蛭11/孵化巣14）で枠を実証。**Phase 2＝この枠で `ability×tier×深度帯×テーマ` を量産**（鋳造所と同じ土台→量産）。後続能力＝鈍化/召喚/弾き返し等。

**迷宮のサイズ拡張＆階の永続化（プレイFB 2026-06-17）：** 「迷宮が手狭」FB＝`genFloor` の寸法を **`36+min(depth,50) × 42+min(depth,50)`**（旧 24+/28+・最大50×54）に拡張。**深度50で頭打ち＝最大 86×92（≒7,912セル・旧最大の約2.9倍／浅層の約5倍）**、以降は同サイズ。「深いほど広い」は維持。**拡張に合わせ密度も増量**＝部屋数 `W*H/72`・**敵数 `W*H/120`・上限 20→42**・**宝箱 `max(2,W*H/1300)+min(depth>>4,2)+…`（d1≈2→d50≈8）**・**化石遭遇 `2+min(2,W*H/3200)`（d1≈2→d50≈4）** が面積比で自動追従（広いほど探索量・滞在ターン＝深蝕の蓄積が増え手応えになる）。「上り下りで宝箱が復活」FB＝`web/main.ts floorCache`（潜行内の階を depth で保持＝再訪で再生成せず宝箱/化石/討った敵の状態を残す。`startDive` でクリア＝潜行ごとリセット。深淵帯は毎回新規＝試練）。

**QAチェック対応（2026-06-17 第2弾・上記拡張に伴う是正）：**
- **A 再潜行farm根絶：** 同一世代で生還→再潜行すると `genFloor` のseedが同じ＝同一ダンジョン＋宝箱復活で無限farmできた（世代加算は死亡時のみ）。→ `World.diveCount`（`startDive` ごと+1）を seed に混ぜ、**潜行ごとに別ダンジョン**に（floorCache は潜行内のみ保持なので両立）。
- **B 撃破XP係数：** 迷宮拡張＋敵上限42で総XPが約2.6倍→Lvが深度を追い越し気味だったため、`xpForKill` に **`XP_KILL_MUL=0.55`**（敵は増やすがXPは抑える＝Lv≈深度を維持・終始シビア）。
- **D 系譜の術継承（4-11F② の実装）：** 新キャラが毎回術ゼロ開始で、4Lv間隔習得と相まって35種が世代を跨いで活きにくかった。→ 死亡時に `Fossil.spells` へ術を記録し、`createCharacter` で初期習得（構えにも自動装填）。**※継承量は v0.55.0 の二択リワークで改訂＝血縁は2つを自分で選ぶ／弟子は4つ自動。詳細は 4-10D(D) を正とする。**
- **E 召喚は疾走中も動く：** `resolveSummons` を `!hasted` ブロック外へ（味方なので疾走の手番でも稼働）。
- **F（非対応＝設計通り）：** 地図モードは地形オーバービューで敵も描画しない仕様＝召喚が出ないのは一貫（修正不要）。

**中ボス／エリアボス（v1 実装済み・Web）：** 緊張感とゲーム性の主役。**エリアボス＝深度8ごとに下り階段の傍を守る山場**（`glyph Ω`・紫紅の脈動・HP=20+深度×2・tier5・最初から覚醒）。可能なら**過去の探索者化石の名を冠する**（「○○の成れの果て」＝敵性化＝後の ⑤鎮め筋への布石）。**中ボス（エリート）＝深度5+で30%、奥の部屋に出る増強個体**（`手負いの○○`・金の輝き・base×2.4HP+α）。撃破は**大XP＋年代記(legend)に記録**＋特別演出。実装の正は `dungeon.ts`（`makeAreaBoss`/`boss` フラグ／エリアボスは出自 `Monster.fossilId` を持つ）。
  - **★物語化（4-6・v0.43.0・2026-06-25 ユーザー承認・web限定）：** ①**出自の選定を「縁ある相手」へ加重**（`dungeon.ts pickBossSource`＝絆+5/未完因縁+5/doom・fall 弧の終端+6/元相棒+4・基礎重み1で見知らぬ他人も残す）＝「知っていた者が深みに堕ちて怪物となり還る」情緒を発火（純粋・決定論・golden はボス階＝8の倍数を避けるため不変）。②**決着シーンを setpiece 級に**（`web/main.ts handleBossResolve`＝化石の出自/口癖/縁/弧を織り込む台詞）＋**縁ある相手だけの第三選択「名を呼ぶ（呼び戻し）」**＝`intervene memorial` で安らかに送り深蝕−0.5＋記憶・doom/fall 終端なら弧が閉じる旨を年代記/ログに。討つ＝従来（XP満額＋印①`abyss_boss`）／鎮める＝requiem（非殺・半XP）／名を呼ぶ＝memorial（最も人間的・半XP）。印①は討伐限定を維持。
  - **★戦術化（4-11G Phase 3・B・v0.44.0・2026-06-25 ユーザー承認・web限定）：** エリアボス（`m.boss==="area"`）を「大きな近接」から戦術の主役へ。**全て area ボス限定ゲート＝通常敵・golden 不変**（golden の monsterAI はボス階を避ける）。①**溜め大技**＝隣接時に「渾身の一撃」を予告（テレグラフ・`MonsterIntent.heavy`）→**退けば空振り・受ければ `BOSS_HEAVY_MULT(2)` 倍**。`bigCd`＝間隔（通常4／覚醒2・接敵猶予 `BOSS_HEAVY_GRACE(2)`）。橙白の脈動（`g-boss-heavy`／被予告 `g-player-heavy`）＋告知ログ。②**怒りフェーズ**＝HP `BOSS_ENRAGE_AT(0.5)` 以下で一度きり覚醒＝攻撃 +25%（`monsterDmg`）・大技CD短縮・告知。③**眷属召喚**＝覚醒後に `BOSS_SUMMON_CHANCE(0.2)`／`BOSS_SUMMON_CD(4)` で脆い眷属（`breederMinion` 流用・`MONSTER_HARDCAP` 管理）。実装＝`dungeon.ts`（`planMonsters` ボス維持ブロック＋隣接の大技分岐／`resolveMonsters` 倍率）＋`web/main.ts`（`announceBossCues`＝engine 純粋ゆえ覚醒/溜めを web が告知・heavy 被弾演出）。ヘッドレス検証＝例外0・大技は退避で空振り（landed 0）/隣接維持で命中（5-8）・覚醒/猶予/上限すべて作動。数値はテストプレイ調整候補。中ボス/エリートは対象外（後続・任意）。

**⑤ 鎮め筋（v1 実装済み・Web。4-11D）：** 戦闘版の干渉動詞。**敵性化した探索者ボス（エリアボス＝出自化石を持つもの）のみ**、HPを0まで削った瞬間に**「討つ／鎮める」の決着**を選べる（獣・異形＝倒すのみ）。**討つ**＝通常撃破（XP満額＋遺物ドロップ＋legend）。**鎮める**＝非殺：出自の化石を `intervene(requiem)` で鎮魂し、XPは半分（慈悲の代償）、年代記に "intervention" として刻む。＝**力（討つ）と物語/慈悲（鎮める）のトレードオフ**。中ボス（エリート＝獣の増強）は対象外。実装：`downOrKill`→手番末 `handleBossResolve`。次段：鎮めた相手の noteを噂/伝説へ、専用報酬（絆・形質）の検討。

**慰霊堂〈鎮魂の堂〉の鎮魂（2026-06-15 ユーザー承認・実装済み Web。`fix_shrine` 動詞①）：** 街の慰霊堂で**縁ある化石を鎮魂**＝`intervene(requiem)` で**変質クロックを巻き戻し因縁を閉じる**（4-2）。あわせて自分の深蝕も少し浄める（`REQUIEM_RELIEF`＝人間性の回復弁。戦闘版⑤鎮め筋と対）。**バランスガード（承認＝「絆あり＋金貨コスト」）：対象は「絆を持つ化石（＝迷宮で出会った相手）」のみ＋金貨コスト（`8+深度×2+死亡時深蝕×8`）**。街から全化石を無料リセットできないようにし、「**放置すると世代を経て怨念へ漂う**」堆積/変質のテンソン（4-1×4-2）を守る。今世代で既に手をかけた相手は対象外（時計は今リセット済み）。実装＝`web/main.ts shrineRequiem`／`talkKeeper` 結線。

**慰霊堂を仕上げ（2026-06-15 ユーザー承認・実装済み Web。動詞②③）：** 同じ店を 1店=1PR で完結。
- **②「先人を悼む」（供養）：** 歴代の**キャラ化石**（自分の系譜＝血/教えの先祖を先頭に表示）を悼む＝**記憶の堆積**（4-4 パリンプセスト）。名を `town.memorials` に残し年代記(legend)に刻む。鎮魂(①)が「絆ある相手の因縁を閉じる」のに対し供養は**戦力ではなく記憶**で、**機械的強化は持たせない**。系譜の先祖を悼むと後継への一言の反応（4-4「旧キャラが後継に反応」）。**供物＝5金貨**（`MOURN_OFFERING`）。実装＝`shrineMourn`。
- **③「深蝕を清める祈り」：** 自分の深蝕を浄める。薬師（金貨・−0.6）と差別化し、**バランスガード（承認＝「無料・小幅・1世代1回」）：無料だが −0.2（`PRAY_RELIEF`）・`Character.prayedAtShrineGen` で 1世代1回に制限**。無料の深蝕回復弁を増やしすぎず深蝕プレッシャー（堆積）を守る。実装＝`shrinePray`。

道具屋＝消耗品（持ち物システム Phase1〜3）・**書記＝伝説化承認／ギルド＝等級・英雄譜（4-4）はいずれも実装済み**（下記）。

**書記＝伝説化承認／ギルド＝等級・英雄譜（4-4・2026-06-15 ユーザー承認・実装済み Web）：** 旧キャラを街の英雄に昇華する 4-4 の中核を結線。
- **書記 act1「旧キャラを伝説として承認する」（`legendApprove`）：** **神話極（`tonePole:"myth"`）の旧キャラ**（＝偉業の最期。`guard_relic`／高絆 `leave_will`／`noble`）を `TrackedEntity(source:"player_legend", arcType:"retire")` に昇格。無料・各化石1回・seeded(gen0)は対象外。昇格後は**後世で `legend_return`（祝福の山場）として帰還**でき、**英雄譜**に名が載る＝M3「運命の弧」の布石。`chronicle("legend")` に刻む。**★資格の厳格化（2026-07-01 ユーザー承認・v0.91 実装済み）＝** 旧「神話極のみ」は死に方（最後の一手）次第で深度8程度でも伝説（＝奉献の印④）になれた→ `legendEligible` を複合ゲートに：**神話極 ＋ 深度25以上（`LEGEND_MIN_DEPTH=25`・死亡深度）＋ 偉功≥30（`legendMerit`＝Lv＋ステ総量/6＋修めた術数＝深いが未熟な階段潜り stair-dive を除外）**。Lv≈深度の終始シビア（4-11F）に整合。`scenario-clear` 緑＝5印の付与経路は健在（クリア可能性維持）。数値（`LEGEND_MIN_DEPTH`/`LEGEND_MERIT_MIN`）はテストプレイ調整候補。**★バグ修正（v0.138.0・2026-07-08・テストプレイFB「伝説として承認する候補にも自分でないキャラが表示される」・系譜の間 #342 と同根）＝** `legendEligible` が `isOwnLineFossil` を経由していなかったため、深度・実力の条件さえ満たせば**死んだ相棒（wasCompanion）・縁を結んだ生者NPC（wasAlly）も伝説化候補に混入**していた。`legendEligible` の先頭条件に `isOwnLineFossil(f)` を追加＝自分の血統のみに限定。**`grudge_hunt`（宿敵）等の遭遇/再発見は意図どおり相棒/縁NPC化石も対象**（見捨てた相棒が怨念化して迷宮で再会する等）＝今回のスコープ外。web 限定・engine 非改変・golden 8/8 完全不変・SAVE_VERSION 据置。
- **書記 act2「系譜をたどる」（`lineageScene`）／ギルド act2「系譜の恩寵」（`lineageBoon`）：** 先代→現キャラの系譜と継いだもの（絆・形質「○○の血/教え」）を表示。
- **書記「拾得品を読み返す」（`keepsakeShelf`・v0.54.0／専用プール化）：** 迷宮の宝箱から低確率で出る**詩情系の収集物（好古の棚）**を世代を越えて読み返せる＝収集の楽しみ。定義＝`content/keepsakes.json`（id/title/story/band・50種）。**出現頻度は定義数から分離**（宝箱開封時に `KEEPSAKE_CHANCE`・深度band 適合・未収集優先で付与）＝何種足しても宝箱が拾得品だらけにならない。セーブは id 参照（本文は複製しない＝後の文章修正も既収集に反映）。純フレーバー・効果なし（`Prereq` に trait/keepsake ゲートは無い＝「全てが戦利品ではない」モデルC のフレーバー）。
- **ギルド act1「等級・英雄譜を見る」（`heroRoll`）：** 現キャラの**等級**（金属6段階＝アイアン/ブロンズ/シルバー/ゴールド/プラチナ。ミスリルは死後称号。レベル帯対応＝4-4E）＋遺した伝説数＋`world.tracked` の英雄譜一覧（源＝街の古い伝説/あなたの伝説/因縁、弧の型つき）。
実装＝`web/main.ts legendApprove/lineageScene/heroRoll/lineageBoon`＋`talkKeeper` 結線（archive/guild act1・2）。残り stub の店動詞はなし（全店の主要動詞が結線済み）。

**教団〈深淵を讃える者たち〉＝深蝕と引き換えの恩恵（2026-06-15 ユーザー承認・実装済み Web。`fix_cult`）：** **金貨でなく深蝕で支払う店**＝慰霊堂/薬師（深蝕を減らす）の**暗い鏡**。「深蝕は呪いではなく祝福だ」。
- **act1「深蝕を捧げる（危険な恩恵）」（`cultOffering`）：** 恩恵を1つ得る＝**禁術を識る**（未習得 `SPELLS` を即習得・通常はレベルアップの二者択一が要る）／**深淵の力＝理 or 力 +1**（XPを介さない近道）。対価は**深蝕**を上げること。
- **深蝕の意味（前提の整理）：** 現状の実装では深蝕に上限も即死も無い。効くのは①変質閾値 `0.5/1.2/2.5` 超えで**奇癖**取得（現状フレーバー）②**死亡時に `exposureAtDeath` が化石へ焼き付き、高いほど怨念極へ寄り・速く変質＝後世で敵性化**（堆積/変質テンソンの核）。＝教団の代償は「即時」でなく「**後世＝自分が後の宿敵になる**」に効く。
- **バランスガード v2（2026-06-30〜07-01 テストプレイFB「リスクが低すぎる」→再設計・ユーザー承認・v0.83〜0.85 実装済み）：** 旧 v1（金貨不要・回数無制限・対価＝深蝕の線形逓増 `0.6+0.2×回数`）は「深蝕だけ・レベル不問・無制限」で割安すぎた。v2＝5本柱：
  - **A. 生涯上限**＝1世代（1キャラ）につき `CULT_BOON_CAP=3` 回まで（死で次代リセット・`Character.cultBoonsThisGen`）＝「深淵がひとつの魂と交わす取引は3度まで」。
  - **B. 永続汚染 `exposureTaint`**＝1取引ごと `CULT_PERMA=0.2` ずつ**二度と祓えない深蝕の下限**が上がる（薬師/安息所/解呪/鎮静薬など全浄化経路が `cleanseExposure` に集約され `max(exposureBrand, exposureTaint)` を下限に尊重）。集めるほど死亡時の怨念化が不可避＝「代償は後世に効く」を機械化した真の対価。
  - **C. 急峻逓増**＝深蝕コスト `cultCost = CULT_COST_BASE(0.6) × CULT_COST_GROWTH(1.8)^今世代の回数`（線形→乗算へ）。
  - **D. レベルゲート**＝授かる術は `minLevel ≤ Lv + CULT_REACH(8)` に制限（上限3回ゆえ1枠の価値を高め、上位術も「ある程度」届く＝帰還 minLv9 が Lv1 から狙える。真の終盤術 omni22/死戸36/断罪44 等はレベル相応にゲート＝cap3＋急峻深蝕＋永続汚染が歯止め）。
  - **E. 金貨も逓増**＝喜捨が回数で増える（`×(1+回数×0.5)`）。
  - 補足＝**烙印 `exposureBrand`（一時分）**は潜行の新フロア初訪ごとに `BRAND_DECAY=0.1` ずつ薄れる（永続下限 `exposureTaint` までで止まる）＝「潜って薄める」余地は残しつつ、汚染は残る。数値（cap/係数）はテストプレイ調整候補。
- **act0「深淵の福音」/ act2「儀式について尋ねる」（`cultLore`）：** 深蝕肯定の世界観と変質/怨念の示唆（純テキスト）。
実装＝`web/main.ts cultOffering/cultLore`／`talkKeeper` 結線。
### 4-12. 遭遇イベントとストーリーレット駆動 — ★確定★
化石／追跡対象との遭遇を、平坦な「再発見テキスト＋固定3動詞」から **「ストーリーレット駆動のイベントノード」** へ昇格させる。**選択ごとに大小のイベントが分岐し、結果（effects）が世界状態へ還流する。** これにより「プレイのたび・プレイヤーごとに異なる物語」を、4-9（制作時鋳造所＋実行時プロセダル・**実行時LLMゼロ**）の枠内で生み出す。（2026-06-13 議論で確定）

**位置づけ（既存システムの結節点）：** プレイヤーが挙げた要望（キャラの深掘り＝lore／宝の発見・捜索／依頼／戦闘／継承）は、いずれも既存の確定システムに散在している（4-6 弧・4-2 干渉・4-11E 装備/異物・4-10E 異物・4-10G 回収業・4-11D 戦闘）。**足りないのは個々の要素ではなく、遭遇をそれらへ振り分ける結節点。** 本節はそれを定義し、spec §3 に**スキーマだけ用意され未結線だった** `Storylet`/`SetPiece`/`Fragment`/`Effect` を正式に屋台骨として採用する。

**(A) 遭遇ノードの三段構造**
1. **状況の抽選** — 化石の{極／変質段階／死の一手／origin} × プレイヤーの{絆・未完の因縁・深蝕・系譜・所持品・現在深度} を prerequisites に、**重み付き（4-7）でストーリーレットを選ぶ。** 同じ化石でも、世界状態が違えば別の状況が立ち上がる。
2. **文脈依存の選択肢（動詞）の提示** — 常設：**鎮魂／継承／立ち去る**。状況に応じて追加：**調べる**（lore深掘り）／**捜索**（宝・遺品）／**依頼**（回収業 4-10G）／**戦う**（敵性化・怨霊 → ⑤鎮め筋）。出る動詞はストーリーレットと状態が決める。
3. **effects の実行と還流** — 各選択が世界状態を書き換える：新たな化石／追跡対象の生成・更新、依頼の発行、宝／異物のドロップ（4-11E／4-10E）、深蝕の変動、絆／因縁の更新、年代記追記、**次の伏線（後続ストーリーレットの prereq を立てる）。**

**(B) 各動詞の役割と分岐の指針**
- **鎮魂** … 末路を閉じ変質クロックをリセット（4-1C）。情緒的決着。effects例：lore の最後の一片が明かされる／**深蝕がわずかに和らぐ（人間性の回復）**／神話極なら残響召喚の種（4-10I）。**「報酬」ではなく行為の意味として設計**（鎮魂＝魔法が深蝕を燃やすことの対の極：人間性の回復弁）。
- **継承** … 力・遺志・遺物を得る（4-11E 武器奪還）。多くは**未完の目的を負う**＝奪還クエスト等の依頼が発生（前のめりの代償）。遺物は未鑑定・副作用つきの異物でありうる（4-10E）。
- **立ち去る** … 放置＝**最も濃く危険な物語の種**（4-2 怨念極）。effects：変質が進み、未完の因縁は重みを保ち、深度近接で**確実に再来（名指しの怨霊）**。"何もしない"が将来の大イベントを仕込む。
- **調べる／捜索／依頼／戦う** … 文脈依存の入口。lore・宝・依頼経済・戦闘（鎮め筋）へ分岐。
- **原則：** 大半は小さな分岐（静かな喪失）。山場（SetPiece）は条件を満たした**少数だけ**に出す（4-2／4-9C の希少性を守る）。

**(C) 還流＝モデルCの駆動** — effects が世界状態へ書き戻されるから、**次の世代・次のプレイの遭遇が変わる。** プレイヤーごとに固有の蓄積史（§3 重要原則）が分岐木を変形させる。背景の常時シミュレートはしない（4-6 lazy）：観測（遭遇）時に状況を生成し、**選択の結果だけを永続化**する。

**(D) 4-9 整合（実行時LLMゼロの担保）** — ストーリーレット・断片・SetPiece・effects テンプレは**制作時の鋳造所が量産**し、極／深度帯／段階／archetype でタグ付けして同梱。実行時はプロセダルに〔prereq照合 → 重み抽選 → スロット充填（痕跡保証 ASSERT＝4-2／spec §4.4）〕するだけ。**多様性の源は「ストーリーレット × 蓄積状態」の組み合わせ爆発であって、ライブ生成ではない**（§0 の予測不能性の源泉に直結）。破綻文が出る経路は原理的に無い（4-9）。

**(E) 段階実装（PR分割の指針）**
- **遭-① 最小の手応え** … 〈調べる〉＋ストーリーレット＋effects還流 — **完了（PR #13）**。
- **遭-② 還流の本格化** … 〈捜索〉＋伏線フラグ→後続ストーリーレットの prereq — **完了（PR #15）**。型 Storylet/Prereq/Effect・伏線フラグ（化石スコープ）が確定。
- **イベント拡充（ユーザー要望・下記(F)(G)(H)）** … スキーマが固まったので、コンテキスト別に大量追加して「出来るだけ多く・ランダムに近い」体験へ。**(H)-1 第1弾 完了（PR #39：40→76件）。** 併せて常設動詞（鎮魂/継承/立ち去る）を下記(B)どおりフル結線し「選択の差が出ない・文が反復」を解消（PR #40：鎮魂=深蝕軽減／立ち去る=未完を残す＝再来の種／継承=未完の目的を負う）。次は **(H)-2 アクター記述子**。
- **遭-③ 経済と戦闘の結線** … 〈依頼〉（回収業 4-10G）＋〈戦う〉ルート（敵性化 → ⑤鎮め筋）。
- **遭-④ 山場** … SetPiece（legend_return＝myth／grudge_hunt＝grudge／**inheritance＝loss**）を遭遇ノードに接続。**myth/grudge/loss の3極すべてにクライマックスが揃う**（inheritance＝倒れた者の「遺志を継ぐ／安らかに送る」二択の決着・v0.42.0・2026-06-25 実装）。
- **戦闘トラック（4-11）と並走。** 〈戦う〉動詞は 4-11 の盤面へ遷移するため、戦闘②以降と素材を相互共有する。

**(F) コンテキスト・タクソノミー（拡充の土台）— ★確定★（2026-06-13）**
イベントは「無区別の1プール」にしない。**発生場所＝コンテキストごとに型を切り**、コンテキスト内で〔大量プール × 重み × 蓄積状態〕で「ランダムに近い」を出す（コンテキストをまたいだ混合はしない）。理由：埋められるスロットと使える動詞がコンテキストで異なり、混合すると痕跡保証（4-2）が壊れる。
- **遭遇（encounter）** … 実体に出会う。アンカー＝アクター（化石／生者NPC＝(G)）の origin。動詞：調べる/捜索/鎮魂/継承/戦う/依頼。
- **ダンジョン（dungeon）** … 探索中の環境イベント（実体なし）。アンカー＝深度・階層・理の緩み。動詞：調べる/採取/触れる/通過。異物・現象・発見の宝庫。**NPC不要。**
- **街（town）** … 潜行の合間。アンカー＝**骨格NPC(4-4B)**・長命の証人(4-10F)・追跡対象の弧(4-6)。動詞：聞く/受ける/応じる。街は固定マップを `@` で歩く（4-4B）。
- **依頼（quest）** … 街発→迷宮で達成（回収業 4-10G）。アンカー＝依頼主NPC＋対象（化石/場所）。
> 多様性は「コンテキスト内ストーリーレット × アンカー × 蓄積状態 × effects」の組み合わせで出す（4-9整合＝(D)）。prereq を緩めに広く作るほど「何が出るか読めない」のに破綻しない。

**(G) アクター記述子（NPC＝化石 origin の一般化）— ★確定★（2026-06-13）**
「出会い・街・依頼」の richな部分はNPC生成に律速されるが、**NPCは新システムではなく、化石の `origin`（名前/職分/装備/二つ名/口癖）を一般化した「アクター記述子」に統合**する。
- 生者NPCを作る＝そのアクター記述子を**鋳造所断片から埋める**（化石に origin を与えるのと同じ機構）。
- ストーリーレットは「アクター」にアンカーするだけ（化石か生者かを問わない）。スロット充填・痕跡保証は既存機構を再利用。
- ＝「NPC作成」＝既存 origin/スロット系の一般化。重い新規実装ではない（4-9整合）。

**(H) 拡充の進め方（順序）— 合意（2026-06-13）**
1. **NPC不要の2コンテキストを先に厚く** … 遭遇（化石）を大量追加＋ダンジョン環境イベントを新設（最速で「多く・ランダムに近い」を体感）。
2. **アクター記述子(G)を実装** … 街/依頼/生者の出会いを解錠する鍵。
3. **街・依頼・生者の出会い**を一気に拡充（遭-③の依頼経済と合流）。

> **要点：** 4-2（干渉）・4-6（弧）・4-7（重み）・4-9（鋳造所）・4-10G（依頼）・4-11D/E（戦闘・装備）を **一つの遭遇ループに束ねる結節点**が本節。effects の語彙（型）と係数は実装PRで確定する。

**(I) 長尺アーク＝進行度クオリティで「長いイベント」を組む — ★確定★（2026-06-16 ユーザー承認・実装済み Web）**
単発の storylet や2段のフラグ連鎖は「尾を引く体験」には短い。他作品調査（Fallen London の **Quality-Based Narrative**＝進行度クオリティで段を進める／Witcher3「血まみれ男爵」＝早い選択が後段・複数結末を変える／side-quest の diamond 分岐）を踏まえ、**多段の弧（サブクエスト相当）**を本作の storylet 機構の上に乗せる。
- **進行度クオリティ：** `World.arcs[]`（`{key, step, pick?, done?}`）＝boolean フラグでは数えられない**段カウンタ**。世代越え・非破壊。
- **段ゲート（Prereq）：** `arc`（その弧が進行中）＋`arcStep`（現在段が一致）＋`arcPick`（早い分岐の選択が一致）／`notArc`（未開始＝開幕の重複防止）。3つの選出器（encounter/dungeon/town）すべてに `arcMatches` を結線。
- **段の前進（Effect）：** `arc:{key,step,pick?,done?}`＝開始/前進/分岐記録/完了。**アーク段は抽選で最優先**（`pickPreferArc`）＝flavor に埋もれず弧が確実に進む。
- **長さ＝段数 × 分岐 × 蓄積ゲート：** 早い選択が後段の有無と結末を変える（branch-and-bottleneck）。深度帯・深蝕などの**蓄積状態でしか開かない段**を置ける（QBN）。
- **見本①＝「沈黙の扉」の弧（dungeon・4段・複数潜行をまたぐ）：** 唸る封印扉を見つける→死んだ探索者の手記で開け方を知る→深層で扉が自分の深蝕と同拍で脈打つ→**開ける(力＋深蝕の代償・奥の聖堂で結晶を得る)／封印を守る(深蝕が和らぐ・別の結末)** の分岐。`pick=open` のみ4段目（報酬）へ進む。実装＝`content/storylets.json ad_door_*`、機構＝`world.ts getArc/setArc`・`storylets.ts arcMatches/pickPreferArc`・`types.ts ArcState/ArcEffect/World.arcs`。
- **報酬は全イベント共通でセット（2026-06-16 ユーザー要望・実装済み）：** `Effect.gold`（金貨）／`Effect.item`（消耗品キー＝容量超過なら持ちきれず破棄）／既存 `bond`（絆）。`applyReward` を encounter/dungeon/town の全 apply に結線。`grantConsumable`（`items.ts`・純粋）を切り出し `addConsumable` と共用。
- **特定NPCに戻る弧＝アクターアンカー（2026-06-16・実装済み Web）：** `ArcState.actorRef`＋`ArcEffect.anchor`（開幕で「今会っている生者」をアンカー）＋`Prereq.arcActor`（同一NPC本人にだけ後段が出る・他人に漏れない）。`resolveMeetActor` が進行中アークのアンカーNPCを優先再会させ、弧を前へ。**見本②＝「片割れの形見」の弧（街→深層→街・3段）：** 冒険者が「深層で還らなかった相棒の形見」を頼む→深層で護符を見つける→**届ける（金貨+35・絆+2・膏薬＝金/物/絆の3報酬）／着服する（深蝕+・裏切りの形質）** の分岐。実装＝`kp_*`。
- **アンビエント街イベント（4-12 J・新イベント型の器・2026-06-16 ユーザー承認・実装中 Web）：** 潜行帰還時に稀に起きる"街規模の出来事"。`maybeTownEvent` が**1帰還につき最大1件**を、型ごとの**クールダウン**＋全体確率で抽選。型を増やすのは1行。リサーチ（RimWorld/DF の脅威・中立・好機・災厄／RPG の取得・護衛・調査・対人）を踏まえ、**ユーザーが7型を全採用（2026-06-16）。**
  - **① 街の防衛（襲撃・実装済み／2026-06-26 盤上化・3規模）：** 街規模の危機。**規模＝プレイヤーLvで自動選択**。**小（Lv<20）＝テキスト寸劇**（従来の3ビート＝門/避難/商店街→最後の一匹・`raidTextScene`）。**中（Lv20-33）・大（Lv34+）＝街マップ上の盤上防衛戦**（`enterRaid`／`mode="raid"`）＝迷宮の戦闘エンジンを街の特設戦場（`genRaidField`＝広場/街路＋瓦礫の遮蔽＋上辺の湧き口）で回す。**他の冒険者と共闘**（縁あるNPC優先＝再会／大=3〜5名・中=1〜2名）＝戦闘エンジンの**味方を複数化**（`planMonsters/resolveMonsters` に `allies` 引数を追加＝味方0で従来と完全一致＝golden 不変）。**wave で押し寄せ**（中2波／大3波＋最後にエリアボス「襲撃の主」）。**大規模は逃げ遅れた市民（守る対象）**＝隣接した獣に呑まれると喪失／プレイヤーが隣へ踏み込めば避難誘導（救助）。**プレイヤー死＝通常の死（deathFlow）／味方の戦死＝化石化（後世で再会＝4-14）／市民の喪失＝年代記**。中・大は突入前に**「街で共に迎え撃つ／采配で手早く捌く（テキスト）」を選択**。術・持ち物も使える。報酬＝規模×tier＋撃破数＋救助＋生存仲間。**クールダウン制（14〜20帰還・世代越え）**（`raidCooldown`）。**web 限定**（CLI/demo は盤上戦に入らない＝engine の味方複数化のみ共有）。
  - **④ 追悼の日（祭礼・実装済み）：** 襲撃の"対"＝好機の定期イベント。縁ある死者/無名の死者を悼む→**深蝕が和らぐ（人の温もり）＋評判形質＋悼みの護符**。慰霊/化石テーマに直結（`memorialCooldown`・`townMemorialScene`）。
  - **⑥ 依頼の二重底（呪われた依頼・実装済み・content）：** 善意に見えた依頼の真相が渡す段で反転（黒い小函＝深みに仕える者の忌物）。**着服（礼金＋深蝕）／見破り（報酬なし＋真相＋封じの薬）** の分岐。アクターアンカー弧（`cw_*`）。
  - **⑦ 貴族の使者（布石・実装済み・content）：** 高名な者（`minLevel`）にだけ、封鎖された貴族街の関心が向く噂（`noble_rumor`）。
- **貴族・統治者エリアの超長尺アーク（⑦の発展・4-12 J/I・2026-06-17 実装済み Web）：** 高名（Lv8+）になると一度だけ、封鎖貴族街から**正式な召喚**が来る（`maybeTownEvent` 最優先・`noble_summoned` で一度きり）。`nobleSummonsScene` が**専用の貴族アクターを構築・アンカー**し弧 `noble` を開始。多段の大命＝門→謁見（血に巣食う深みの呪い＝深蝕テーマ／『原初の証』を持ち帰れ）→**深層で証＋一族の罪（供物の契約）を発見（忠義/暴露の分岐）**→**謁見の決着（忠義＝莫大な礼+客人の称号／罪を黙して礼＝深蝕の澱み／真実を暴く＝礼減+深蝕緩和+暴いた者の称号）**。中盤=dungeon、終盤=`arcActor`+`arcPick` で同一貴族・分岐別の謁見。報酬は最大級（金70）。実装＝`nb_seek/nb_relic/nb_return_loyal/nb_return_curious`＋`nobleSummonsScene`。決定論ウォーク3分岐＋実ブラウザで召喚/謁見を確認。**この Lv45 アークはクリア前の導入（召喚→シート謁見）として温存。専用の歩ける貴族街区画は奉献クリア後に解禁＝実装済み（4-14G 層4b・v0.61.0／別軸両立）。**
- **イベント設計の見直し（2026-06-21・ユーザー承認・Web／P1-P4）：** ①**P1 ペーシング**＝(H)へ反映（序盤スロープ／1降下1イベント／直近重複回避メモリ）。②**P2 出し分け精緻化**＝`Prereq` に `minDepth`/`maxDepth` を追加し、dungeon/chest 選択でも `minDepth/maxDepth/minLevel` を適用（`pickByContext` に深度/レベルを渡す＝語りと深度の整合）。化石遭遇の配置数を深度で疎密化（浅層1体・深層は面積比＝世代過密の抑制）。**web の宝箱が chest storylet（既存45件）を初めて使用**（罠15%/装備40%/中身の物語45%＝`rollChestOutcome`・従来は罠/装備固定で chest content は CLI/demo のみ使用だった）。③**P3 選択の手応え**＝gold↔深蝕・item↔深蝕の二律背反 storylet を投入（決定論・既存語彙）。④**P4 伏線→回収**＝`arc`（世界スコープ）で深く潜るほど後段が開く2連鎖（先人の目印 deep_marker／血の誓い echo_vow）。実装＝`storylets.ts`（`pickByContext`/`pickPreferArc(avoid)`）・`web/main.ts`（`recentEvents`/`maybeDungeonEvent` 逓増・`chestScene`）。
- **イベントのレベル帯ペーシング（~Lv50 スケール・★クリア想定レベル確定★ 2026-06-17 ユーザー承認）：** クリアは**おおよそ Lv50**、**クリア以降は自由なH&S**（古典ローグライク ~Lv27-30 より高く、ARPG/Caves of Qud 寄り）。~~等級(4-4E)は Lv12=プラチナで飽和＝以降の高位ゲートは**レベル数値**で行う。~~ **★2026-06-23 改訂：等級は Lv50 スケールへ再配分＋実績ゲート化（4-4E(E)）＝プラチナはクリア近辺・生存ミスリルはクリア後。高位ゲートは引き続き storylet の `minLevel` で行う。** 帯＝**早期1-10／中期11-25／後期26-40／超期45+**。低レベルでまとめて着火しないよう各イベントを帯ゲート：襲撃Lv11／疫病Lv14／追悼Lv4／依頼アーク(片割れLv8・二重底Lv14・好敵手Lv18)／貴族の噂Lv30→**貴族の大命Lv45**。実装＝`web/main.ts BAND` ＋ storylet `prerequisites.minLevel`。
  - **クリア帯＝案2に確定（2026-06-17 ユーザー承認）：クリア深度を深く（~depth50）＋クリア以降エンドレス（Qud寄り）。最速クリア ~30時間 を想定。** クリア目標は明確に置く（深淵帯＝奉献の試練の聖遺物）が、4-3①「底は永久に未到達」に従い**クリア後も深層へ無限に潜れる**（印リセットせず反復＝H&S継続）。
    - **★数値確定（2026-06-17 ユーザー承認・終始シビア軸＝Lv≈深度／次PRで実装）：** ①**深度**＝`DEPTH_SEAL_AT 16→~40`／`ABYSS_DEPTH 28→~50`／depth50以降エンドレス。②**敵スケール**＝深度係数で無限スケール。**雑魚 `HP=6+round(深度×1.6)`・`dmg=1+round(深度×0.18)`／エリアボス `HP=雑魚×4+20`・`dmg=雑魚+4`**（ボスは硬め維持＝止め/距離/弱体/回復/遠距離の駆け引き前提・8刻みで節目維持）。ogre(22)頭打ちを解消し新ティアは帯へ。脅威源＝包囲。③**XP曲線**＝`round(8+4L+0.8L²)`（案B・Lv50総≈37.6k。旧`6+6L`≈7,644 は浅すぎ）。④**装備/アイテム拡充**＝武器/防具/遺物/消耗品の高深度ロウ追加（金属6等級4-4E を深度対応）＋クリア後エンドレス用の上位ドロップ。⑤**クリア判定**＝5印＋深淵帯(depth~50)→聖遺物→帰還の試練→奉献。⑥**壁ビルド抑制**＝`maxHp=6+3×min(体,16)+1×max(0,体−16)`（4-11F②）。⑦**魔法**＝ロードアウト制10構え＋40種カタログ（4-11F③）。
  - **② 深蝕の瘴気（疫病・実装済み）：** 深みが地表へ滲み街の者が病む災厄。看病（深蝕を浴びる＋評判形質＋護符）／隔離（街を守るが見捨てる者＝断行の形質）／退く の分岐（`plagueCooldown`・`townPlagueScene`）。
  - **③ 好敵手の弧（対人・実装済み・content）：** 冒険者が深度勝負を挑む→深層で落とし物を拾う/拾わない（矜持/狡）→**健闘を讃え合う（戦友＝絆+金）／挑発し返す（宿敵）** の分岐。アクターアンカー弧（`rv_*`）。
  - **⑤ 崩落・閉じ込め（迷宮の救助・実装済み・content）：** 崩落で生き埋め/挟まれた探索者を**掘り出す（金+消耗品+評判）／見捨てる（深蝕の重み）**。中層/深層（獣迫る）の2種（`cl_*`）。
  - **→ 7型すべて実装（2026-06-16）。** 量産は道2（鋳造所）で桁を上げる。
- **鋳造所（道2）の試作＝コンテンツ受理ゲート（4-9・2026-06-17・リサーチ後実装）：** 研究知見「LLMは構造的に無効な出力を出しがち→**schema-governed generation＋engine-aligned admission（検証/修復）**」に基づき、`tools/validate-content.mjs` で storylets.json をエンジンのスキーマで厳密検証（context/prereq/effect キー・enum値・**context別スロット充填可否**・id一意・arc整合・item キー・weight）。負のテストで6種の不正を捕捉、**CIスモークに常駐**（壊れた content を通さない＝量産の安全弁）。生成パイプライン仕様は `tools/FOUNDRY.md`。**実際の大量生成はユーザー合意のうえ context・テーマ単位で**（受理ゲート→レビュー）。
- **今後：** 弧を**量産（鋳造所＝道2）＝着手前に各種ゲームをリサーチしてから**（ユーザー指示）。**同種イベントの飽きを避け種類を増やす**（街・化石・同行・依頼をまたぐ弧、新イベント型）。**将来構想（視野に入れる・未実装）＝ 貴族・統治者エリア（未実装区画）でしか受注できない超長尺イベント。**

---

### 4-13. クリア／長期目標＝「奉献の試練」 — ★確定★（2026-06-15）
「**底は永久に未到達**」（4-3①）を維持しつつ、底に依存しないクリア／長期目標を確定する。**クリア＝深度到達ではなく、①多様な偉業で資格を開く（gate）＋②帰還の試練（climax）の二本柱。** 本セッションで実装した深蝕/HPドレイン・怨念/敵性化・伝説化・因縁/鎮魂と直結させ、これまでの全システムを回さないと到達できない長期目標にする。クリアしても世界は続く（モデルC・endless）。

> **設計の出自：** 当初案「深淵帯＝高深度で聖遺物→生還＝クリア」は「深度到達だけで即クリア＝甘すぎる」として却下。他作品リサーチ（DCSS の Orb run＝rune を集めて Zot 解禁→Orb 奪取→脱出／NetHack のアセンション＝最深で Amulet→地上まで帰還、追手あり）を翻案し、**難しさの本体を「深度」から「多様な偉業の達成」へ移した。**

**(A) 資格＝印（しるし）を5つ集める＝難しさの本体（gating）**
深淵帯は封印され、**別系統の偉業＝印**を5種集めて初めて開く。印は **World に蓄積し、世代をまたいで残る**（個々のキャラの一代では揃いにくい＝系譜をまたぐ長期目標）。源は多様（各1種）：
1. **エリアボス（成れの果て）を撃破** … 4-11 戦闘／4-6 弧。
2. **因縁（grudge 化石）を鎮魂する** … 4-2 干渉／4-12 鎮魂。
3. **山場（grudge_hunt／legend_return／inheritance）を決着させる** … 4-12(山場SetPiece)。3極すべての山場決着が印③を付与。
4. **旧キャラを伝説化する**（player_legend 昇格） … 4-4 英雄／伝説化承認。**資格は厳格化済み（v0.91・詳細は 4-4 伝説化承認＝神話極＋深度25+＋偉功30+）。**
5. **深淵帯手前の高深度に到達する** … 探索（深度はもはや唯一条件ではなく5要素の1つ）。

→ 戦闘・遭遇・継承・伝説化・探索を**全部回さないと開かない**。1回の深いダイブだけでは不可。

**(B) 深淵帯へ**
5印が揃うと、街の門に「**奉献の試練へ潜る**」が出現（専用ダイブ）。深淵帯＝高深度の封印フロアで、最奥の**主**（最強の成れの果て）が**聖遺物**を守る。

**(C) 帰還の試練（＝climax）**
聖遺物を取ると深淵が覚醒：
- **深蝕が急上昇**（携行中は毎手 追加加算）→ HPドレインが強まる（4-10C）。
- **追手（敵性化した旧キャラ＝怨霊）が湧き、地上まで追う**（上りのたびに spawn・4-2 怨念極）。
- **生還（深度1→街へ戻る）して初めてクリア成立。** 死ねば聖遺物は化石に刻まれ、後世が奪還しうる（モデルC 還流）＋強い怨念化。

**(D) 報酬とモデルC（クリア後も続く）**
- 生還＝大きな**達成**（年代記 "legend"）＋このキャラ英雄譜入り。**聖遺物の処遇を選ぶ**：**奉納**（街に恒久変化＝メタ長期目標へ接続）／**佩用**（伝説級遺物＝個人の力）。
- **印は World に蓄積し続け、クリアでは自動リセットしない。** 一度5印を揃えれば深淵帯は開いたまま、聖遺物を消費しても**試練を自由に反復できる（H&S サンドボックス継続）。** 印が0に戻るのは**データ初期化（新規セーブ）時のみ。**
- クリアごとに `ascended` を加算。複数回クリアで街が自分の歴史に塗り替わる（メタ長期目標）。節目（深度記録・英雄譜数・初決着）は年代記に編む。

> **底未到達との両立：** 聖遺物は「底」ではなく深淵帯（神話極の封印層）に置かれる。底（世界の真の最深）は依然として永久に未到達のまま。奉献の試練は「底へ達する」物語ではなく「深淵から聖遺物を地上へ持ち帰る」物語。

> **実装ステータス（2026-06-18）：** Phase1-4 完了（web）。Phase4 メタ達成＝街の塗り替え（PR #101）＝奉献の碑/像（`refreshAscendMonument`）／貴族街が反応（軽量アーク `noble_ack`・**門は解禁せず** Lv45「原初の証」アークと別軸両立）／統治者の大命（`Quest.patron:"noble"`＝奉献後の高報酬依頼）。設計本文は不変＝上記が正。残＝数値バランス（聖遺物の深蝕加算/追手頻度）はテストプレイで調整。

> **バランス改修（2026-06-19・ユーザー承認＝「深度の刻印型」）：** テストプレイFB「深蝕HPドレインがきつすぎ＝クリア不能」への対応。**深蝕の累積を「毎手×歩数」から「降りた階数」へ移行**（`exposurePerDescent` 中0.10/深0.20×心係数＝マップの広さに不依存。巨大マップの滞在ターンで exposure が青天井に膨れ、帰還の試練の聖遺物往復が致死量＝生還不能だった構造を是正）。あわせて**牙（HPドレイン）に上限 −2/手**（`CORRUPTION_DRAIN_CAP`・発動閾値1.5・+2.0ごとに+1）／**深淵帯フロアを小型固定 40×44**（脱出スプリント化＝往復の被ドレインを回復薬で埋まる量に）／**聖遺物携行の毎手深蝕 0.12→0.03**／**解呪術＝毎手抑制→その場で深蝕 −0.6 の浄化弁に転用**。web 限定（CLI/demo は旧 `exposureGain` 毎手モデル据え置き）。数値は要テストプレイ微調整。

> **深蝕リワーク v2（2026-06-19・ユーザー承認・web 限定／上記「深度の刻印型」を置換）：** 「じっくり攻略を一切罰しない」を構造化。**(1) 受動累積を全撤去**（per-turn も per-descent も廃止＝`enterFloor` の降下刻印を撤去）。**深蝕の蓄積源は3つだけ**＝①**術使用**（`castSpell` で `def.cost×心係数`）②**異物装備**（呪われた装備の代償。**降下1階ごとの固定** `equipExposure×ODDITY_DESCENT_MULT(10)×心係数`≈0.2-0.5/floor＝滞在ターン非依存・2026-06-19 微調整。旧「毎手 drip」は大マップで青天井になり"じっくり攻略を罰した"ため是正）③**聖遺物携行**の毎手（`RELIC_EXPOSURE_PER_TURN(0.015)×心係数`・帰還経路が生存可能なよう 0.03→0.015 に減・同日微調整）。探索・移動では一切増えない＝近接主体なら無傷で深く潜れ、魔法に頼るほど自分が危うい（強さと人間性の取引）。**(2) 牙（HPドレイン）は維持**（閾値1.5・上限 −2/手）。**(3) 回復ノード**（`Shrine`・一度使用で消える）＝**回復の泉**（HP回復・深層は1階確定/浅中はランダム）＋**安息所**（深蝕浄化 −0.8・深いほど設置数増）。**(4) 深淵帯フロアはフル寸法に戻す**（広さで死なないため縮小不要・40×44固定を撤去）。**(5) 帰還方法3経路**＝①**上り階段直帰**（深度1の‹で街へ＝潜行終了→次は新ダンジョン1階から＝farm根絶維持）／②**帰還の詠唱**（術 `homeward`＝任意地点で数手チャネル・詠唱中は無防備・動くと中断→満ちたら街へ＝潜行終了。聖遺物携行中は奉献成立＝NetHack 昇天ラン型の山場）／③**帰還の扉**（エリアボス撃破地点に出現。くぐると**あの階の盤面〔ボス討伐済み〕を丸ごと「駐機」して街へ戻る**＝`World.town.returnPortal`〔world セーブに保存＝リロード/アプリ再起動でも消えない〕＋専用スナップショット。以後、**街の慰霊碑の傍に「帰還の扉」が開き、一度だけ**同じ盤面へ戻れる〔`floorCache` 込みで復元＝ボスは討たれたまま〕。**一回きり＝farm根絶**（新規潜行を始めると駐機は破棄）。深淵帯では出さない＝試練の脱出は①②のみ。★2026-07-04 改訂（v0.107.0）＝旧「潜行中に何度でも往復」はメモリ上の一時状態で、街に戻ってからアプリを閉じる/PWA自動更新でリロードすると帰り道が消える不具合があった〔テストプレイFB「8階ボス撃破後に街から戻れない」〕。街側の永続扉＋一回きりに作り替えて解消・分かりやすい専用入口に）。リサーチ準拠（世界樹＝アリアドネの糸〔一度きり〕＋地脈〔恒久〕／NetHack＝追われる帰路／Wizardry＝高リスク MALOR 往復）。数値は要テストプレイ微調整（`HOMEWARD_CHANT 3`／泉 `SPRING_HEAL_FRAC 0.6`／安息所 `REST_CLEANSE 0.8`／設置数）。

---

### 4-14. 冒険者イベントと同行 — ★確定★（2026-06-15）
生者の冒険者（`adventurers.md`・4-4E）を、ただの会話NPCでなく**モデルC（堆積する世界）の主役**として機能させる。

**(A) 背骨＝「生者＝まだ書きかけの化石」**
- 出会う生者は、いずれ化石になる存在。**プレイヤーの関わり方が、その化石の"原稿"になる。** 化石・深蝕・世代交代・討つ/鎮めるの既存機構により、**書いた原稿に後で必ず再会する**（亡霊・宿敵・伝説・聖遺物）。会話を「後世への書き込み」にすることで、すべての交流が賭けになる。普通の「会話するだけ」NPCとの差別化＝本作の独自性の核。

**(B) キャラ起点イベントの5類型**
- **A 同行** … 一時的にパーティを組む。相方の深蝕も上がる＝他人の人間性を戦力に換える選択。(C)で詳述。
- **B 見殺し／救助** … 深層で窮地の冒険者を救う（コスト）か見捨てるか。**見捨てれば怨念極の化石へ寄り、後世で grudge_hunt の宿敵としてプレイヤーを狩る＝宿敵を自分で執筆する。**
- **C 生者を討つ／鎮める** … 再会のたび深蝕の進行が描写で見える。閾値で危険化し、**死を待たず生者のうちに**討つ（慈悲＝即化石化）/鎮める（心ステで正気に戻す）を迫る。
- **D 深蝕の受け渡し** … 深蝕を人と人の間でやり取りする通貨に（肩代わり＝自己犠牲／押し付け＝搾取。教団・ネレイア P5 が市場）。**後段に温存。**
- **E 約束と系譜の記憶** … 約束を世界が記録。果たせば孤が retire/legend へ・後継を祝福／破れば grudge へ漂う。生者は前キャラを覚えている（系譜）。**後段に温存。**
- NPCからの「依頼受注」型はギルド経済（4-10G）と二重になり世界観的にも不自然なので**採らない**。冒険者イベントは上記の関係性ベースで駆動する。
- **✅ すれ違い（迷宮の生者冒険者・実装 2026-06-21・v0.12.2・Web限定・ユーザー要望）：** 「他の冒険者も普通に同じ迷宮を潜っている」を実体化＝化石（死者）・手負い（救助）とは別に、**五体満足で潜行中の生者**と盤上ですれ違う**軽イベント**（context=`delver`・緑の `@`＝`g-delver`・接触で**一度きりの会話**＝近況交換／膏薬・金貨の分け合い／忠告・噂・助言／焚き火で一服）。手負い機構を流用（`Floor.delver`/`DelverActor`・`enterFloor` 初訪・深度2+・`DELVER_CHANCE`／名簿員なら `ROSTER_DELVER_CHANCE`）。**勧誘はしない**（軽さ優先＝同行(C)・救助(B)とは別レイヤ）。bond/plant が立てば `rememberActor` で永続＝後で街で見かける「また会えそう」の余韻（→E と地続き）。`delverScene`→`selectDelverStorylet`→`applyActorEffects`（街の生者アンカーを流用）。数値はテストプレイ調整候補。

**(C) 同行＝「連れ歩く、書きかけの化石」 — 仕様確定**
Web版ダンジョン（タイルクロール・FOV・テレグラフ戦闘）に乗せる。CLI/demo は対象外（テキストメニュー据え置き）。
- **基盤：** 相棒は**1体限定**のグリッドエンティティ（`Pos`＋hp＋深蝕＋絆＋actorRef）。@に追従し隣接攻撃、**テレグラフを出す**（移動/攻撃/待機＝4-11A・決定論維持）。階段は隣接で同行降下／離れるとはぐれる（NetHack挙動）。
- **★交戦支援＝挟撃志向のAI（2026-07-04 テストプレイFB「相棒が回り込まず挟撃にならない」→ユーザー指示で改善・v0.116.0 実装済み）：** 旧AIは「隣接敵を殴る／それ以外は@追従」だけ＝@が棒立ちで殴り合うと相棒は後ろで待機し、位置取りの通貨（4-11A 挟撃）が死んでいた。改善＝`planCompanion` に**交戦支援の一段**を追加：@が敵と交戦中（隣接）なら、**敵を挟んで@の反対側（挟撃位置）を最優先目標**に、塞がっていれば敵に隣接する空き床のうち最寄りへ自分から寄る。移動も**8方向から目標へ最も近づく空きマスを選ぶ**方式（同距離の横滑り許可＝@の真後ろからでも回り込める・遠ざかる一歩は踏まない＝振動防止・決定論＝走査順で安定）。erratic（連帯深蝕の逸脱）・追従・攻撃は不変。**raid の共闘者も同じ `planCompanion`＝街防衛戦でも味方が自分から囲む。** companion は golden 指紋外・CLI/demo 非使用＝golden 8/8 不変。E2E＝真後ろ湧きでも4手で回り込み挟撃成立。
- **★被ダメ軽減＝「すぐ死ぬ」是正（2026-07-07 ユーザー承認・v0.132.0 実装済み）：** 相棒は生ダメージを丸受け（防具軽減なし＝v1の暫定制限）で、HPが本体の140%あっても実効耐久は本体の約半分＝群れの集中攻撃で溶けていた。`dungeon.ts companionReduce(grade,depth)`＝等級＋深度で緩くスケールする軽減値を追加し、被弾を `max(1, dmg-reduce)` に（下限1＝無敵化しない）。火力（`companionDmg`）は不変＝延命のみを狙った是正。dive・raid（街防衛戦の共闘者）双方に適用。数値はテスト調整候補。
- **連帯深蝕（採用）：** 潜行ごとに相棒の深蝕も上昇。深追い＝相棒の人間性を削る。閾値で奇癖・挙動の乱れ（erratic化）→ (B)-C が盤上に自然発生。
- **死＝化石化（採用）：** 相棒のHPが0になると、その床に**絆つき（高関与度）の `FossilEntity` をドロップ**。既存の再発見エンジンで後世に亡霊/怨念/伝説として再会する。生者→化石変換は `アクター記述子`（4-12G）で地続き。
- **入口：** 街で勧誘／**フロアで手負いを救助＝相棒化**（B 救助との融合。見捨てれば怨念化）。
- **生還：** 二人で生還すれば相棒は街の生者NPC（`LivingActor`）として残存、絆と**等級が上昇（⤴ 4-4E）**、次世代で系譜を記憶。**契約モデル（下記2026-06-16 改訂）で雇い直せる反復キャラになる。**
- **再利用フック：** `meetActor`/`LivingActor`（勧誘・永続）・`exposure`/奇癖・`FossilEntity`＋再発見エンジン・`intervene`(requiem/inherit)・グリッド/テレグラフ（`dungeon.ts`）。**新規：** 相棒エンティティ＋手番AI（追従/隣接攻撃）＋テレグラフ描画＋死亡時の化石ドロップ。
- **等級と強さ＝設定ファイル駆動（4-4E ⤴ の実体化・2026-06-16 ユーザー承認・実装済み Web）：** 相棒の**初期等級は設定ファイル（`content/fragments.json` の `actor_grade` スロット）由来**＝鋳造所断片の一部としてアクターに付く。ピラミッド重み（下ほど多い＝アイアン優勢・プラチナ希少）で 4-4E の「生きた名簿」を procedural に再現。
- **雇用のランクゲート（上位は滅多に雇えない・2026-06-16 ユーザー承認・実装済み Web）：** **雇えるのは「相棒の（蓄積）等級 ≤ プレイヤーの等級（`levelGrade(level)`）」まで。** 格上の冒険者は雇用を断り（街の勧誘＝「もっと高みへ来い」）、手負いを救助しても雇えず礼を残して去る（救った＝怨念化はしない）。プレイヤーが相応の等級に上がって初めて上位を誘える＝**初期ガチャで昇格の坂を飛ばせない**。実装＝`outranksPlayer`／`offerCompanion`・`rescueScene` のゲート。**格上のスポット同行（イベント限定・一時）は次段に分離。**
- **同行＝「目的のための契約パーティ」（モデル改訂・2026-06-16 ユーザー承認）：** 同行は世代を貫く強制継承ではなく**その時の契約**。① **雇用**＝街の生者NPC/救助した相手に持ちかける。**誘う（プレイヤー発）＝前金（同行費用＝等級で変動 `HIRE_FEE_BASE×(grade+1)`）＋同行中の金貨を折半**／**誘われる（救助の申し出）＝前金なし・折半のみ**。② **報酬折半＝金貨のみ**：同行中に得たダンジョン金貨（拾得装備の売却益・行商人・依頼報酬）を相棒が50%自動天引き（`splitGold`）。装備ドロップはプレイヤーが保持。③ **解散自由**：街でいつでも無料解散（`heroRoll` から）→相棒は生者NPCに戻り**再雇用可**。ソロ攻略したい時は解散して一人で潜る。④ **プレイヤー死亡＝契約終了**：相棒は生き延びて街へ（次代が再雇用可）＝旧『プレイヤー死を越えた強制継承』を**廃止**。⑤ **深みはフル維持**：戦死→化石化→後世で再会／連帯深蝕→危機（討つ/鎮める）はそのまま。**等級昇格は生者NPC（`world.actors` の記録）に蓄積**し、何度も雇った相棒は昇格して再雇用時に反映（前金も上がる）＝「あの新人が今や精鋭」を契約越しに保つ。実装＝`world.companion`＝現在の契約／`persistCompanionRecord` で書き戻し／`LivingActor.grade/bond/feats`。**強さも設定ランクで変動：** 相棒の最大HP・攻撃力は等級で増える（`companionMaxHp/companionDmg`＝`dungeon.ts`）。**昇格は「生存と偉業で1段ずつ」＝滅多に上がらない（⤴）：** 段kへ上がるには **生還の蓄積（`bond`）と偉業（`feats`＝ボス撃破/山場決着）の両ゲート**を満たす必要がある（通算＝ブロンズ:生還3+偉業1／シルバー:7+2／ゴールド:12+4／プラチナ:18+6）。偉業は**相棒が盤上に共に在る時だけ**カウント（`recordCompanionFeat`）。生者は**プラチナで頭打ち**。**ミスリル（秘銀）は死後のみ**＝相棒が神話極で化石化→既存 `legendApprove` で授かる（生者ミスリルは設定上ごく少数）。等級は `heroRoll`（ギルド）で相棒行として表示。データ＝`Actor.grade`/`Companion.grade`/`CompanionEntity.dmg`、ラベルは `GRADE_LABELS` に集約し `rankLabel`（プレイヤーのレベル帯）も共有。
- **見捨て＝怨念を執筆（B 救助の裏・2026-06-16 実装済み Web）：** 手負いを見捨てると、その場で**怨念極（grudge）の化石を執筆**（`fossilizeAbandoned`＝`manner:betrayed`/`curse_dungeon`・高深蝕・`bondAtDeath=3`）。後世で `grudge_hunt`（宿敵）として確実に還り、プレイヤーを狩る＝「宿敵を自分で書く」(B) を結線。
- **未決の小事：** 相棒グリフ/色（街規約＝群衆ラテン c/$/n/t/f と衝突しない色違い`@`か大文字ラテンが候補）。

**(F) 化石の供給＝初期12体＋生者→化石ループ（★確定 2026-06-28・ユーザー承認・実装済み）**
テストプレイFB「いつも同じ化石ばかりで楽しくない」への根本対応。原因＝化石プールが**シード2体＋自分の歴代死体**だけで、プレイヤーが触れている“生きた世界”（delver/街常連/名簿150名）から切り離されていた。**(A)「生者＝書きかけの化石」の輪を閉じる**のが本質的解決。
- **① 初期シード12体（新規ワールドのみ）：** `world.ts SEED_FOSSILS`＝`adventurers.md` の故人系を元にした12体を `newWorld` で配置。**深度帯（浅3／中5／深3／深淵1）・極（loss5/grudge4/myth3）・継承可（guard_relic/leave_will）5体**を散らし、`rollEncounter`（深度近傍しか拾わない）が**どの深度でも常に複数候補**を立てる。名簿(`adventurers.json`)の**生者とは別人**＝「街に生きる者／迷宮に骨となった者」の層。全員 epithet 付き（名簿 ASSERT 4-2）。代表＝アウレル〈黄金の烙印〉（深淵44・myth・奉献の象徴）／沈黙のヴァイス〈鎮める者〉（深33・myth・鎮魂派）／シオン〈実験者〉（深30・grudge・術を遺す）／クラン〈師父〉（中16・loss・継承の源流）／カルト〈遺された家族〉（浅7・loss・継承）等。`level`/`stats` を記録＝系譜継承の配分基準。**既存セーブは grandfather**（シードは新規 world のみ・旧 world は従来の化石を保持）。
- **② 生者→化石ループ＝縁を結んだ相手のみ（トリガー b）：** 相棒(`fossilizeCompanion`)・見捨て(`fossilizeAbandoned`)とは別経路。**自分が斃れた世代の合間に、その代が絆(value≥1)を結んだ生者NPC（`world.actors`）のうち最大1体が、深みで還らず化石化**（`world.ts fossilizeBondedActor`／`maybeFossilizeBondedActor`＝`fossilizeCurrent` 内・世代を進める直前）。**確率＝縁ある生者がいる世代に約50%で1体（`BONDED_FALL_CHANCE`）**＝知らぬ他人を撒くのでなく「**会って縁を結んだ相手が化石になる**」＝再会が刺さり、(A) の感情エンジンそのものになる。**自己調整性**＝絆が前提ゆえ遊ぶほど化石が増え、関わらねば静かなまま（水増しにならない）。**極**＝多くは静かな喪失、高位(grade≥3)は神話、稀に怨念（4-2 両極は稀）。**深度**＝等級が高いほど深部で果てる（`BONDED_FALL_DEPTH`±3）。化石化した者は `world.actors` から除去（街での生存と矛盾させない）。**決定論**＝seed×世代のハッシュ（rng 非依存＝stress-save/determinism 安全）。
- **③ 再会の固有性：** `Fossil.wasAlly`（任意・非破壊）を立て、`fossilScene`／ボス決着で「**かつて街で、迷宮で、言葉を交わした者だ**」の一言＋出会えば「名を呼ぶ」第三選択（`wasCompanion` の仕組みを bonded 一般へ拡張）。
- **波及：** golden は `worldLifecycle`（シード2→12でのみ更新＝意図的な決定論変化／genFloor/monsterAI 等は不変＝ボス階を避けるため）。CLI/demo もシードを共有（demo の放置化石参照は「踏破者レン」→「シオン」へ更新）。**数値（`BONDED_FALL_CHANCE 0.5`・深度基準・極の閾値）はテストプレイ調整候補。**

**(G) 世代を束ねる＝退隠・世界クロック・家系図（★設計確定 2026-06-28・ユーザー承認・段階実装）**
**課題：** 「世代を跨ぐほど熱くなる」設計（弧・化石変質・生者→化石ループ・系譜は全て `world.generation` 起点）だが、**generation はプレイヤーの死でしか進まない**＝(1)易しすぎると世界が回らず、(2)**うまいプレイヤーは死を回避＝世界が完全に凍結**（`advanceArcs` は `fossilizeCurrent` のみ・`computeVariation` も generation 差分）。**方針＝(B) 多世代の家系体験を主役に据える**（縦の単独クリアでなく、王朝を束ねる）。難易度（normal の序盤）は据え置き＝歯応えとして適切（戦闘シムで easy 比 約2.5倍の噛み・1v1は死亡0%＝健全と確認）。
- **層2（主役）＝退隠／襲名：** 死（不本意・遺産散逸・grudge/loss 寄り）と対に、**街で自ら退く選択**を置く。`generation+1`・系譜が heir へ・先代は**生ける守護者NPC**として街に残る（retire 弧インフラ `refreshRetireGuardians`/`guardianScene` を再利用＝再雇用可で「先代と並んで潜る」）。**亡骸を残さない＝迷宮遭遇から除外**（`retired` フラグ）。**★ボーナスは功績比例（雪だるま防止の本命）**＝退いたキャラの功績（`worldAchievement`＝印×2＋伝説×2＋クリア×4＋依頼）に応じ、(a)継ぐ術数が増え最大は全術／(b)装備の直接相続（印1つ以上 or 一定深度のみ）／(c)守護者＋恒久の薫陶（伝説級のみ）。駆け出しを退かせても殆ど継げない＝cheese 不能。各退隠は**そのキャラの奉献チャンスを捨てる**機会損失も併存＝「育て上げてから冠を譲る」が最適。
- **層1（背景・従）＝世界クロック（★Phase 2 実装済み・v0.58.0）：** `worldTime = generation + eraBeats`（`eraBeats`＝新フィールド・任意）を時間系（変質 `computeVariation`・弧 `advanceArcs`・遭遇重み `encounterWeight`）が参照＝死を回避しても深部での営みで世界が老ける。**深度積分クロック**＝生還（街到着）ごと `gain = worldTickGain(maxDepth) = min(0.6, max(0,(maxDepth-8))/90)`・累積1で1ビート（最大2/生還）＝**浅層(≤8)の低層周回ではクロックが進まない**（ユーザー要件）。Bでは脇役ゆえ `DIV=90` 大きめ（深層D34≈3.5生還/ビート・テスト調整）。1ビート（`worldBeat`）＝`advanceArcs`（弧前進・doom 終端化石）＋縁ループ抽選（`maybeFossilizeBondedActor`・tag で死/世界ビートを区別）。**★①フロンティア相対**＝`Fossil.frontierHeld`（シード/doom終端/縁ループ）は `reachedAt`（初到達 worldTime）が立つまで `computeVariation` で変質を凍結＝起点 `max(lastTouchedGeneration, reachedAt)`＝「**まず出会わせて、放置すれば歪む**」順序を保証（出会う前に alien 化を防止）。stamp＝`enterFloor` の `stampReached(depth)`（深度帯到達）＋`recordRediscovery`（遭遇）。**喪失の可視化**＝世界ビートで縁者が還ったら `tickWorldClock` が「お前が生き延びる間に〇〇は深みへ還った」を即ログ。発火＝`surfaceReturn`（階段/帰還詠唱/奉献）＋`returnViaDoor`。`World.eraBeats/eraClock/diveMaxDepth`・`Fossil.reachedAt/frontierHeld` 追加（任意・SAVE_VERSION 据置）。**CLI/demo/golden は `eraBeats=0`＝worldTime==generation＝golden 8/8 不変（裏取り済み）。** 数値（`DIV=90`・上限0.6・最大2ビート/生還）はテストプレイ調整候補。
- **層3＝家系図（自宅「系譜の間」・★Phase 3 実装済み・v0.59.0）：** 多世代の重みを**見える化**（Rogue Legacy の家系図/館が参照）。自宅（`home`）に act「系譜の間（家系図を見る）」＝`lineageHallScene`＝歴代当主（`world.fossils` の kind=character＝死者＋退隠）を世代順に「どう終えたか（深度Xで斃れた＝極／伝説として退いた＝守護者）＋遺品＋**今どう変わっているか**（`computeVariation` の風化/歪み/異形）」で連ねる。**家格**＝`houseRank`（退隠×2＋クリア×3＋伝説×1→5段）を表示＝家の誉れ。**家格の恩恵（微小）**＝`characterCreation` で次の当主が家の蓄え（治癒の膏薬×min(3,tier)＋餞別 tier×5金貨）を携えて発つ（web のみ・additive・終始シビアを崩さない小ささ）。web＋content（town.json home act 追加）のみ・engine 非依存＝golden 8/8 不変。**★バグ修正（v0.137.0・2026-07-08・テストプレイFB「系譜の間に自分でないキャラが混ざる」）＝** `lineageHallScene` と `characterCreation` の先祖抽出が `kind==='character'` 全てを拾い、**死んだ相棒（wasCompanion）・縁を結んだ生者NPC（wasAlly）＝同 kind だが自分の血統でない化石**まで歴代当主／継承候補に混入していた（縁者化石は自死化石の後に push されるため継承候補の先頭を奪う恐れも）。`isOwnLineFossil`（kind===character && !wasCompanion && !wasAlly）で自分の血統のみに限定＝系譜の間の一覧・歴代人数・血縁/弟子/襲名の継承候補を是正。web 限定・golden 8/8 不変。
- **層4＝貴族街の館（終盤メタ・★Phase 4 実装済み・v0.60.0）：** 家格が街の地図に出る到達点。**自宅機能の格上げ**＝`world.manorUnlocked`・解放条件 `manorEligible`（奉献 `ascended≥1` or 家格 tier4+）→ 一度きり `maybeGrantManor`（ギルド/自宅/館/謁見入場で発火）。効果＝保管庫拡張（`STASH_CAP 60→100`）・相続枠拡張（`STASH_INHERIT 4→8`＝engine `fossilizeCurrent` が `world.manorUnlocked` で分岐・golden 安全）・系譜の間が大広間に・家の恩恵手厚く（膏薬上限 3→4）。`World.manorUnlocked`・`STASH_CAP_MANOR/STASH_INHERIT_MANOR`。golden 8/8 不変。
- **層4b＝歩ける貴族街区画（クリア後の楽しみ・★実装済み・v0.61.0）：** 4-12 J「専用の歩ける貴族街区画＝将来拡張」を実体化。**奉献クリア（`nobleQuarterOpen()`＝`ascended≥1`）で貴族街の門が開き、歩いて入れる区画**に＝`townAct` が解禁時 `ngate`/`noble` タイルを歩行可に＋`refreshNobleQuarter`（`townLoop`）が門番を guardMap から退かせる。区画に**自家の館**（`kind:"manor"`＝`homeDeposit`/`homeWithdraw`/`lineageHallScene` を再利用＝`world.stash` 共有で自宅と二重化なし）＋**謁見の間**（`kind:"audience"`＝`audienceScene`＝領主の戴き/当主ごと一度きり客人の礼＋`questBoard("noble")`＝統治者の大命 `generateNobleOffers`）。**豪華演出**＝噴水/庭園/像/旗を `town.json` の props/buildings/interiors に追加＝**100% データ駆動＋既存 `drawTown`＝Swift 移植安全**（TS 限定演出なし）。`townscene.ts buildTownGrid` は noble 矩形を「床」のみに敷き建物 footprint を保護。`#243` の館 glyph（`refreshManorMark`）は入れる建物に発展＝廃止。**Lv45「原初の証」アークはクリア前の導入として温存**（別軸両立）。web 限定・engine 非依存＝golden 8/8 不変。**★残（貴族街・後続バッチ＝ROADMAP に TODO 化）＝貴族街限定の新規クエスト型/謁見イベント群/貴族NPC群の量産・Lv45 アーク作り直し検討・更なる美術。**
- **層4b・後続バッチ1＝宮廷に生命を（★実装済み・v0.62.0）：** "noble" storylet context 新設＋宮廷NPC（家令/廷臣/再会の客人＝歴代の伝説・退隠者を招く＝`refreshNobleQuarter`/`courtNpcScene`）＋謁見の話題ローテ（`audienceScene`）＋starter noble storylets 18件＋大命タイトル多彩化。web+content・golden 8/8 不変。
- **層4b・後続バッチ2＝高難度の大命＋深度50超の難易度逓増（★実装済み・v0.63.0）：** 統治者の大命を「かなり高難度」に＋「深度50より下にも難易度設定を」（ユーザー要望）。**①深度50超の難易度逓増（engine・golden 安全）**＝`dungeon.ts abyssalScale(depth)=depth<=50?1:1+(depth-50)×ABYSSAL_K(0.05)` を `scaleKind`（雑魚 HP/dmg）/`makeAreaBoss`（ボス）/`spawnPursuer`（追手）に乗算＋敵数上限を深度50超で緩和。`depthHpBonus/depthDmgBonus` は元から上限なしで線形に難化していたが、クリア後の強プレイヤーには緩いので深部に上乗せ＝真のエンドゲーム。**depth≤50 は厳密 1.0＝完全不変**（golden は genFloor/monsterAI を ≤42 のみ指紋化＝`abyssalScale=1.0`＝golden 8/8 不変・裏取り済み）。**②高難度大命**（`generateNobleOffers` 刷新）＝**新 `Quest.kind:"slay"`**（深度56/64 の《成れの果て》撃破＝`onSlayBoss` 深度ベース完了＝`pickBossSource` 非改修＝golden 安全）／reclaim（深度38+ の最深化石を名指し）／descend（深度52-58）。報酬＝大金貨＋実績厚（`claimQuest` で noble は `questsDone+=2`）＋稀（rng<0.33）に固有報酬 `rewardRelic`（+3 遺物＋称号「統治者の覇者」）。配信＝謁見の間 `questBoard("noble")`＋ギルド相乗り。`Quest.kind:"slay"`/`Quest.rewardRelic?` 追加（任意・非破壊・SAVE_VERSION 据置）。**★報酬カーブ是正（v0.63.4）：** 旧式（全型 ×2.2・`NOBLE_REWARD_CAP=700`）は全パターンが上限700に張り付き、素値では到達のみの descend が最難の slay を上回る難度逆転だった。**難度順 slay(×22)>reclaim(×2.4)>descend(×12)・`NOBLE_REWARD_CAP=1500`** に是正＝金貨が難度差を反映（slay 1232-1408＞reclaim 778-1008＞descend 624-696・上限張付ゼロ・golden 8/8 不変）。数値（`ABYSSAL_K`・報酬係数/上限・深度キャップ）はテストプレイ調整候補。
- **実装順（完了）：** Phase1 層2（退隠 v0.57.0）→ Phase2 層1（世界クロック v0.58.0）→ Phase3 家系図（自宅 v0.59.0）→ Phase4 貴族街の館（v0.60.0）→ Phase4b 歩ける貴族街区画（v0.61.0）→ 後続バッチ1 宮廷NPC/noble context（v0.62.0）→ 後続バッチ2 高難度大命/深度50超逓増（v0.63.0）。各 web 中心・`npm run check`／golden 8/8 裏取り。数値（`DIV=90`・退隠ゲート `playerGrade≥2`・襲名 `2+ach/2`・家格しきい値・館 `STASH_*_MANOR`・`ABYSSAL_K`・大命報酬）はテストプレイ調整候補。

---

### 4-15. メインの縦糸「アウレルの遺した問い」 — ★確定★（2026-07-04 ユーザー承認・v0.114.0 実装済み）

エマージェントな個人史（モデルC）が主役である本作に、**「重い一本道」ではなく「謎の縦糸（mystery spine）」**を一本通す。ユーザー相談「基本は自分自身のストーリーを作るデザインだが、単調になる可能性があるのでメインとなるストーリーの骨格があってもいい」への回答＝奉献の試練（4-13）という機械的な背骨に、**意味の背骨**を沿わせる。

**(A) 設計原則**
- **答えは出さない（鉄則）。** 4-3①「底は永久に未到達」を守る＝各章は謎を「解く」のでなく「深める」。最終章でも欠けた頁は埋まらない（フロム式＝理解そのものが報酬・解釈は人の数だけ）。
- **既存の進行に意味を上乗せするだけ**＝プレイの手順・テンポは変えない。章のゲートは全て既存マイルストン（書記訪問・深度15・教団接触・5印・深淵帯・奉献）。
- **世代テーマの補強**＝章は World 蓄積（`World.aurelChapter` 0..6・世代越え）＝「解読を継ぐ家系」。終章後は家系図に一行（「欠けた頁の、次の頁を書き続ける家系」）。
- 素材は `adventurers.md` の設計済みキャスト（M1 アウレル＝始祖／P1 ミラ・片目＝深淵帯の生き証人／M4 ライラ・境＝生ける神話・世界の果ての証人）を実体化。

**(B) 六章の構成（トリガー→内容）**
1. **序章→第一章「欠けた頁」**＝書記の館の初回訪問（`maybeAurelIntro`）：イェンが年代記の失われた冒頭を見せる。始祖は名を失い「烙印（アウレル）」とだけ伝わる。
2. **第二章「痕」**＝深度15以深のフロアに遺構ランドマーク「痕」（金・`floor.aurelSite kind:"mark"`・確定配置＝縦糸の灯台）。bump で断章＝『名を残すな。おれの代わりに、底を見ろ』。
3. **第三章「声」**＝教団の戸をくぐる（`maybeAurelCult`）：教主が素の声で語る＝「深淵の底から、誰かがずっと呼んでいる。確かめた者だけが、いない」＝教団 lore の裏面。
4. **第四章「導き」**＝5印が揃った迷宮の口（`maybeAurelMira`・`promptDescend` フック）：**ミラ・片目が生きていれば本人**が骨片と伝言を託す／**化石化していれば遺した手記**が代わりに導く（モデルC 整合＝世界の状態が語りを変える）。
5. **第五章「境」**＝深淵帯のフロアに「境」（菫・`kind:"laila"`）。ライラ・境＝岩壁に半ば溶けた生ける神話との邂逅＝『問いのほうが、あの人を選んだ』。
6. **終章「継がれる問い」**＝奉献達成後（`ascended≥1`）の書記の館（`maybeAurelFinale`）：頁は埋まらない。『問いは、答えより長生きする』＝問いが家系に継がれて幕。

**(C) 実装（web限定・engine 非改変・golden 8/8 不変）**
- `World.aurelChapter?: number`（任意＝旧セーブ非破壊・`migrateWorld` で 0 バックフィル）／`Floor.aurelSite?`（任意＝web が enterFloor 初訪時に配置・engine は触らない・DiveSnapshot の floorCache で自然に永続）。
- シーン＝`web/main.ts` の固定シーン群（`maybeAurelIntro/aurelMarkScene/maybeAurelCult/maybeAurelMira/aurelLailaScene/maybeAurelFinale`）＋各章 `chronicle("legend")`。
- **可視化**＝「進行中の事ども」（eventsScreen）に常設セクション（読んだ章数＋次にどこへ行くかのヒント1行）＝縦糸の迷子防止。盤上グリフ「痕」（金）「境」（菫）＋peek＋凡例。
- 数値（`AUREL_MARK_DEPTH=15`）はテストプレイ調整候補。報酬は物語のみ（バランス中立＝第四章の trait「伝言」は純記憶）。

---

## 5. 残課題と次の一手

**確定済みの骨格：** 世界観（4-3）・永続性（モデルC）・変質（4-1/4-2）・英雄（4-4）・提示形態（4-5）・存在ティアとlazy動態（4-6）・再会重み（4-7）・実装/商用アーキテクチャ（4-9：実行時LLM排除・オフライン二層・買い切り）・追加システム（4-10 A〜I）・戦闘（4-11）・遭遇イベント/ストーリーレット駆動（4-12）・クリア/長期目標（4-13）・冒険者イベントと同行（4-14）。第2回通し点検（2026-06-12：4-9整合の残存矛盾の掃除＋他作品リサーチ）も反映済み。

**残課題（小）：**
- 死に様トリガーの細部数値化（方向は 4-10B「死の瞬間の一手」で確定。実装時に詰める）
- 重ね書き（複合変質）／抑止力メカニクス化／浅層の生存中エントロピー — 将来フック
- モデルD（プレイヤー横断の共有世界）— 将来構想

**要・設計確定：**
- **クリア／長期目標 → ★確定（4-13「奉献の試練」）★（2026-06-15）。** 「底は永久に未到達」（4-3①）を維持しつつ、①5印（多様な偉業・World 蓄積）で深淵帯を解錠＋②帰還の試練（聖遺物を地上へ生還）の二本柱。詳細は **4-13** 参照。実装は段階的（印の収集→深淵帯/聖遺物→帰還の試練→メタ達成）。

**次の一手＝二本のトラックを並走させる：**
- **戦闘（4-11）：** ①テレグラフ＋決定論戦闘【完了・PR #10】 → ②ステ/レベル → ③深蝕魔法 → ④装備 → ⑤鎮め筋。
- **遭遇イベント（4-12）：** 遭-①〈調べる〉【完了・PR #13】→ 遭-②〈捜索〉＋伏線還流【完了・PR #15】→ **イベント拡充**（4-12(F)(G)(H)：コンテキスト別に大量追加。まず化石遭遇＋ダンジョン文脈）→ 遭-③（依頼・戦闘結線）→ 遭-④（山場SetPiece）。〈戦う〉動詞で 4-11 と合流する。

**完了済み＝プロトタイプの最小仕様化（4-9 前提）：**
1. **化石のデータモデル**（永続データ層のスキーマ。深蝕量(4-10C)・死の一手(4-10B)・系譜(4-10D)を含める）
2. **鋳造所の断片スキーマ＋文法/ストーリーレットの最小形**（山場の予約セットピース含む）
3. **最小ループ**：潜行 → 深蝕 → 死（最後の一手）→ 化石化 → 世代交代（系譜選択）→ 再発見 → 干渉 → 年代記に記載。**この一周が情緒的に刺されば、設計の核は正しい。**
